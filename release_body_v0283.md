## v0.28.3 · 真机四修：灰度思考显示 + 方言串味根治 + 头顶工作气泡 + 团队/技能库修复

（本版含 0.28.0 ~ 0.28.3 全部能力）

### 一、回复丢字根治 + DeepSeek 式灰度思考
- **丢字根因**：云端模型有时把思考以内嵌 `<think>…</think>` 混在正文里返回，聊天窗把它当
  未知 HTML 标签**连同内容一起吞掉**——这就是"部分文字丢失"；独立的 `reasoning_content`
  思考字段也一直被静默丢弃。
- **修复**：网关新增流式思考拆分器（跨网络分包安全），把思考剥到独立通道；非流式路径兜底剥离。
- **新体验**：思考以**灰度小字实时上屏**（💭 前缀），正文随后流出，最终态保留
  "灰度思考 + Markdown 正文"，与 DeepSeek 官方一致。

### 二、方言串味根治（粤语 → 东北味）
- **根因**：soft 词表全是普通话高频单字（东北 整/贼/老/啥/咋、粤语 啦、北京 儿），河南 markers
  还有裸「中」——普通话聊几句就被偷偷攒分带歪口音与文风。
- **修复**：词表全部收紧为方言独有强词，切音改为计数取最大；普通话归普通话，
  七地方言（粤语/台湾/四川/河南/东北/山东/北京）照常识别与朗读。

### 三、小人工作提示 → 头顶冒泡气泡
- 工作提示（如「与XX聊天」）从头像**下方**的矩形改到**头顶上方**的冒泡气泡：
  白底圆角 + 青色描边 + 向下小尾巴 + 柔和投影；头像逐像素与旧版一致。

### 四、团队功能无反应 + 技能库为空 根治（历史遗留，重点）
- **团队无反应**：打包缺陷致 `pasm` 引擎包从未进安装包——`import agent_team` 在安装版静默失败，
  **团队/数学脑/感知/自修复等自 v0.24 起在所有发行版里实际失效**。
- **技能库为空**：0.28.2/0.28.3 安装包构建漏掉 `skills/` 数据目录，内置 8 篇技能全部缺失。
- **修复**：spec 增加 pathex + 显式 hiddenimports，按 spec 正规重建并逐模块校验
  （PyInstaller 官方 archive reader 读包内模块表：`pasm.cognitive.*` 全在、skills 8 篇齐全、
  对 0.28.1 基线为严格超集）。**团队功能在安装版全面恢复可用。**

### 验证
- accent / pet_behavior 自检 PASS；思考拆分器 5 组用例全过；气泡离屏渲染正常；
- 安装包逐模块符号校验齐全。

**下载**：附件 `PASMStudio-Setup-0.28.3.exe`（约 51MB，Win10/11）
