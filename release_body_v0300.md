# PASM Studio v0.30.0 · 3D 机甲小人 + 栏目整合 + 六项工作能力

> 这一版是**形象、栏目、工作能力**三块一起动的版本。
> 下载：`PASMStudio-Setup-0.30.0.exe`（约 75.8 MB，Windows 10/11 x64）

---

## 一、小人有立体感了，而且**真的会动**

桌面和界面里的小人换成了 **GPU 真 3D 机甲造型**：

- **造型**：外壳分片（弧形甲片 + 倒角甲块 + 鳍脊 + 尖锥）、金属质感、清漆层高光、
  胸口会发光的徽记；头 / 躯干 / 肩臂肘腕手 / 髋腿膝脚**共 60+ 部件**完整人形
- **它会活**：呼吸、身体摇摆、头顶天线轻晃，还会**跟着鼠标方向转头看你**
- **六个动作看得清**：静止 / 走路 / 挥手 / 跳跃 / 跳舞 / 思考 / 抱球
- **会成长**：四档从"圆头圆脑"长到"修长青年"，身高体态逐级变化，
  青年期还会长出**肩甲与天线**（此前那个形态因为一个档位错位的 bug，**永远取不到**）

两个实现上的讲究：

1. **零新增依赖**。用的是 PySide6 自带的 OpenGL 封装类，没有引入 PyOpenGL / numpy /
   QtQuick3D（后者会把安装包推高约 150 MB）。
2. **形体全部程序化生成**，不依赖外部 3D 模型资产 —— 所以它能随成长档位**连续变形**，
   而不是几个固定模型之间切换。

**万一你的机器跑不了 3D**，它会自动退回原来的 2D 画法，绝不崩。

> 顺带修掉一个"看不见"的缺陷：早先的实现用的是"定格姿势"接口，而它不推进动画时钟，
> 导致小人是**一尊静止的雕塑** —— 外观是立体的、动作却是死的（实测连续帧差异 0.00%，
> 而 2D 是 5.10%）。改成按真实时间推进后，静止 0.66% / 挥手 5.79% / 跳舞 16.03%。

---

## 二、工作栏清爽了：4 个按钮 → 1 个入口

原来「✍️ 文案 / 📊 表格 / 📽 PPT / 📄 Word」是四个并列按钮，现在收成一个
**「📄 文档与演示」**：点它，在按钮下方弹出**卡片式展示框**，
四种格式各带一句说明、当前格式高亮。

- 平时**不占布局高度**（老做法是按钮下方常驻一行子标签）
- **点面板外部自动关闭**，屏幕边缘点击会自动回拉，不会弹到屏幕外
- 依据很简单：**格式不是意图，是交付选项**。你要的是"一份能给客户看的东西"，
  不该被迫先做一次格式决策

**显式指定依然优先**：直接说「给我个 Word」，它照样直接开工，不弹选择器。

**老状态零破坏**：内部的工种标识一个都没改，所以老配置的工作模式、老会话、
`/mode` 命令全部照旧 —— 升级不会让你丢任何东西。

---

## 三、新增六项工作能力

| 能力 | 说明 |
|---|---|
| **📎 聊天框「＋」** | 图片 / 文档 / 任意文件 / 截屏。Word、PPT、Excel 会**真读正文与表格**（不只是认扩展名），所以可以直接说"照这份报表做个 PPT"。单文件截断 6000 字、单次最多 3 个，坏文件只提示不崩 |
| **🔐 权限三档** | **安全**（默认，写文件 / 执行命令都先问）· **标准**（低风险放行、高危确认）· **完全访问**（全放行，切换有明确风险提示 + 二次确认）。**拿不准一律停下来问你**，不自己猜 |
| **✨ 页面转场动效** | 八个页面切换带入场动效，连切不错乱、动画期间不阻塞流式输出 |
| **🎙 语音唤醒** | 直接说「小U」就醒。**默认关闭**（占用麦克风是隐私敏感行为，设置里打开才启用）；助手说话时不会自唤醒；**改了名字唤醒词立刻跟着变**，不用重启 |
| **🚀 开机自动启动** | 设置里一键开关，写当前用户注册表，**不需要管理员权限**，开机不弹黑框 |
| **🏷 名字统一「小U」** | 桌宠 / 设置 / 知识库 / 语音各处兜底值统一；并加一次性迁移 —— **你自己改过的名字不会被覆盖** |

---

## 四、工程侧（给关心实现的人）

- **新增守门**：成长契约 12 项（防止"档位错位"这类问题再发生）
- **分叉守卫**登记 7 个新模块，并把**登记判据**写进脚本注释 ——
  它不该沦为"绕过警告的白名单"
- **打包防漏**：`PASMStudio.spec` 补 15 条 `hiddenimports`。
  新模块多为**函数内延迟导入**（省启动时间），PyInstaller 静态分析看不见 ——
  不声明就会在安装包里**静默丢功能**（3D 降级、点按钮没反应），一个字都不报错
- **修了一处假 FAIL**：核心完整性守门把"本机没装 numpy"报成"核心不完整"。
  已改为判 SKIP 并点明缺了什么 —— 假红和假绿一样会把人带偏

---

## 五、升级与兼容

- 直接覆盖安装即可；配置文件、会话记录、记忆数据**全部保留**
- 默认名统一为「小U」，但**你自定义过的名字不会被改**
- 卸载重装不会丢数据（数据在 `%APPDATA%\PASMStudio`）

**自检汇总**：分叉守卫 0 警告 · 核心完整性 41 项 0 失败 · 成长契约 12/12 ·
六个新模块 15+14+33+8+9+7 项全通过 · 栏目整合端到端 19/19 · 3D 运动诊断全部"会动"
