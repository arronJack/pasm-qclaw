# PASM Studio v0.30.2

**修「聊天内容会丢」+ 不再编造「已执行」**（含 0.30.1 未发布批次）

---

## 你报的这两个问题都修好了

### 一、自己发的话有时会丢（不见了，切走再切回又出现）

**原因**：回复是逐字蹦出来的（约 1.2 秒），而这段时间输入框已经能打字。
你抢在它蹦完之前发一条，那条消息就会在下一帧被它自己的动画覆盖掉。
消息其实一直存着 —— 所以换到别的对话再换回来又会冒出来，看着像灵异事件。

**现在**：它先确认「这一段是不是我自己的」，不是就另起一段，**绝不覆盖你的话**；
你抢话时它会把没说完的半句**立刻补完整**再停（不会停在半句上）。

### 二、「已生成并打开浏览器」是编的

**核查结论：确实没有执行。** 三条依据：那条是纯文本回复、措辞与程序里任何真实返回值都不同；
普通聊天栏目给它的工具只有记忆类（回忆 / 自检 / 记住 / 情绪），
**没有写文件、运行代码、打开浏览器的能力**；电脑上也确实没有任何新生成的网页文件。

**现在**：每件真事都记一笔账（记账用装饰器包在函数外层，**不可能漏记**），
说了「已生成 / 已打开 / 已运行 / 已保存」却查不到记录时，它会自己更正为「我并没有真的做到」。

> 判据刻意保守：只认「动作词 + 交付物词**同句**出现」，且「打开过 / 生成过」这类
> 讲**过去经历**的一律放行 —— **宁可漏拦，也绝不误伤正确回复**。

---

## 还包含（0.30.1 批次，此前未对外发布）

- **走路先转身**：不再正面朝前横向平移；转身未完成时会收小步幅，走完自动转回正面
- **动作 7 → 16 个**：新增伸懒腰 / 抱臂 / 弹跳撒欢 / 点头致意 / **飞天转圈**
- **动作跟着性格抽**：九种性格各有偏好（调皮灵动爱颠球跳舞，温和沉稳爱点头思考……）；
  超过 90 秒没人理它，还会自己飞天转个圈
- **换形象会同步**：桌宠换了皮肤，聊天区的小人立刻跟着变
- 修「＋」附件上传报「打开文件选择框失败」

---

## 顺带修掉的两个隐藏问题（都属"不报错但很伤"）

- **日志整整一个版本没有写下来**：`logging.basicConfig` 在已有 handler 时**静默失效**，
  而启动时的迁移日志恰好先跑了一步 —— 于是日志文件一直是空的，排查用户问题时无据可依。
  现在日志初始化抽成独立模块、**入口最先执行**，并**装完自检真写、失败留痕**。
- **数据目录会分家**：从终端等方式启动时可能把数据写到另一个目录（双击启动却不会），
  两个目录会同时被写。已改用系统标准路径（Win32 API），两边统一。

---

## 安装

1. 下载 `PASMStudio-Setup-0.30.2.exe`（约 75.8 MB）
2. 双击安装 → 打开 PASM Studio → 设置里填 LLM Key（或留空用本地 Ollama）
3. 应用内「检查更新」也会收到这一版

> 系统要求：**Windows 10/11 x64** · CPU 即可 · 首次安装解压约 1 分钟
