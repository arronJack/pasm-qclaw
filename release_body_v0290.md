## v0.29.0 · 记忆与预演：修掉「悄悄丢记忆」的缺陷 + 新增事实层与世界模型 + 找回跨表述记忆

（本版同时含 0.28.6 / 0.28.7 / 0.28.8 全部能力）

### 一、修掉一个正在悄悄丢记忆的缺陷（本版最要紧的一条）
- **问题**：桌面端此前**自己留了一份记忆实现**，而且是**拷贝**、不是同一份。同一件事在两份副本里
  各记各的，写进这份的那份看不见 —— 表现为「聊天时**有时记得、有时不记得**」（半失忆），
  而且**不报任何错、不崩溃**，只丢数据。
- **修复**：收敛为**唯一真相源**（核心包持有实现，桌面侧只留薄壳再导出）。
- **防复发**：加了两道自动守门 ——
  - **结构侧** `tools/check_core_fork.py`：校验 12 个薄壳的运行时身份指向核心本体，
    并校验 **PyInstaller 打包声明完整**（这类模块是动态加载的，漏声明**只在安装版**表现为"功能没反应"）；
  - **行为侧** `tools/audit_memory_worldmodel.py`：30 项对抗式断言（见下）。
- **实证**：容量 200 的记忆库被 **260 条无关闲聊**灌满后，**三条早期重要记忆一条没丢**。

### 二、新增「事实层」：关键事实记得住，改口会留痕
- 专门记住 **姓名 / 年龄 / 住址 / 用药 / 过敏 / 家人电话** 这类关键事实，与闲聊记忆**分开存**，
  保证不被日常对话挤掉（此前闲聊多了会把要紧事冲走）。
- **双时态**：从「北京」改说「上海」，旧事实**不是删掉**，而是标记为已失效 ——
  历史可查、可回放，检索只返回**当前有效**的那条。
- **可信度保护（置信门）**：把握不大的新说法**不会**盖掉把握很大的旧事实，
  而是先**挂起待确认**（这一状态在记忆面板里单独计数，不再"悄悄被覆盖"）。
- **口语问法能命中**：问「我住哪儿」「吃什么药」「对啥过敏」都能查到对应事实。
- **本轮对抗性复核修掉的 5 处实际缺陷**：
  1. **单字别名假阳性** —— 别名表里的单字「住」会让「记住我明天要去医院**复查**」被误判成住址事实；
  2. **跨关系串味** —— 问「儿子是谁」会把「**老伴**」一起带出来；
  3. **同句多事实取错** —— 「我住北京，也住过上海」保留了**先说的**（应后说的为准）；
  4. **低把握覆盖高把握** —— 0.3 把握的「过敏=花粉」会顶掉 0.95 把握的「过敏=青霉素」；
  5. **重复写入留下假历史** —— 同值重复写会白留一条失效记录。
- 附带：别名表升级后，库里的旧条目会**自动重算**检索键（词表演进不埋雷）。

### 三、新增「世界模型」：办事前先预演一下
- 同类处境**做过几次、成过几次** → 给出**成功率估计**与**瓶颈提示**
  （如「成功率约 83%」；多步任务会指出卡在哪一步）。
- **知道就说、不知道就说不知道**：只做过 1 次不会给出满把握；完全没有经验时**如实返回不确定**，
  不给虚假确定。
- 本轮补了两处让领域经验真正生效的改动：
  - 新识别 **健康 / 家人 / 生活** 三类日常处境词 —— 此前「买菜 / 去医院 / 给女儿打电话」
    会被归成「未分类」，领域经验等于白存；
  - 新增**第三层回退**：「带伞」这个动作在**别的处境**下有经验时，给一个**弱提示**而不是一律 50%
    （用强收缩，最弱一档证据，绝不与直接经验同权）。

### 四、记忆检索：换种说法也找得到
- **问题**：问「我叫什么名字」查不到存成「姓名」的事实 —— 两句话**一个字都不重合**。
- **修复**：补上「口语说法 → 事实字段」的对应通道。
- **实测**：该场景召回率 **50% → 100%**。
- **如实说明边界**：纯口语长句（如「您老家是哪儿的呀」）仍受本地词袋检索所限，
  需接入语义模型才能完全解决 —— 我们**没有**用改评测口径的方式把它"刷绿"。

### 五、同时包含 0.28.6 / 0.28.7 / 0.28.8 全部能力
- **0.28.6**：由「长期验证智能体」压出的三个核心缺陷（记忆同分排序崩溃 / 容量淘汰不分重要度 /
  反馈无法指定动作）；
- **0.28.7**：中文语言锁（复杂问题不再莫名答英文）+ 干活前确认门 + 浏览器自动化 + 实时天气
  + 语音稳定性 + PPT 插图；
- **0.28.8**：定时 / 周期任务、邮件连接器、日历连接器、跨端远程桥、自进化沉淀技能、多智能体协作层。

### 验证
- **全量验证套件**：**0 失败**（105 项通过 / 7 项已知缺口告警）。7 条告警全部是设计内的已知边界
  （安全底线层未实现、遗忘曲线未实现、纯口语问句受词袋限制等），**无一条是回归**；
- **记忆质量评测 24/24 全过**；**对抗式行为审计 30/30 通过**；
- **分叉守门**：12 个薄壳身份一致、打包声明 11/11 覆盖、0 警告 0 失败；
- **冻结环境模拟**（安装包实际走的降级路径）：契约校验通过、act / snapshot / 环境全可用；
- **真机启动冒烟**：冻结版正常启动无闪退；**桌面运行时冒烟 13 项全过**
  （真实窗口可构造、记忆面板可刷新、后台重放线程已拉起、系统提示含新增事实与预演块）；
- 打包产物收录检查：事实层 / 世界模型 / 记忆层等**全部确认已进包**。

**下载**：附件 `PASMStudio-Setup-0.29.0.exe`（约 77.5MB，Windows 10/11 x64，无需 API Key 也能用）

> 体积说明：本版 77.5MB，较 0.28.5 的 51MB 增加，来自 0.28.7 引入的**浏览器自动化运行时**。
