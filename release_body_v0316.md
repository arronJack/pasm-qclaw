# PASM Studio v0.31.6 · 过程看得见、干活会自我复查、连接器真的接上了

> 这一版把「像 WorkBuddy 一样工作」补齐了：**过程实时可见 → 干完自动复查 → 复查不过自己重来。**

---

## 一、过程卡对齐 WorkBuddy（聊天 + 干活都可见）

| 展示 | 效果 |
|---|---|
| **头部实时计时** | 干活中「过程 · N 步 · ⏳ 处理中 36s」每秒跳动 → 收口定格「✓ 已处理 2m36s」；你往上翻历史时不会被强制拉到底部 |
| **✍ 生成回复中…** | 首字一到就上屏，回复完成自动收口（不会再出现"僵尸行"） |
| **每步耗时** | 每步行尾灰字「· 3s」，一眼看出哪步最慢 |
| **深度思考上屏** | 推理型问题请求思考、闲聊不请求（响应速度不受影响）；思考过程灰度实时显示，状态条同步「💭 深度思考中…」 |

## 二、深度思考不再「想一小段就断了」

真机实录：思考只跑了 8 秒、313 字就被强制收敛（日志：`思考链未吐正文（超预算，已想 313 字）`）。
根因：**慢机（生成 < 15 tok/s）的思考预算被硬压到 8 秒**，而本机 Qwen3.5-4B 约 9.7 tok/s，每轮思考都被掐断。

| 改动 | 旧行为 | 新行为 |
|---|---|---|
| 慢机思考预算 | **8 秒**（只想了 313 字） | **20 秒** |
| 思考档设为「强制开」 | 照样被砍到 8 秒 | **给足 45 秒** —— 你明确要看深度思考，应用不再替你省时间 |
| 手动设预算 | 无此能力 | 说「**思考预算 60**」固定 60 秒、「**思考预算 自动**」恢复自动、问「**思考预算多久**」给当前策略 |
| 收敛过程透明化 | 只在日志里（用户以为"莫名断了一小段"） | 过程卡写明「思考到预算上限，已带着思路收敛作答 · 20s / 313 字」 |

优先级：**用户设的预算 > 强制开(45s) > 按本机速度自动收口(慢机 20s)**。

## 三、自我复查闭环（干活类与聊天对齐）

| 链路 | 旧行为 | 新行为 |
|---|---|---|
| **写脚本** | 失败只自动修 1 次 | 新模块 `self_verify`：最多 **3 轮**「语法验证（py_compile / node --check / json 解析）→ 真跑 → 带报错修复」 |
| **项目开发（多文件）** | 写完只运行一次，报错就交给你 | **语法校验 → 最多 2 轮只修出错文件 → 运行 → 运行失败再修一轮重跑**（修复跑通会诚实标注） |
| **多步计划执行 / 协作评审 / 创作企划** | 推理期间界面全黑 | 全部接上真实思考流 |

修复轮若没产出新代码会**诚实停止**：不假装修好，也不无限重试。

## 四、连接器真正关联应用

- **MCP 状态可查**：聊一句「MCP 状态」→ 桥连接状态 + 可用工具清单
- **任务完成自动推送**：向已连接的 **飞书 / Discord / 微信 / Webhook** 通道推送「✅ 任务完成」（没配的通道安静跳过；推送在后台线程，不影响应用速度）
- **破坏性操作前强制弹选项卡**：删除 / 清空 / 格式化文件、目录、项目之前先与你确认范围；「删掉这句话里的错别字」这类文字修改**不会误拦**

## 五、其它修复

- **老抖动根治**：`verify_v0310` 里 `location.href` 读到 `about:blank` 的竞态（历史基线同样偶发）已修，守门 **1184/1184 全绿无抖动**
- **顺带新增维护脚本** `tools/clean_build_artifacts.py`：一条命令清理构建产物（默认预演、自动保留最新 `dist_*`、`installer/` 从不触碰）

---

## 产物

- **Windows**：`PASMStudio-Setup-0.31.6.exe`（单文件，约 191 MB，sha256 前 12 位 `43f54d827e6b`）
- **Gitee**：分卷版 `PASMStudio-Setup-0.31.6-gitee.exe` + `.bin` 切片
  —— 把 `.exe` 与**全部 `.bin` 分卷下到同一目录**，直接运行 exe 即可（安装程序自己找分卷，无需手动合并）
- **macOS / Linux**：本版未重出（需 CI 跑 PyInstaller，本机不能交叉编译）

## 验证

`self_verify` 自检 15/15 · 项目流自我复查 AST 测试 12/12 · 过程卡叙述层 13/13 ·
全量守门 `run_all` **1184/1184 SUITE_PASS** · fork 守门 0 失败 · 跨仓同步 20/20 · 冻结启动冒烟 PASS
