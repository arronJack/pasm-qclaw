## v0.28.5 · 三层契约收敛：认知核心层落盘 + 引擎/环境/学习三层接口统一 + 文档补全

（本版含 0.28.4 全部能力）

### 一、认知核心层落盘 PASM 核心包（单一真相源）
- **问题**：符号推理 / 记忆路由 / 向量记忆 / 学习层此前只存在于桌面端，PASM 核心包从未包含，
  导致「只有 pasm-qclaw 实现了、PASM 核心没落盘」，且核心引擎与桌面端版本分裂。
- **修复**：四者提升为 `pasm.cognitive.*` 核心包（唯一实现），桌面端改为再导出薄壳，
  旧调用零改动。跨版本行为一致，为多端复用打好地基。
- 新增 `LearningEngine`：把桌面小人「自我设计 + 自我优化」抽象成可挂载到任意智能体的通用引擎。

### 二、桌面端改用引擎接口统一调用（「换引擎不改代码」真正生效）
- 新增桌面引擎工厂：按契约 `create_best()` **择优创建**，并返回**降级报告**
  （用了谁 / 试过谁 / 相对完整引擎缺哪些能力）——界面与日志如实告知当前跑的是
  完整引擎还是内置轻量链路，不再靠调用方自己写 try/except 猜。
- 原先写死在桌面端的 `PASMAgent / PASMConfig / GridWorld` 直连整段删除——
  **26 处调用点一行未改**，这正是「上层只依赖接口」的直接证明。

### 三、引擎接口契约升级 api 1.0 → 1.1
- 新增**环境插件注册表**（`EnvRegistry / register_env / make_env / env_conforms`）——换世界 = 换注册表里的名字；
- 环境契约刻意**不规定 `step` 返回形状**（教学版三元组 / 完整引擎四元组），由引擎自己适配它要跑的环境；
- 新增 `capability_gap()` 能力缺口报告；扁平参数适配（`create("pasm", seed=1, ...)`）；
- 契约自检 12 → **20 项**。

### 四、学习层收敛为「同一接口两档实现」
- 契约 `pasm.learning/1.0`：必需五件套 `info / capabilities / learn / bias / state`；
  full 档（离散动作 + 性格设计）与 teaching 档（连续向量关联式）**同接口、可互换**，
  并支持**运行时热插拔**（契约 / 潜维 / 动作数三项校验，不过则拒绝且**不动原实现**）。
- **踩到并修正的真坑**：两档 `learn()` 签名语义原本不一致，教学引擎换上核心档后跑 40 步
  `skills=0`——**学习完全没落进去，且不报任何错**。已统一为 `learn(z, action, reward)`。

### 五、PASM-Lite 引擎化补齐（v0.2.0 → v0.2.1）
- 环境插件：`grid-10x10`（网格）与 `toy-vector`（**非网格世界**，obs_dim=3 / n_actions=2）；
- 引擎读环境申报维度**自适应**调整感知、世界模型与规划器；
- `freeze_vae()` / `unfreeze_vae()` 对齐完整引擎语义；`save/load` 走契约，**档位不符明确报错不串档**；
- 实证：非网格世界实跑奖励 **-185.04 → -141.02**（真的在学）。

### 六、文档补全
- 《PASM 核心知识总览》：三层大脑体系 / 七层仿生认知架构 / 认知执行皮层 / 契约 / 目录地图 / 常见问题；
- 《PASM Studio 全部功能总览》：16 章 + 完整模块索引；
- 《PASM-Lite 全部功能总览》：实现了哪几块 / 四种用法 / 契约详解 / 诚实边界。

### 验证
- **冻结环境模拟**（安装包实际走的降级路径）8/8 通过：降级报告正确、契约校验通过、
  act / snapshot / env 全可用；
- 契约自检 20/20；两档 `learning_conforms(strict)` 通过；跨档互换验证 19 项全过；
- 两仓 `pasm/` 下 **37 个文件逐字一致**；打包产物启动冒烟通过；
- 安装包瘦身：清理构建中间残留，**63MB → 51MB**。

**下载**：附件 `PASMStudio-Setup-0.28.5.exe`（约 51MB，Windows 10/11 x64，无需 API Key 也能用）
