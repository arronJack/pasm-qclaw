# PASM Studio v0.30.3

**小人形象与行为修复：成长体型 / 头部放大 / 灵活性 / 自检修正**

---

## 这次修的是「长歪了」的几处

### 一、成长体型之前是反的

**原因**：`pet_avatar.py` 的 `G` 外观表里 `scale` 原随成长**递减**（越长大越小），看着像"缩水"。

**现在**：改成**递增**（幼儿 `0.98` → 青年 `1.16`），陪伴越久越高大，符合"长高长大"的直觉。

### 二、头部太小、看不清脸

- **2D 头像**：`G` 表头部尺寸整体放大；
- **3D 模型**：`pet3d/rig.py` 的 `PROFILES.head` 由 `0.158→0.095` 改为 `0.172→0.126`（青年期放大约 33%），躯干四肢仍随成长长高。

### 三、更灵动、更主动

- `pasm_pet.py` 的 `_maybe_hobby()` 触发间隔缩短（高能量 `22–46s→10–24s`、低能量 `34–80s→16–40s`）、触发概率提高（`0.82→0.9` / `0.5→0.62`）；
- `_sleep_tick()` 睡眠阈值 `45s → 150s`：你放着它，它自己也会动、会玩、会歇，不像块木头。

### 四、行为自检不再误报

`pet_behavior.py` 的 `_selftest()` 原本写死"满级 7 动作 / 0 级 3 动作"，动作集一扩就莫名 FAIL。
**现在**改成从 `ALL_ACTS` 动态推导（现 12 个动作、0 级解锁 5 个），以后加动作也不会再写死数字 FAIL。

### 五、数据目录彻底统一

`growth.py` 的 `DATA_DIR` 由 `os.environ.get("APPDATA") or ~` 改为复用 `logsetup.data_dir()`（Win32 权威解析），
与日志落盘口径一致，消除"双击启动写 AppData、其他启动写 home"导致的数据分裂。

---

### 验证

- `python pet_behavior.py` 自检 RESULT: PASS（满级 12 动作、0 级 5 基础动作）；
- 整个 `desktop/` 目录 `compileall` 通过；pet 文件仅在 `desktop/` 与 `desktop/pet3d/`，`pasm/cognitive/` 无同名孪生，不触发分叉铁律。

---

## 安装

1. 下载 `PASMStudio-Setup-0.30.3.exe`（约 75.8 MB）
2. 双击安装 → 打开 PASM Studio → 设置里填 LLM Key（或留空用本地 Ollama）
3. 应用内「检查更新」也会收到这一版

> 系统要求：**Windows 10/11 x64** · CPU 即可 · 首次安装解压约 1 分钟
