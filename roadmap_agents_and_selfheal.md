# 方向分析与方案设计 · ① 多智能体技能团队 ② 自检·自修·自升级

> 2026-09-08 · PASM / PASM-qclaw 路线规划（分析稿，非本期实现）
> 原则：先分析、给分级方案与落地顺序，拆成可逐步验证的小闭环，不一口吃成胖子。

## 方向一：技能互作 —— 把"一个 AI"变成"一支可按需组装的 Agent 团队"

### 1.1 现状与差距
现有「技能」= 单份规程文本（skillstore），由**一个 agent 顺序执行**：
用户一句需求 → LLM 读技能正文 → 一次成稿。缺点：
- 没有"角色分工"：没人做质量把关、没人做需求澄清；
- 复杂任务（开发→审查→修复）全靠一个 LLM 自己既当运动员又当裁判；
- 用户无法"点名"让不同角色接力，也没有可复用的流程模板。

### 1.2 目标形态（你要的样子）
用户能**自己添加"员工"（角色 Agent）**，每个员工有：职责卡(角色说明)、擅长技能、
接收/产出格式、质量红线。员工可自由**组队**并保存成**流程**，例如：

项目经理 → 策划 → 开发 → 审查 → 调试 → 交付
（需求澄清）（拆任务）（产出代码）（找问题）（修复回归）（收尾总结）

让团队整体为一件大事工作；用户中途可换人/插人/改流程。

### 1.3 落地设计（分四步，每步可独立交付）
| 阶段 | 做什么 | 复用什么 | 验证方式 |
|---|---|---|---|
| P1 角色卡 | skillstore 升级：新增 "role 卡"（描述/输入/输出/红线/话术），
  可内置 6 个种子角色(PM/策划/调度/开发/审查/调试)并支持用户添加 | planner.py、prompts.py | 冒烟：新建角色→用技能→产出含角色视角 |
| P2 流程编排 | 用户把角色串成"流水线 DAG"（顺序/条件跳转/循环），存成"团队流程"，
  支持"让我的团队做 X"一键跑 | planner.make_plan 规则化 | 冒烟：3 步链执行、失败单步重试 |
| P3 团队执行器 | 每步 = 角色卡 + 该角色调 LLM + PASM 校验；上一步产出作为下一步输入，
  审查者发现的问题自动回到开发者（带上下文），步间产物落盘可见 | _brain_coop(评审) | 全链路对拍：手写 vs 团队版质量分 |
| P4 团队沉淀 | 把跑通的团队+流程固化为"可复用部门"，跨用户分享(JSON) | knowledge/proc 记忆 | 用户可用性走查 |

关键设计决策：
- **角色 = 规程 + 视角**（不是独立进程）：复用现有单 LLM 网关，"员工"之间通过
  结构化交接单(需求/上下文/红线)传话 → 成本低、可插拔、坏一个角色可换人重跑。
- **让 PASM 当"调度员"**：选人、排序、判定"这步该交给谁/要不要返工"由 PASM 规则与
  记忆裁决，LLM 只负责当某个角色的嘴和手 → 天然接上第四点"真协同"。
- 每步交接单都写程序记忆，团队越用越会按教训分工（避免"审查永远抓不到问题"）。

### 1.4 与现有模块的衔接
- 技能页 → 升级成"员工/技能 双库"（角色卡 + 规程技能）；
- 漫剧分步等既有管线不破坏，可选"用团队模式"；
- UI：对话里说「让我的团队做 X」→ 先用 P2 的流程模板弹计划待确认。

## 方向二：自检 · 自修 · 自升级（让 PASM 与 PASM-qclaw 自己维护自己）

### 2.1 现状与差距
- 有：validator(规则)、planner、评审闭环(coop)、冒烟测试、统一网关、升级器(updater)。
- 无：把"真机报错/崩溃/日志"接回来的通道；没有自动生成补丁的能力；升级靠人按按钮。

### 2.2 分解为 4 个可独立增益的闭环
| 闭环 | 输入 → 处理 → 输出 | 落点 |
|---|---|---|
| C1 自检哨兵 | 崩溃栈(crash.log) + pasm.log 错误行 + voice.log 诊断 → 汇总成"症状+上下文" → 本地知识库"故障案例" | companion 启动后异步分析，记入 episodic；重要故障气泡提示 |
| C2 自动定位 | 故障案例 + 代码树 → LLM 定位根因（给文件+行号级分析）→ 落盘 "bug_report" | 复用 _brain；类似人工 bug 流程，但自动跑 |
| C3 自动修复 | bug_report + 仓库 → LLM 提补丁 diff → PASM 用"冒烟+回归测试"验证 → 不过就带失败日志再修(最多N轮) | validator/planner 全套复用；补丁先落 feature 分支 |
| C4 自升级发布 | 修复合入 + 版本号 + 自动打包/双端发布 + latest.json → 用户端"有更新"推送 | 现成的打包发布流程脚本化 |

### 2.3 关键工程约束（诚实说明难点）
1. **安全阀**：C3 不能自动 merge/发布——补丁必须过冒烟回归 + 用户确认开关
   （cfg `autofix: off/建议/自动-仅本地`）。自升级永远需要用户点确认，绝不静默换代码。
2. **谁修谁**：PASM 本体引擎（pasm/）由 PASM+LLM 一起修但**模型卡版本**；
   桌面壳(PASM-qclaw)是主战场（Python 单仓，可跑冒烟，风险低）。
3. **闭环验证**：每轮"自动修"必须产出可复现验证（冒烟新增/回归全过），
   否则视为没修好并回滚该补丁——用我们自己的 validator 给我们的修复打分。
4. **自更新**：updater 已有；新增"自升级包先本地演练（dry-run 冒烟）再提示用户"。

### 2.4 落地顺序（建议）
1. C1 哨兵 + 日志诊断 → 2. C2 自动 bug_report → 3. C3 自动修(先"建议补丁"模式)
→ 4. C3 自动验证闭环 → 5. C4 一键演练发布。每个闭环都能独立冒烟 + 独立验收。

---
*结论：两个方向都建议"先做成看得见的闭环小件"再横向扩展——方向一先让 3 人小队
(PM→开发→审查)跑通一个真实任务；方向二先把"崩溃自动定位"做出来。两者共享同一套
"计划→执行→验证→记忆"骨架，先打好骨架，再往上长角色与自愈能力。*
