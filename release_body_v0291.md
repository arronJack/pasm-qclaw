## v0.29.1 · 修「本地模型回复突然变慢 5 倍」+「莫名背能力清单」+「语音变哑/念一串英文」

本版是 **0.29.0 的修复版**，三个问题都来自真机反馈，且都定位到了根因。

---

### 一、修「聊天从 10 秒变 52 秒」—— 40 秒是**装载**，不是推理慢

**反馈**：同一句话，0.28.5 约 10 秒回复，0.29.0 变成 52 秒。

**定位**（日志里那一行就把答案写明了）：

```
local-native Qwen3.5-4B | 提示1467 tok | 首字48.92s | 总79.34s
                        | 生成315 tok@10.4tok/s | 装载40.2s | 思考关
```

- 提示只有 1467 tok、思考链关着、生成 10.4 tok/s —— **与基线一致，推理侧没有退化**；
- 48.92 秒的「首字」里 **40.2 秒是把模型重新装进显存**；
- 本版新增的认知层在每轮提示词拼装里只花 **约 21 毫秒**（实测），完全不背锅；
- 同样 1467 tok 提示实测**热态：首字 1.35s、总 8.7 秒** —— 正是你说的「10 秒」。

**根因**：`keep_alive` 是**逐请求**带的，而 **Ollama 服务端的 `OLLAMA_KEEP_ALIVE` 默认只有 5 分钟**。
于是只要 App 侧闲置超过 5 分钟、某条路径掉到兼容端点兜底（会静默丢弃 `keep_alive`）、
或者 Ollama 自身重启，模型就被卸载，下一句白等 40 秒。
（本次日志实证：Ollama 在 17:30:53 重启过，模型被清空。）

**修复**：

- **驻留守卫**：每 4 分钟体检一次，**只有**发现模型真不在显存时才在后台补载回来 ——
  于是「模型被换出 / Ollama 重启」这类意外不再落到你头上。
  ⚠️ 守卫刻意做成**按需**：模型好好驻留时**绝不打扰**。实测任何触碰都会重置 Ollama 的
  **前缀缓存**，把本来 0.3 秒的提示评估打回 4.2 秒（等于每句白扣几秒）。
- **请求前体检 + 显式预热**：模型不在时先把它装好再发正式请求，
  「装载 40 秒」从此不再算进正式请求的「首字」，也不会伪装成「模型卡死 / 响应超时」。
- **预热改成「只装载、不推理」**（`/api/generate` 不带 prompt，Ollama 返回 `done_reason: "load"`）：
  语义更准更快，且**只预热本机已装的模型**，绝不会触发联网下载。
- **启动兜底预热**：此前只在「没填 API Key」那一条分支里预热，所以**从云端切到本地、
  或在设置里换本地模型时预热根本不会跑**（日志里从头到尾没有 `warmup ok`）——
  现在不论走哪个分支，只要当前生效的是本地模型就先预热。
- 想关掉守卫：设环境变量 `PASM_LOCAL_KEEPALIVE=0`。

---

### 二、修「答非所问，突然背一串能力清单」

**反馈**：问「能否先吃点小吃？」，回的确是「小U能帮你：读文件、找文件、写文案、做 PPT…」。

**根因**：能力询问的判定**太宽** —— 「能否 / 能不能 / 可不可以 / 会不会」只要出现在句子里
**任意位置**就算「在问能力」，而末尾又有一句**无条件兜底**。两者叠加，
任何「能否…」「会不会…」「这样行不行」都会被判成问能力，再落进兜底直接背清单，**模型完全没参与**。

**修复**：能力询问必须**开在句首**（可带「请问 / 你 / 您」），且**能力领域词必须紧跟其后 12 字内**；
去掉无条件兜底 —— 没有领域词就交回模型正常回答。

**实测**（14 条负样本 + 8 条正样本）：误触发 **9/13 → 0/14**，正样本 **8/8** 全中。
「可以用微波炉热一下吗」「会不会下雨」「这样行不行」「能不能用 Excel 打开这个文件」等
同一家族的说法一并修好。

---

### 三、修「语音变哑 / 念出一串英文」

**根因**：微软会**静默下架**在线神经声线，而被下架的声线会**返回空音频而且不报错**。
本机实测已下架 **7 个**（女童声线、男童声线，以及四川 / 河南 / 山东三地方言声线），
而成长档表把**幼儿 / 童年两档**全指向女童声线 —— 正好命中，于是**每次在线合成都失败**，
只能回落到系统声线硬念中英混排，听着就是「一串英文」。

**修复**：

- 声线表改用**当前存活声线 + 音高塑形**做年龄感（不再依赖已下架声线）；
- 三个已下架的方言声线留空，回落到标准声线；
- 新增**声线守卫**：合成返回空音频即记入失效名单、下次自动换同性别替身，
  并每小时校验一次服务端声线表 —— **以后微软再下架也不会静默变哑**。

**实测**：12 个（性别 × 成长档）组合 + 全部方言声线**逐个真合成，0 处空音频**；
拿已下架声线喂守卫，3/3 被正确替换。

---

### 安装

1. 下载 `PASMStudio-Setup-0.29.1.exe`（单文件约 77.5 MB）
2. 双击安装（覆盖安装即可，本地数据与记忆保留）
3. 首次启动会自动把本地模型加载进显存（后台进行，不阻塞界面）

> 想安静一点：设置里关掉「自动朗读」；想更省显存：
> 设环境变量 `PASM_LOCAL_KEEPALIVE=0` 关闭模型驻留守卫。
