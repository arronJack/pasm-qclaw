# 安装包不入库（发行走 Releases 附件，Gitee 附件配额有限）
PASMStudio-Setup-*.exe
# ★ 分卷版（-gitee 的 .bin 切片）同样不入库 —— 体量和 exe 一样大，
#   一旦提交进历史就再也删不掉（铁律：安装包只挂 Release 附件）
PASMStudio-Setup-*.bin
*.bin

# 桌面端那份 latest.json 已废弃（**不是**构建产物，没有任何代码读写它；
# 升级通道只有本仓根目录这一份）。保留忽略只是防止它被误提交回来，
# 来龙去脉见 PASM/desktop/README.md 的「同步约定」
desktop/latest.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 虚拟环境
.venv/
venv/
.buildvenv/
env/

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db

# 本机一次性产物
_patch_backup/
_*.txt
*.log
