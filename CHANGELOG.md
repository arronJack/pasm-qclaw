# 版本历史

> **版本号说明**：本仓有两条版本轴，别混 ——
> · **引擎/内核** `pasm.__version__`（当前 `0.7.2`）+ `pasm.cognitive.__version__`（`0.8.0`），
>   与 `pyproject.toml` 的 `version` 必须一致；
> · **桌面产品** PASM Studio `APP_VERSION`（当前 `0.30.8`，见 `desktop/appinfo.py`），
>   驱动安装包与升级通道，其发行记录见 `pasm-qclaw/CHANGELOG.md`。

## 0.7.2（2026-09-15）· 新增桌面运行时 `desktop/runtime/`：让认知真正跑起来

> 认知层决定**做什么**，但一直缺一层决定**什么时候做、被打断怎么办、卡住怎么办**。

### 新增：`desktop/runtime/`

| 模块 | 解决什么 |
|---|---|
| `scheduler.py` · `RuntimeScheduler` | 持续主循环 + 优先级队列（`USER > REACTIVE > BACKGROUND > IDLE`）+ **新消息抢占** + **卡死看门狗** + **空闲自主 tick**（`on_idle`） |
| `injector.py` · `ContextInjector` | 在模型说话**之前**同步预取记忆 / 情绪 / 焦点，产出可直接拼进提示词的片段（ACI） |
| `__main__.py` | `python -m runtime` 自检 |

### 为什么这么设计

- **协作式抢占，不强杀线程**：Python 无法安全中断线程，强杀会留下半写状态。
  任务自己在 `ctx["_cancelled"]` 检查点退出。慢一点比状态损坏安全。
- **看门狗只记录不停掉**：同理 —— 留下证据（计入 `stuck` 并留痕）比粗暴恢复更重要。
- **纯标准库，不引入 asyncio**：桌面端已有 Qt 事件循环，再叠一层只会增加理解成本。

### 补上的三个长期问题

1. 纯被动 → 现在有持续循环 + 空闲自主行为入口
2. 无抢占 → 用户新消息可以打断后台任务（"叫得应"）
3. 说话时手上没材料 → 上下文预取注入

### 验证

- `cd desktop && python -m runtime` → **8 项通过，0 失败**
- 生态索引（六仓同频）写入 README

### 新增：`tools/verify_core_complete.py`（核心完整性守卫）

此前**没有任何工具能回答"核心还是完整的吗"**：核心依赖 torch，
没装 torch 的机器（CI / 打包机 / 别人的笔记本）连 import 都做不到，
改坏了也照样提交发版。本脚本用 AST 静态解析 + 零依赖部分实跑绕开这个盲区：

| 检查 | 内容 |
|---|---|
| 七层齐全性 | 层0 感知编码 → 层6 元认知 + 全局工作空间 + 规划器，每层类与关键方法都在 |
| 方法完整性 | 按**真实实现**核对（不是通用命名），残一个方法立刻报出 |
| 契约可用性 | `pasm.engine_api` 零依赖实跑：注册表 / 自描述 / `conforms` / `capability_gap` |
| 配件完整性 | 皮层 15 模块、桌面运行时、睡眠巩固 `training.consolidate`、环境契约 |
| 全仓语法 | 102 个 .py 全部编译通过 |

`python tools/verify_core_complete.py` → **42 项通过，0 失败**（`--json` 供 CI 机读）。

> 项数随环境浮动，**不是**"少了一项就是坏了"：缺 `numpy` 等可选依赖时该项判 `SKIP`
> 而不计入总数（本机无 numpy 时是 **41 项通过 / 1 项 SKIP**，装了 torch 的打包 venv 里是 **42 项**）。
> 全仓 `.py` 语法检查项数同理随仓内文件数增长（0.30.2 时为 **129 个**）。

### 修复

- **运行时版本号对齐**：`pasm.__version__` 停在 `0.7.1`，而 CHANGELOG / 桌面端已按
  `0.7.2` 发版 —— 依赖方按版本判断能力会误判。已对齐为 `0.7.2`。


## 0.30.9（2026-09-16）· 聊天页头部压扁 / 右侧栏改浏览器式产出标签 / 小人放大 + 走路修复 + 飞天（**未发版**）

> 本版按小志的界面反馈逐条重做。**未打包**，`latest.json` 仍指 0.30.8。

### 一、聊天页头部：压扁、去噪

- **头像 78 → 64**（64 × 1.40 = 89，装进 96px 的卡片；第一版收到 52 后小志说"还是小了"）；五行竖排 → **两行**：
  ① 名字 + 格言 + 今日心情 同一行 ② 内心一行。整块高度钉死在 76px，不再占小半屏。
- **内心不再用 `QTextBrowser`**（那必然带滚动条，小志："不要滚动条"）→ 换 **单行 QLabel**，
  放不下的省略、完整内容挂 tooltip。
- **删掉那条会动的波形线**（`VoiceWave`）—— 小志："上方的波浪线不要了"。
- 语音三键改为与头像**垂直居中**（原来顶到最上沿）。

> ⚠️ 试过"把气泡预留的那 0.28× 高度压掉"来省地方，**实测不成立**：
> 成年档本体就有 74px 高，压缩控件高度会让它在聊天页**变小**（74 → 59），
> 那是改坏了不是美化。已回退，改成调头像**宽度**（要更小就调这个数字）。

### 二、右侧栏：改成「浏览器式产出标签」（参考 WorkBuddy）

| 小志说的 | 现在的做法 |
|---|---|
| 「产物/自动化/工作」三个固定页没必要 | **删掉固定页**，改成**按产出开标签**：出方案多一个「📋 …」页，出图多一个「🖼 …」页，可关、可拖动 |
| 预览应包括文案、产物 | 每个产出页里**文案区 + 产物预览区并存**（同一页里既看方案也看图） |
| 自动化工作流放进自动化模块 | 建流 / 执行下一步 / 完成此步 / 自主续跑 / 后台状态 / 当前工作 —— 全部收进**固定的「🤖 自动化」页**（不可关闭） |
| 工作板块不需要了 | 删除该页 + 目录筛选 + 工作列表 |
| 复盘是人工作的不需要显示 | 删除复盘区 |

- 同一份内容重复展示**复用同一页**（不刷屏）；**闲聊不开页**（判据保守，附反例断言）。
- 「✕ 清空」一键关掉全部产出页；固定页关不掉（否则用户一关就没了）。
- 侧栏宽 272 → 300，收起仍压到 34px 只露展开键。

### 三、小人：走路手脚不再冻结 + 飞天（飞机形态 / 更高 / 尾气粒子）

**① 走路走一会儿手脚不动了 —— 找到根因并收口（不只是打补丁）**
- 根因：**散步链和姿态模式是两条独立的东西** —— `_hop()` 链负责移动窗口
  （`_steps` 减到 0 才停），而四肢摆不摆只看 `avatar.mode == "walk"`。
  于是**只要有人在散步中途改了模式**，就变成「窗口还在挪、手脚一动不动」。
  取证到两条真实路径：**阅读结束**（`set_mode("idle")`）与**空闲 >150s 入睡**（`set_mode("sleep")`）
  —— 0.30.6 修过的"心跳 speaking 覆盖 mode"只是其中一条。
- 修法：所有姿态切换收成**唯一入口** `_pet_mode()`：散步链还活着就拒绝改模式；
  用户显式指令（"去睡吧"、躲边缘硬中断）走 `force=True`。全文件已无绕过入口的裸调用（有断言钉住）。

**② 飞天：先变飞机 → 高空绕屏一圈（带尾气粒子）→ 落地前变回人形**
| 你说的 | 实现 |
|---|---|
| 飞得高一点 | 巡航高度取可用屏高的 **5%**（贴着上沿飞），实测比原位高 200px 以上 |
| 先变成飞机形态再飞 | 起飞前先 `set_fly(True)`，**形变 ~0.9s 走完才开始抬升** |
| 落地前变回人形再落地 | 下降段后半程 `set_fly(False)`，触地时已完全是人形 |
| 绕屏一圈带尾气、粒子 | 沿可用屏幕走 **上→右→下→左→回上** 一圈；尾气是粒子系统（上限 44 颗、寿命 1.05s），
按**飞行方向**从机尾喷、越飘越大越淡，另有会呼吸的机尾喷口火焰 |

- 飞机形态**整机按飞行方向旋转**（小人是正面朝用户画的，不旋转看不出"在飞"），
  有机体、后掠翼、水平/垂直尾翼、机头锥、座舱光、翼尖灯；形变过程中有一圈光环提示。
- 诚实边界：3D 通道的"抬升姿态"仍压在 0.30 —— 相机取景是按**静立包围盒**算的（避免画面漂移），
  抬太高会被裁；**可见的"飞得高"由窗口路径实现**（无论 2D/3D 都生效）。
- 飞天期间不散步、不入睡；被"别打扰"时会立刻落地回位。

### 四、验证脚本搬进仓库

散在 `%TEMP%` 里的 6 套行为验证脚本收进 **`tools/desktop_verify/`**
（`%TEMP%` 会被系统清、换台机器就没有），新增 `verify_ui_v0309.py`（44 项）与 README
（写明三条必守纪律：打桩 `os.startfile`、自装 excepthook、一律 `-u`）。

### 验证

| 项 | 结果 |
|---|---|
| `verify_pet_v0309`（新，小人三项：尺寸/走路不冻结/飞天全流程） | **31 项全过** |
| `verify_ui_v0309`（工作台重做 + 尺寸同步） | **44 项全过** |
| `verify_ui_v0306`（同步 7 条旧断言后） | 50 项全过 |
| P0 / 预算 / 超时 / 意图 / 资料库 五套 | 全过 |
| 结构守门 / 成长守门 / 记忆与世界观 30 项 | 0 警告 0 失败 / 13/13 / 30/30 |
| 全量编译 / 行尾 | 132 文件 0 失败 / 两口径一致（无污染） |

## 0.30.8（2026-09-16）· 资料库 Obsidian 化 / 数学脑在包里"复活" / 智能体优化（**已发布**）

> 版本号 `0.30.8`，安装包 `PASMStudio-Setup-0.30.8.exe`（**82.5 MB**，比 0.30.4 的 76.0 MB 大 6.5 MB
> —— 那是把 numpy 装回来的代价，换回一个真实可用的核心能力，见第二节）。

### 一、资料库：从"只有要点"变成真正的笔记库（参考 Obsidian）

用户原话：**「上网学习的资料并不全并不完善，只记录了相关要点，达不到资料库的作用，参考 Obsidian」**。

取证：`record()` 其实**早就**收全文（≤40000 字）与来源，但那条全文只躺在 `knowledge.json` 里，
给用户看的只有 bullets；而且上网抓回的原文**原样入库** —— 必应/搜狗结果页里导航、广告、
"相关搜索"、成段重复的样板占了大半，**"资料不全"的真凶是"噪声太多"**。

- **`strip_web_noise()`**：入库前清洗网页噪声（导航/广告/超短行/纯符号行/重复行）。
  阈值实测调到 120 字（原来卡 30 字，漏掉一大片成段重复的样板）；
  更长的重复行**保留**（正文很少整段逐字重复，诗歌/歌词会 —— 宁可少删不可多删）。
  **只删明显不是内容的行，不"总结"**：资料库的价值就在原文可得。
- **资料库 = 一堆标准 `.md` 笔记**（`<数据目录>/vault/`），**可直接用 Obsidian
  「打开文件夹作为库」**：frontmatter（title/tags/category/source/confidence/reads）
  + `> [!note]` 概要 callout + `## 要点` + `## 全文` + `## 相关`（`[[双链]]`）+ `## 来源`。
- **`index.md` 目录页（MOC）**：按分类列出全部 `[[笔记]]`，标注总条数与全文字数。
- 导出**幂等**（内容没变不重写）+ 原子替换（`.tmp` → `os.replace`，不留半截文件）。
- 资料库页新增两个按钮：**📂 打开资料库** / **↻ 重建笔记**；读书与自学完成后**立刻落笔记**。
- 顺带修：读书入库原来传的是**文件名**（资料库里显示「网络安全入门.md」），改用干净标题。
- 新增 `knowledge.selftest()`（本模块此前没有自检）：**29 项**，含"真内容不许被清洗掉"、
  "幂等"、"重复学习保留更全的一份原文"等反例。

### 二、全生态核查发现：**数学脑在安装包里是死的**（本轮最重要的一个真问题）

- `PASMStudio.spec` 的 `_PASM_HEAVY` 里有 `'torch', 'numpy'`，注释写"排除完整引擎及其
  torch 依赖链" —— **numpy 是跟着 torch 一起被排掉的**；
- 但 `pasm/cognitive/mathlab.py`（数学脑）**自己 import numpy**
  （`np.polyfit` / `np.linalg.lstsq` / `np.corrcoef`），而它**不属于**那条重链；
- 后果（在真实用户装的包里）：`MLAB = None` → ① `tableana` 意图的准入条件
  `MLAB is not None` **永远为假**，表格分析根本不路由；② 真走到只回一句
  "数学脑模块这次没加载成功——**重启软件再试试**" —— 重启一万次也没用（包里没有 numpy）。
- 代价对比：**numpy 装完 34 MB（包内 6.1 MB），torch 是 ~1 GB**。
  为省这点让一个核心能力在产品里彻底消失，是笔坏账。
- **修**：spec 只排 torch 重链（numpy 拿回来）；打包前确认打包 venv 里有 numpy；
  降级文案改成**如实说明**（缺 numpy / 这一版打包清单漏了 / 源码装 `pip install numpy`），
  并明确"不会拿模型猜的数字冒充计算结果"。
- **验收钉死**：`desktop/frozen_smoke.py` 新增第 ③ 段「数学脑（mathlab / numpy）验收」，
  以后每次打包都会打印 `数学脑判定：✓ 可用于产品`，再排掉就会立刻显形。

### 三、智能体优化：补上「产品层没人验」的已知缺口

原有 7 个验证智能体**全部只体检 `pasm.cognitive`**（核心认知层）；产品层本身
（`pasm_agents` 的 npc / companion / tutor）长期**没有任何人验** —— 这是本仓自己
记在案的已知缺口。新增 **`product-verifier`**（第 8 个）：查公开 API、隔离性、
真实行为与反例（详见 pasm-agents 的 CHANGELOG）。

**它上线当场抓到一个真 bug**：`ElderlyCompanion.chat()` 的 `_render_reply` 硬取
`hit['content']`，而 `persona['key_facts']` 的字段名是用户自定的 ——
用户写 `value` / `text`，就在**聊天路径**上抛 `KeyError: 'content'`
（陪伴产品里最差的一种失败：用户看到空白）。已改为容错取值 `_fact_text()`
（content/value/text/brief/desc/detail 依次找），取不到走正常兜底，**绝不抛异常**；
并加了回归断言。

### 四、修掉一个会弹系统对话框的坑：点「打开资料库」弹出「位置不可用」

- **现象**：点资料库页的 **📂 打开资料库**，若目标目录不在，Windows 会弹
  「`…\vault` 不可用。如果该位置位于这台电脑上，请确保设备或驱动器已连接……」
  —— 看着像软件坏了或硬盘掉了。
- **根因**（两层）：
  ① `knowledge.open_vault()` **不检查目录是否存在**就把路径交给 `os.startfile`；
  ② 它把异常**全吞了**，无论成没成都返回同一个值 → 界面**永远显示"已打开"**。
  用户遇到"没打开"时看不到任何原因，只会以为资料库丢了。
- **修法**：返回值拆成两个**独立**状态 —— `ok`（笔记导出成功）与 `opened`
  （文件夹真的被唤起来了）；目录没建出来就**不交给系统**（不弹对话框）。
  界面按三种情形如实说话：真打开 / 笔记好了但窗口没起来（给出路径 + 原因）/ 失败。
- **顺带**：`knowledge.selftest()` **29 → 35 项**（新增 6 条：真调用打开器、
  打开器抛错时 `opened=False` 且带原因、目录建不出来时不把死路径丢给系统）。
  这条修复是**自检 + 真 GUI 验证**一起逼出来的，不是事后补的说明。

### 五、用**真实数据**试跑才发现：「要点」里混着标题本身

修完前面几条后，我拿小志本机真实的 `knowledge.json`（**23 条**）在临时目录试跑了一遍导出
—— 这一步是**必须的**：合成数据永远看不出这种问题。

- **18/23 条的第一个「要点」就是标题本身**（`# Python 基础`）→
  ① callout 变成 `> [!note] # Python 基础`（等于没说）；
  ② 要点列表第一行是废话；③ `# xxx` 在列表项里还会被 Markdown 当标题渲染。
- 修法：**只在渲染时清洗**（绝不改写用户已存数据）——过滤掉「内容等于标题」的要点、
  脱掉要点里残留的前导符号与 `#` 记号、脱壳后去重、清洗空了但有全文时仍走"现场补速记索引"。
- 修完复跑真实数据：《Python 基础》的 callout 从 `> [!note] # Python 基础`
  变成 `> [!note] Python 是一种简单清晰的编程语言`。

**顺带确认的两件事**（都属于"先取证再下结论"）：
- 真实数据里 **0/23 条**会被噪声清洗整段清空 —— 目前没有实际损失，机制留在自检里钉住；
- `sentence_bullets` 只收「脱标点后 ≥8 字」的句子，所以极短正文不会生成 `## 要点`
  —— 这是设计（避免要点与全文重复），但**原文一个字都不许丢**已写成断言。

### 六、验证

| 项 | 结果 |
|---|---|
| `knowledge` 自检（新增） | **44 项 · 0 失败**（含「打开失败不许谎报」「要点清洗」共 9 条反例） |
| 资料库真 GUI 验证（真写文件） | 25 项全过（含"噪声清掉但真内容全留"、"幂等"、"双链"、"反例"） |
| `product-verifier`（新） | **ok 33 / warn 0 / fail 0 / skip 0** |
| 全生态套件（8 个智能体） | 无 FAIL（`core-verifier` 的 3 项 FAIL 是**本机两个解释器都没有 numpy/torch** 所致，换解释器同样复现 → 环境差异，非回归） |
| 桌面 17 模块自检 / 三个守门 | 全绿 |
| UI / P0 / 预算 / 超时 / 意图 / 资料库 六套行为验证 | 全过 |
| 全量编译 / 行尾 | 125 文件 0 失败 / 两口径一致（无污染） |
| 打包产物 | PYZ 16/16 关键模块在包内；`_internal/numpy` 6.1 MB（含 `_multiarray_umath.cp313-win_amd64.pyd`） |

## 0.30.7（2026-09-16）· 弱机首字超时修复 / 对电脑的控制增强（**未发版**）

> 本版改动**已随 v0.30.8 一起发布**（Gitee + GitHub 双端 Release，`latest.json` 已指向 0.30.8）。
> 触发这次修复的是**朋友真机反馈**。

### 一、真机事故：问「你好」显示"没有成功回应你"

朋友机（Win，无独立显卡）日志取证：

- `提示1730 tok | 首字110.24s | 总119.25s | 生成44 tok@4.9tok/s` —— 一句问候编译出
  **1730 token** 提示词，而这台机器**预填充只有 15.7 tok/s**，"读提示"本身就要 110 秒；
- 换 Qwen3:1.7B（更慢）直接撞穿聊天任务 300 秒预算 → `APITimeoutError` →
  界面兜底文案"（我这边出错了，没有成功回应你）"；
- 对照：本机带独立显卡实测 **580 tok/s**，同一段提示只要 3 秒 ——
  **提示词预算一直是按"有独显的机器"定的，慢 37 倍的机器上必然崩。**

### 二、修法：把"本机速度"变成一等公民

- `llm_gateway.SPEED`：每次原生调用后按 `prompt_eval_duration` 记实测预填充/生成速度
  （拿不到就退回「首字 − 装载」），EMA 平滑；**没测过按保守值 60 tok/s 估**。
- `budget()`：系统提示与历史上下文的字符预算**按实测速度算**。
  朋友机 → 1000 字；本机 ≤8000 字（**旧行为不变**，已做等价性验证）；未知 → 保守。
- 「规则0 你的真实能力」长表（~700 字）在慢机上换**短表（~150 字，语义不变）**；
  用户真在问能力时（复用 `ir.capability_kind`）仍给长表。
- `_join_budget()`：提示块按优先级装入，超预算**整块丢弃**（绝不腰斩半句），
  并记日志说明丢了哪几块；身份/中文/诚实/执行纪律四条永不丢。
- `adapt_timeout()`：慢机按预测抬升超时（硬上限 600s）；**快机完全不动**。
- 首字 ≥5s 时状态栏直接写「本地模型正在读你的话（预计首字约 Ns，实测 X tok/s）」——
  不再是 110 秒静默。
- 超时文案改成实话：**是超时** + **本机实测速度** + **三条出路**（换小模型 / 关占用 / 用云端）。

实测：朋友机提示词 **2084 → 962 字**，首字 110s → **41s**；
本机 2084 字**逐字节不变**（等价性验证）。

### 三、顺带修掉的真问题

- **错误归因日志**：每次启动都打"PASM 完整引擎不可用（桌面包不含 torch）"——
  真相是 `pasm/pasmlink.py` **全仓不存在**、`DualBrainRouter` 也没人定义，
  导入**必然失败**（装不装 torch 都一样）。死代码已删，能力早已并入系统提示「规则2」。
- **日志假数**：生成速率算不出来时打 `1000000.0tok/s`（`ed≈0` 除出来的），改打 `—`。
- **`is_timeout` 会误吞一切异常**：openai 缺失时 `APITimeoutError` 被别名成 `Exception`，
  用 `isinstance` 判定会把所有错误都当超时 —— 改成按类型名判定。

### 四、对电脑的控制：补齐三个最常问的缺口

- **整机现状** `sysops.sysinfo()`：CPU 占用与核数、内存、各盘剩余、开机时长
  （只读，CIM 逐字段容错，取不到就如实说）。
- **进程占用** `sysops.top_processes()`：按内存降序列出（只读）。
- **结束进程** `sysops.kill_process()`：三重护栏 ——
  ① 名字白名单字符（防命令注入）；② `PROTECTED_PROCS` 硬拒
  （lsass / svchost / explorer 等，结束会掉桌面 / 断网 / 蓝屏）；
  ③ 必须**真的在跑**。流程是**先只读预检 → 不合格不弹窗 → 合格才弹授权 → 才动手**。
- 意图已接入：「我电脑卡不卡 / 本机 CPU 用了多少 / 电脑配置」→ 整机现状；
  「看看进程 / 哪个程序占内存」→ 进程列表；「关掉 chrome」→ 结束进程（弹确认）。
- 给模型新增**只读工具 `sysinfo`**：原来它能调的工具只有 recall/verify/learn/mood，
  正则没命中时"我电脑卡不卡"只能靠记忆答（=编造）。

### 五、验证

| 项 | 结果 |
|---|---|
| `_build_system` 等价性（同窗口同 snap，新旧模块对比） | 快机 **2084 字逐字节相同** |
| 慢机瘦身 / 底线保留 / 问能力仍给长表 | 962 字 / 四条底线全在 / 反例通过 |
| 超时链路回放（朋友机真实数字） | 通过（含 6 条反例） |
| 新意图 34 项（正例 + 反例） | 全部通过（闲聊 9 条反例零误伤） |
| `sysops` 自检 | **34 项通过 · 0 失败** |
| 桌面 18 模块自检 / 两个守门 / 30 项审计 | 全绿 |
| 全量编译 / 行尾 | 125 文件 0 失败 / 无污染 |

## 0.30.6（2026-09-16）· 界面专业化 / 工作链路准确性 / 白龙马对标收口（**未发版**）

> 本版改动**已随 v0.30.8 一起发布**。以下是 0.30.4 发布后累积的改动。

### 一、聊天页：小人与内心合成「科技卡片」

- **去掉头顶气泡**：`pet_avatar.set_bubble(bool)`（默认 True=原行为），聊天页关掉；
  桌面浮窗保持开启（那是它唯一能"说出"自己在干什么的地方）。
- **小人与「小U」顶部对齐**（`Qt.AlignTop`）。
- **内心从最右侧栏搬进卡片** —— 原来看的时候视线要横跨整个窗口。
- **科技感**：新增 `ui_tech.py`（渐变描边卡片 + **会动的波形线**）：
  说话时振幅放大、节奏加快（同步真实 TTS 状态）；情绪越强波形越明显；
  **不可见即停表**，不在后台空转重绘。

### 二、输入区重排（参考 DeepSeek / WorkBuddy）

`[＋ 附件] [🎤 语音] …… Enter 提示 …… [⏹ 停止] [发送]` 一行工具条，与输入框同底成一体；
发送键改主色实心、停止键红色描边。

### 三、右侧工作侧栏：分标签 + 可收起

- 四页：🔎 预览 / 🖼 产物 / 🤖 自动化 / 🛠 工作（**自动化单独归页**）。
- 收起后压到 34px 只露展开键，位置不跑（VSCode 习惯）。
- **出结果自动展示**：含表格/代码/多级标题/步骤清单/长文 → 自动切预览页并展开；
  检测到真实产物文件 → 切产物页（**广告出图后右侧直接看到效果**）。
  判据保守：闲聊/寒暄/短答一律不打扰。
- 「工作」页新增**当前工作**卡片，与列表 ★ 条目对应。
- 关于内嵌浏览器：本机 PySide6 不含 QtWebEngine（+150MB Addons），不值当 ——
  产物页用图片预览 + 「用系统程序打开」替代，网页内容由已有 playwright 截图后展示。

### 四、聊天气泡：整块 markdown 卡片 + 复制

- 助手回复渲染为**一整块卡片**（底色/圆角/边界）+ 下方「📋 复制」；
  复制走锚点 `pasm://copy/<token>` 写剪贴板（QTextBrowser 放不了真按钮）。
- **补上 Markdown 表格渲染**：`md_to_html` 原本**完全没有表格支持**，
  模型输出的表格会退化成一堆带竖线的段落 —— 这是"看着不专业"的直接来源。

### 五、工作链路的准确性（P0，含取证）

- **广告文案来源可追溯**：`copywrite()` 原本支持 `llm_fn` 但**从没人传** →
  产出其实是写死的模板句。现在接真模型（结构化三行：标题/卖点/号召），
  返回值带 `source`（`llm`/`template`），方案文件与界面**都如实标注来源**。
- **产出质检**：新增 `executors.qa_check()` —— 文件必须真实存在且非空、
  文本走验证器；**没过质检不标完成**，与"没执行器不标完成"同一条底线。

### 六、白龙马对标收口（阶段三）

- **媒体生成矩阵**：`media_job.ordered_engines()` 按当前配置给降级链排序
  （可用引擎排前），失败原因带"试过哪些引擎"，执行器报"用了哪个引擎"。
  修掉"只配本地引擎也先试云端"的浪费与误导。
- **多渠道路由**：`msg_source` 新增 `WebhookSource`（真出站 POST JSON）、
  `channels()`、`send()`；并修掉自动选路写死 `bridge→local`
  （**配了 webhook 也永远轮不到**）的 bug。
- 诚实边界：未配地址不假装发出；指定通道失败不静默回退。

### 七、顺手修掉的两个真 bug

- `_refresh_voice_btns()` 改的是 `self.stop_btn`，而它被输入区按钮覆盖 →
  朗读时**输入区**按钮莫名变红、头部朗读停止键永不变色（改名 `tts_stop_btn`）。
- `tts` 补公开 `is_paused()`（UI 不再摸私有的 `_PAUSE`）。

### 验证

| 项 | 结果 |
|---|---|
| 真 GUI 行为验证（offscreen 真窗口） | 50 项全过（含"输入框还在""气泡已关""复制真进剪贴板""闲聊不弹侧栏"等反例） |
| P0 行为验证 | 27 项全过（含 9 条反例） |
| 桌面 11 模块自检 | 全绿（msg_source 18→22 项） |
| 结构守门 / 成长守门 | 0 警告 0 失败 / 13 passed |
| 全量编译 / 行尾 | 125 文件 0 失败 / 无污染 |

## 0.30.4（2026-09-16）· 成长补全到成年 / 走路修复 / 设置面板分页 / 工作面板 + 广告设计 / 自主工作引擎 / 核心引擎覆盖率补齐

> 纯桌面端改动起步，后续阶段二/三深入核心（详见下文六、七节）。`APP_VERSION` = `0.30.4`。本轮对应小志的五项要求：①小人 ②全生态同频 ③工作与能力 ④流程深化 ⑤白龙马对标。

### 一、小人：从幼儿到成年（5 档）＋ 真的会走路

- **成长档 4→5**：`desktop/growth.py` 的 `STAGES/THRESH` 新增「成年期」（exp 400 起）。
  旧用户 exp < 400 的阶段完全不变（向后兼容，不是重算）。
- **身高按实测重定，不再靠估**：`pet_avatar.py` 的 `G` 表新增 `up`/`down`
  （离屏真实渲染量出的"中心以上 / 以下"尺寸），`scale` 由「目标实高 ÷ (up+down)」反推；
  并加安全钳制，保证任意窗口尺寸都装得下。两处生产尺寸实测实高**单调递增**：
  桌面浮窗 `81 / 106 / 129 / 139 / 148 px`，主窗头像 `60 / 67 / 81 / 88 / 93 px`。
- **走路"像飘"的根因**：心跳每 1.5s 调 `set_state(speaking=True)`，把 `mode` 从 `walk`
  覆盖成 `talk` —— 四肢摆动随之冻结，只剩窗口平移，看着就是飘。
  改为**只在不忙（idle/talk）时才切 talk**；并加大手臂摆幅、腿摆幅与上下起伏
  （`abs(sin)` 单向 → `sin` 正负起伏）。
- **3D 侧补齐**：`pet3d/rig.py` 补第 4 档 `PROFILES`（成年），`profile_for` 钳制由 3 放宽到 4。

### 二、声音：成年声线补全（原来会 KeyError）

- `tts.py` 的 `_EDGE_VOICE` 缺 `("female", 4)` / `("none", 4)` 两组 —— 成年档一朗读
  就 `KeyError`。现 3 性别 × 5 档 = **15 组齐全**，并给成年加了更沉稳的语速/音高。
- 设置页「试听声线」补「成年」项，默认档位钳制由 `min(3)` 放宽到 `min(4)`。

### 三、设置面板：分页 + 分栏（原来是滚到底的一根长条）

- 改为 4 页：**🎭 基本 / 🔊 声音 / 🎨 创作引擎 / 🧠 模型与思考**；
  「基本」页内再分左右两栏（左：名字/开机自启/权限/语音唤醒；右：性格/性别/语音引擎）。
- 保存/取消固定在底部，任何一页都能直接保存。实测单页最高 367 px、整框 sizeHint 453 px
  （旧版一屏放不下）。

### 四、工作：进度面板 + 层级 + 广告设计版

- `worklog.py` 的工作对象新增 `dir`（工作目录）/ `next_step` / `level` / `parent_id`，
  并新增 `by_dir()` 按目录复盘、`children_of()` 层级、`progress_of()` 整体进度、
  `step_complete()` 回写父任务。
- 新模块 `workflow_engine.py`：一个目标 → 步骤拆解（复用 `planner`）→ 父/子任务树 →
  进度 / 下一步 / 推进。
- 新模块 `ad_design.py` + 新工种 **📣 广告设计**：一句话产出「标题 / 卖点 / 行动号召 + 主视觉提示词」，
  并把提示词交给**已接入**的图像引擎真出图（不新增第二种引擎，用户不用再配一次）。
- 右侧心情面板下方新增**工作区**：目录筛选 + 工作列表（按层级缩进）+ 进度/产物/下一步 +
  `▶ 启动工作流` / `✅ 完成此步 → 下一步`。进度数字来自真实完成数，不假装执行。

### 五、白龙马对标

- 新增 `docs/bailongma_benchmark_v0.30.4.md`：能力缺口对照 + 优先级路线图。
  本轮已落地其最高价值的两点：**task-manager 式续跑**（`workflow_engine`）、
  **分层任务 + 进度**（`worklog` 层级）。能力注册表范式列为下一批。

### 六、深化（阶段二）：把「自动工作」从骨架做成真能跑

对标白龙马最核心的三件事，本轮全部落地（**纯桌面端，`pasm/` 核心零改动**）。

**1）声明式能力注册表 `desktop/capability.py`（对标 `api-capability.js`）**

把「什么时候该用哪个能力」从散落的 `if/elif` 变成**一条条声明**
（`triggers / detect / tool_when / prefeed / risk / order`）。
新增一个能力只要 `register(...)`，**调度代码一行都不用改**。

> 踩到并修掉的三个判据 bug（都会让路由悄悄跑偏）：
> · **触发词抢跑**：触发词在 `detect` **之前**判定，所以"排除项"形同虚设 ——
>   `画个漫画分镜` 被 `image` 的触发词「画个」先抢走。改：模糊动词全部下沉到 `detect`。
> · **大小写没对齐**：排除表写小写 `ppt`，`detect` 里却拿未小写的 head 去比 →
>   `生成一张 PPT` 的排除**静默失效**。改：`detect` 内部一律 `.lower()`。
> · **12 字句首窗口截断**：`做一份 2026 年度报表` 的「报表」正好被从中间截掉 →
>   掉进闲聊。改：**窗口不放宽**（放宽会让「我昨天在路上看到一张宣传海报」误命中），
>   另用「制作动词在前 8 字、交付物名词紧随其后 14 字内」的相邻判据。
>
> 铁律沿用「**无法判定 = 放行**」：没有命中任何能力 → 回通用的 `chat`，
> **绝不给某个具体能力写"拿不准也用我"**。（历史事故：`_cap_question` 正是因此
> 让模型整轮缺席、直接背出能力清单。）

**2）统一上下文装配 `desktop/context_assembly.py`（对标 Context Assembly 阶段）**

把「命中的能力 + 手头工作 + 相关知识 + 用户记忆」按优先级合成**一段**系统提示，
而不是各模块各塞一段（塞重、塞爆、互相矛盾）。
接在**全仓唯一的系统提示装配点** `_build_system()` 里；
主链路只取 `caps + work` —— 记忆已由 `memrouter` 注入、知识也在那里注过，
**重复塞两遍只会白占上下文**。有硬预算（1100 字），超了**按整节丢弃，绝不腰斩半句**。

**3）自主 Tick 心跳 `desktop/autopilot.py`（对标 `main.cjs` 常驻主循环）**

面板原有的 5 秒定时器升级为「先自主巡检、再刷面板」：
崩溃恢复（中断的工作标 `interrupted`）→ 巡检可续跑的工作流 → 更新「下一步」提示 →
数据体检。重活 20 秒节流，不做无谓的 5 秒扫盘。

> **安全边界（本模块最重要的部分）**：
> · **绝不假装执行** —— 心跳**永不**把任何一步标成 `done`。
>   进度数字只能来自真实完成。（曾想过"自动把下一步标成进行中"来显得热闹，
>   那就是撒谎：并没有真的在执行，用户会看到一个永远绿着的假进度。）
> · **绝不删用户数据** —— 体检只统计、只报告。
> · **默认关闭** —— 没明确开启时只做只读巡检；用户没答应就自动改他的工作状态，
>   比不自动更糟。开启后写的也只是「下一步」提示文本（幂等、可回退）。

**4）真执行器 `workflow_engine.EXECUTORS`（对标 task-manager 续跑）**

「▶ 执行下一步（真跑）」按钮 → 取下一步 → 查执行器 → 真跑 → 回写父进度。
内置执行器 `ad`：把一步广告设计**真做成文件**（文案 + 主视觉提示词的 `.md`，
给了 `image_fn` 再真出图）；没给 image_fn 就**不谎称已出图**，方案里写明「待生成」。

> **诚实边界**：工种没有执行器时只标记「进行中」并如实说明，
> **绝不把这一步标成完成** —— 否则进度条会绿着，而其实什么都没做。

**5）工作流工种由注册表推断**（收口：注册表与工作流原来没接上）

`plan_workflow` 原来给每个子步骤一律写 `kind="general"`，
于是建出来的步骤**永远找不到执行器**，`run_next` 只能一直报 `no_executor`
（"注册表"白建了）。改为调 `capability.best()` 推断工种 ——
新增工种只改 `capability.py` 一处。

### 七、深化（阶段三）：核心引擎覆盖率补齐 + 媒体/浏览器/知识执行器

> 本轮把「自动工作」从能跑推到**够用**：核心引擎从「8 个模块零覆盖」补到探针 15/15，
> 并修掉 3 个真 bug。纯桌面端 + 核心行为修复，无契约变更。

#### 7.1 核心引擎覆盖率：8 个「零覆盖」→ 探针 15/15

套件探针（`pasm-skills/checks.py` 的 `CORE_COGNITIVE_MODULES`）实际探测 15 个认知模块，
其中 **8 个没有 `selftest()` = 零覆盖**（按项目铁律，等于测不出任何回归）。
本轮全部补齐（另补 `coder`）：

| 模块 | 新增断言 | 覆盖要点 |
|---|---|---|
| `cog` | 16 | 认知状态机推进、情绪衰减 |
| `memrouter` | 17 | 路由分类边界（含反例） |
| `workctx` | 12 | 工作上下文开档 / 记录 / 摘要 |
| `selfheal` | 13 | 扫描 / 诊断 / 修复建议（**只读不删**） |
| `agent_team` | 23 | 协作流程校验（含非法步骤） |
| `mathlab` | 25 | 线性拟合 / 解方程 / 图论（numpy 可选，缺则跳过） |
| `percept` | 8 | 图像 base64 校验 + ASR 后端探测 |
| `quantum` | 16 | ★ 含修复后的**收敛性**反例 |
| `coder` | 29 | 脚手架 / 沙箱路径 / ★零文件不许报成功 |

**探针实测：15/15 全部可自检且全通过。**

#### 7.2 修掉 3 个真 bug（都是取证查出来的，不是猜的）

**① `quantum` 的退火方向反了 —— 与模块自述完全相反**
模块文档承诺「成功 → 增强，随经验收敛到最优路线」。决定性取证：让一条路线连续成功 8 次，
其被选中概率从 `0.333` **掉到** `0.183`，而**从未试过**的路线升到 `0.409`。
`BETA = 0.9` → `1.1`（退火项由「惩罚成功」改为「增强成功」）。

**② `coder.gen_project` 假成功 —— 一个文件都没写却返回成功**
`_scaffold()` 漏了 js 分支，而 `all(空) == True`，于是「模型不可用 + js 项目」
→ `{"ok": True, "files": []}`，界面会显示成功。两层修：补 js 脚手架 + 加
「**没有写出任何文件就不许报成功**」的底线。

**③ `knowledge` 召回不到刚自学的内容 —— 影响聊天主链路**
`_query_grams` 长词占满配额，把 2 字核心名词饿死。取证：自学《校园营销海报设计》后，
问「给霖云智学做份开学季招生海报」→ 改前**召回为空**，改后命中。
另修 `_kb_score`：命中「标题 / 标签 / 要点」比命中正文更可信（2 字词 1 分 → 3 分）。
A/B 复测：**6/6 应命中全中、7/7 反例零误召回**。
附带修数据目录硬编码 `%APPDATA%`（不认 `PASM_STUDIO_DIR`、与 `logsetup` 不同源）
→ 统一到 `_resolve_data_dir()`，显式覆盖优先、旧库位置兜底**不丢数据**。

#### 7.3 新增四个模块（均零 Qt 依赖 + `--selftest` + 可降级）

| 模块 | 行数 | 自检 | 作用 |
|---|---|---|---|
| `media_job.py` | 488 | 20 | 统一媒体任务抽象（图 / 视频 / 音频 / 漫剧），状态机 + 降级链 |
| `kb_bridge.py` | 258 | 20 | 知识自学 → **回流工作流**（`learn()` / `hits()`），闭环 |
| `executors.py` | 264 | 13 | 把媒体 / 浏览器 / 知识 / 文档注册成工作流执行器 |
| `msg_source.py` | 387 | 18 | 消息源适配器（本地回环，为多端预留） |

`worklog` / `scheduler` 的 id 生成改为**同毫秒不撞车**（批量创建任务曾互相覆盖），
`worklog` 补 29 项自检。

#### 7.4 自检护栏自身也修了一处

`pasm-agents` 的 `regression` 验机智能体，其「Lite 可用引擎」那一行**永远是空的**：
注释断言「引擎在 `import pasm_lite` 时注册」，实际注册发生在 Lite 的 `engine.py` 末尾，
而探针从没 import 过它 → **这一行无论 Lite 是否可用都只会报「档位不可比」**。
已补 `import engine` 走真实注册路径，并刷新基线（`lite.engines` 首次如实显示 `["pasm-lite"]`）。
> 判据沉淀：**一个检查若能永远给出同一个结果，它就不是检查。**

#### 7.5 清理与规范化

- 全部改动文件行尾归一为 LF（与仓库已跟踪文件一致），并剥离补丁脚本双写产生的游离 `CR`。
  复核：`git diff --numstat` 与 `--ignore-cr-at-eol` **两组完全一致 → 无行尾污染**。
- 补丁脚本一律改用**二进制读写**（`newline=''` 或 bytes 模式），
  从根上杜绝行尾在写回时被悄悄翻转。
- 六个仓的构建产物 / 缓存 / 旧版安装包已移入回收区 `E:/AI/_cleanup_20260916`（可整体删除）。

### 验证（全部真机跑过）

| 项 | 结果 |
|---|---|
| `tools/check_pet_growth.py` | 13 passed / 0 failed |
| `tools/check_core_fork.py` | 0 警告 / 0 失败（新模块已登记 `ALLOWED_UNIQUE`） |
| 真实 PySide6(offscreen) GUI 冒烟 | 设置面板 4 页、18 个控件零孤儿；走路不被说话覆盖；两处生产尺寸 5 档实高单调 —— **全绿** |
| `workflow_engine.py` / `ad_design.py` / `worklog` 自检 | SELFTEST_OK |

> **仍未完成、需继续的**：⑤「功能全参考并补全」仍是对标文档 + 已落地项 ——
> 媒体生成矩阵（多模型路由）、多社交渠道路由仍未移植（浏览器自动化 / 知识回流
> 已在阶段三接入执行器，具备基础能力）。
> **v0.30.4 安装包已发布**（2026-09-16）：Gitee + GitHub 双端 Release，
> `latest.json` 升级源已指向 0.30.4，旧版用户可自动更新。
>
> 注：本轮 ① 小人「贴边」曾被我误判为"被切"并加了边距补丁，
> 后经**反事实对照**（禁用钳制再渲染，画面行差 = 0）证明**根本没有裁切**，
> 该补丁已回滚 —— 详见 `pet_avatar.py` 里那段回滚注释。


## 0.30.3（2026-09-16）· 小人形象与行为修复：成长体型 / 头部放大 / 灵活性 / 自检修正

> 纯桌面端改动，无核心/引擎行为变化；未发版，等小志拍板。

### 修复（用户自检发现）
1. **成长体型反了**：`pet_avatar.py` 的 `G` 外观表 `scale` 原随成长递减（越长大越小），改为递增（幼儿 0.98 → 青年 1.16），符合"长高长大"。
2. **头部太小、细节看不清**：2D `G` 表头部尺寸整体放大；3D `pet3d/rig.py` 的 `PROFILES.head` 由 `0.158→0.095` 改为 `0.172→0.126`（青年期放大约 33%），躯干四肢仍随成长长高。
3. **灵活 / 自主动作少**：`pasm_pet.py` 的 `_maybe_hobby()` 间隔缩短（高能量 22–46s→10–24s、低能量 34–80s→16–40s）、触发概率提高（0.82→0.9 / 0.5→0.62）；`_sleep_tick()` 睡眠阈值 45s→150s。
4. **行为自检 stale 断言**：`pet_behavior.py` 的 `_selftest()` 写死"满级 7 动作 / 0 级 3 动作"，改为从 `ALL_ACTS` 动态推导（现 12 个动作、0 档解锁 5 个），动作集再扩也不会写死数字 FAIL。
5. **数据目录分裂修复**：`growth.py` 的 `DATA_DIR` 由 `os.environ.get("APPDATA") or ~` 改为复用 `logsetup.data_dir()`（Win32 权威解析），与日志落盘口径一致，消除"双击启动写 AppData、其他启动写 ~"的数据分裂。

### 验证
- `python pet_behavior.py` 自检 RESULT: PASS（满级 12 动作、0 级 5 基础动作）；
- 整个 `desktop/` 目录 `compileall` 通过；pet 文件仅在 `desktop/` 与 `desktop/pet3d/`，`pasm/cognitive/` 无同名孪生，不触发分叉铁律。


## 0.30.2 · 修「聊天内容缺失」「编造已执行」「日志不写」三项缺陷

> 本版含 0.30.1 未发布批次（动作系统扩充 / 走路转身 / 九性格自主 / 形象同步）
> 与三项缺陷修复，对外只发这一个版本。

### 一、你看到的那个问题：聊天里的话会丢

**症状**：自己发的消息有时不见了，换到别的对话再换回来又能看见。

**根因**：回复是"打字机式"逐字上屏的（约 1.2 秒播完），而播完之前输入框已经解锁。
你在这 1.2 秒里发新消息，那条新消息就取代了"文档最后一段"的位置，
随后打字机的下一帧把**它**当成自己的段落删掉、改写成了 AI 的半截回复。
消息其实已经存进历史，所以切走再切回又出现了 —— 这就是"有时丢、有时又不丢"的来源。

**修法**：
- 重画段落前先核对"这一段到底是不是我自己的"，不是就另起一段，**绝不删别人的内容**；
- 你抢话时，把上一轮没播完的话**立刻补完整**再停下（否则屏幕会永远停在半句上）；
- 同时取消上一轮遗留的打字机，避免两条回复互相覆盖。

### 二、那句「已生成并打开浏览器」并没有发生

**症状**：它说"已生成并打开浏览器"，但屏幕上什么都没出现。

**核查结论：确实没有执行，是编造。** 依据有三条：那段话是纯文本回复，
措辞与程序里任何真实执行结果的措辞都不同；普通聊天栏目能给它的工具只有
记忆类（回忆/自检/记住/情绪），**根本没有写文件、运行代码、打开浏览器的能力**；
电脑上也确实没有任何新生成的网页文件。

**修法**（两层）：
- **补执行台账**：凡是真正执行的动作用装饰器统一记账（不会漏记），
  生成文档、运行代码、打开文件与网页都留痕；
- **扩防幻觉守卫**：回复宣称"已生成 / 已打开 / 已运行 / 已保存"，
  但台账里查不到真实操作记录 → 自动更正为"我并没有真的做到"。
  > 措辞经过刻意设计：**只认"动作词 + 交付物词同句出现"**，
  > 且"打开过/生成过"（讲过去经历）一律放行 —— 宁可漏拦，绝不误伤正确回复。

### 三、日志整整一个版本没有写下来

**症状**：排查时发现日志文件是空的，最后一条停在上一版。

**根因**：Python 的日志配置有个隐蔽语义 —— 一旦启动路径上**任何一处**先调用了日志，
后续那句"写到文件"的配置就会**静默失效**（不报错、不警告）。
本版之前新增的启动迁移日志恰好先跑了一步，于是日志从此不再落盘。

**修法**：日志初始化抽成独立模块，入口**最先**执行，并强制覆盖已有配置；
数据目录也改为单一来源，避免日志与数据写到不同地方。

### 四、本版还包含（0.30.1 批次）

- **动作从 7 个增到 16 个**：新增伸懒腰 / 抱臂 / 弹跳撒欢 / 点头致意 / 飞天转圈
- **走路先转身**：不再正面朝前横向平移；转身未完成时会收小步幅，
  读起来是"先转过身再迈步"，走完自动转回正面
- **动作由性格自主决定**：九种性格各自的偏好互不相同
  （实测各抽 400 次：调皮灵动偏颠球跳舞，温和沉稳偏点头思考……）
- **无聊时会飞天转圈**：平时权重压到 0.12 倍、超过 90 秒没人理时放大 6 倍
- **换形象会同步**：桌宠换了皮肤，聊天区的小人立刻跟着变
- 修「＋」附件上传报"打开文件选择框失败"

---

## 0.30.0（未发布）· 形象 · 栏目 · 工作能力：3D 机甲小人 + 栏目整合 + 六项新能力

### 一、这一版解决的是什么

三件事：**形象**从 2D 贴图升级为 GPU 真 3D 机甲；**栏目**从 9 个平铺按钮收成 6 个入口；
**工作链路**补齐附件上传、权限分级、转场动效、语音唤醒与开机自启。

---

### 二、3D 立体小人（`desktop/pet3d/`，新包）

- **GPU 真 3D，零新增依赖**：基于 PySide6 自带的 `QtOpenGL` 封装类，
  不引 PyOpenGL / numpy / QtQuick3D（后者会把包体推高约 150MB）
- **程序化几何**：形体由参数算出，因此能随成长档位连续变形，**不依赖外部模型资产**
- **机甲造型**：弧形装甲片 + 倒角甲块 + 鳍脊 + 尖锥；四色装甲体系
  （主甲 / 副甲 / 金饰条 / 黑曜关节 + 发光件）；清漆层二次高光
- **完整人形**：头（面甲 · 发光眼缝 · 颊板 · 冠脊）· 躯干（胸板 · 反应堆 · 腹甲 · 腰环）
  · 肩 / 上臂 / 肘 / 前臂 / 手 · 髋 / 大腿 / 膝 / 小腿 / 脚，共 60+ 部件
- **7 种动作**：静止 / 走路 / 挥手 / 跳跃 / 跳舞 / 思考 / 抱球
- **透明背景**：离屏 RGBA 渲染 + QPainter 合成，桌面宠物悬浮窗与头顶工作气泡都不受影响
- **连续动画**：呼吸、身体摇摆、天线摆动、左右张望全部由真实时间驱动
  （实现要点：3D 必须走 `advance(dt)` 推进内部时钟；若误用"定格"接口，
  小人会变成一尊**静止的雕塑** —— 外观是立体的、动作却是死的）
- **失败即降级**：任何环节不可用（无 GPU / 驱动异常 / 离屏平台）都自动回退原 2D 绘制，绝不崩

### 三、成长系统：修掉一个「永远长不大」的老 bug

- 外观表写了 **5 档**而成长档位只有 **4 档**，后果是**每一档的名称与长相错位一级**
  （界面显示"青年期"、小人还是"少年"样），且最修长的形态**永远取不到**
- 同一处问题还有第二个：青年期肩甲的条件写的是"≥4 档"，**也永远不出现**
- 补上**常态双臂**（此前只有挥手动作才画一只手）
- 头身比随成长从 0.39 抽条到 0.21，幼儿大头圆润 → 青年修长带肩甲与天线

---

### 四、栏目整合：文档类 4 个按钮收成 1 个入口

- 「✍️ 文案 / 📊 表格 / 📽 PPT / 📄 Word」合并为 **「📄 文档与演示」**，
  点它展开子标签选具体格式 → 工种按钮从 **9 个降到 6 个**
- 依据：**格式不是意图，是交付选项**。用户要的是"一份能给客户看的东西"，
  不该被迫先做一次格式决策
- **点击后弹出展示框**（不是占一行子标签）：卡片式列出 4 种格式并各带一句说明，
  平时不占布局高度、点面板外部自动关闭、屏幕边缘点击会自动回拉避免弹到屏外
- **老状态零破坏**：内部工种标识仍是原来的 copy/xls/ppt/doc，
  所以老配置的工作模式、老会话、`/mode` 命令**全部照旧**；
  显式说"给我个 Word"依然直接走，不弹选择器

### 五、新增能力

| 能力 | 说明 |
|---|---|
| **聊天框「＋」附件上传** | 输入框旁新增「＋」：图片 / 文档 / 任意文件 / 截屏。Word·PPT·Excel **真读正文与表格**（不止文件类型），PDF 可选支持；单文件截断 6000 字、单次最多 3 个；坏文件只提示不崩 |
| **权限三档** | 安全（默认，写操作与危险操作都要确认）/ 标准（低风险放行、高危确认）/ 完全访问（全放行，切换有明确风险提示 + 二次确认）。**失败一律往"不动手"方向退** |
| **页面转场动效** | 8 个页面切换带入场动效（淡入 + 轻微位移），连切不错乱、动画期间不阻塞流式输出 |
| **语音唤醒** | 直接说「小U」即可唤醒。唤醒词**跟随你在设置里改的角色名**，改名立即生效、无需重启；常驻监听默认关（占麦克风是隐私敏感行为）；助手说话时不自唤醒 |
| **开机自动启动** | 设置里一键开关，写当前用户注册表（**不需要管理员权限**），源码运行用 pythonw 启动所以开机不弹黑框 |

### 六、其它

- **默认角色名统一为「小U」**（含桌宠、设置、知识库、TTS 各处兜底值）
- 新增一次性数据迁移：只把"恰好等于旧默认名"的配置改过来，**用户自定义过的名字一律不动**
- 新增成长契约守门（12 项）与分叉守卫登记，防止同类"档位错位"再次发生

---

## 0.29.2（未发布）· P3：验证器全链路 —— 结论出口第一次有"代入检验"

### 一、这一版解决的是什么

P1 让三个模块说同一种语言，P2 让"预演"变成真计划。但**说出去的话**仍然没人检查：
`symbolic.verify` 早就会做代入检验，却**只覆盖数学题**。P3 把它推广到**所有对外结论**，
并补上闭环——验不过就回头重绑，再不行就诚实说"不确定"。

### 二、本版落地（P3）

- **新增 `pasm/cognitive/verifier.py`**：四族检查，**全部复用已有能力，不新造真相**

  | 族 | 查什么 | 复用谁 |
  |---|---|---|
  | `numeric` | 数值结论与确定性解矛盾 | `symbolic.verify`（**已有的，不重写**） |
  | `fact_conflict` | 说出的主体-值与**当前有效事实**矛盾 | `facts.active_facts()` |
  | `attribution` | 把**本人**的值安在**亲属**头上（P1 归属规则的输出侧对称检查） | `ir` + 事实层 |
  | `precondition` | 动作序列前提违例 | `planning.replay_violations()` |

- **闭环 A**：验证失败 → 按反例类型重建（换权威值 / **回 P1 重绑主体** / **用 P2 重新规划**）
  → 最多 2 次 → 仍失败则只说"拿不准"。**绝不把没验过的结论说出去，也绝不空转**
  （修不动就只验一次）。
- **唯一一条设计铁律：只有拿到反例才拦，拿不到反例就放行。**
  `CheckResult` 里**没有反例就不允许判不通过**（构造时自动降级为放行）。
  这条是从真机事故换来的：v0.28.1 涨跌链答"跌了 4%"**完全正确**，却被硬加一句"更正"——
  **误拦比漏拦糟得多**。

### 三、实测验收（`tools/audit_verifier.py`，可复核）

| 验收项 | 目标 | 实测 |
|---|---|---|
| 陷阱题拦截率（20 题，四族） | ≥ 80% | **100%**（20/20）✅ |
| **误拦率**（31 条必须放行的结论） | ≤ 5% | **0%**（0/31）✅ |
| 平均延迟增量 | ≤ 20ms | **0.97 ms**（p95 2.04ms）✅ |
| 闭环 A | 全项 | **5/5** ✅ |

误拦集里专门放了**历史真机事故**（涨跌链的等价表达"跌了 4%"）与 10 条普通闲聊。

### 四、顺带修掉的两个真 bug（都是 P3 逼出来的）

1. **`symbolic.py` 只认阿拉伯数字** —— `对折3次` 能解、**`对折三次` 连门都进不去**，
   而用户日常就写"三次/十天/三十个"，符号层对这类提问**完全不工作**。
   修法：在**输入层**做单点归一化（`normalize_numbers`），不动那 20 处正则。
   ⚠️ 这段归一化**踩了两次坑**（注释里都留着）：无脑替换会把「十分好吃」变「10分好吃」；
   只看后一个字仍不够 —— 「零」把 `零件` 拆成「0件」**一句话整废**，
   「倍」把 `翻一倍` 改成「翻1倍」让**倍增求解器失灵**。
   最终字符集**不含零/〇**、量词表**不含倍**，并给 `symbolic.selftest()`
   加了 **3 条正例 + 5 条反例**（普通句子必须一字不改）。
2. **`facts._RULES` 里根本没有「姓名」「电话」规则** —— 这两类事实**只能靠手工写入**，
   用户说"我叫李大明"根本记不住；输出侧验证器也因此完全看不到主体（实测漏拦 3/20）。
   修法：补 姓名 / 电话 / 亲属联系方式 三类规则 + 用药的"吃的是X"。
   精度保护：「我叫X」挡掉「我叫了个外卖 / 我叫你一声」；**不放开裸"吃"**
   （否则「我吃饭」会被抽成 用药="饭"）。

### 五、已知边界

- **覆盖率不是 100%，也不该算成缺陷**。符号层覆盖不到的问法属设计文档 §六.2 的**天花板**；
  把它算进拦截率等于拿"没打算覆盖的题"去拉分。审计**只报拦截率，另记边界**。
- 归属检查收窄到**本人强标识**属性（姓名/电话/居住地/过敏源/用药/工作单位/生日）才判，
  收窄是为了少误拦。
- ⚠️ **使用前置条件**：`fact_conflict` 依赖"用户本轮所说已被 ingest 进事实层"。
  顺序必须是 **ingest(用户输入) → 生成 → verify(候选结论)**。

---

## 0.29.2 前序批次 · P2：算子库 + 规划器 —— PASM 第一次有真正的"多步预演"

### 一、这一版解决的是什么

P1 把三个模块接到了同一份 IR 上，但"预演"这件事本身还是假的：世界模型只存
「(处境, 动作) → 成功率」，所以它能做的只有把若干动作的成功率**相乘**（`rollout`）。
那是**条件独立上界** —— 它不知道"先装依赖才能跑代码"这类前提，也算不出"若 A 失败改走 B"。

**P2 补的就是这一环：把动作从"一个字符串"升级成"带前提与效果的算子"，然后在状态空间里搜索。**

### 二、本版落地（P2）

- **新增 `pasm/cognitive/operators.py`（算子库，STRIPS 式最小子集）**
  - `Operator = {name, preconds[], add[], delete[]}`；模板只能取自 `ir.OPERATOR_TEMPLATES`（**≤5 种**）；
  - **`execute:x` 的依赖前提是结构性的**：它自动要求 `exists:dep/x`。
    于是"未装依赖就运行"在**声明层**就不可能通过，而不是靠调用方记得补一句；
  - 前提/效果只允许声明式命题（`exists/known/ran/online` 四种），**文件里没有一行业务逻辑**。
- **新增 `pasm/cognitive/planning.py`（A\* 前向搜索，限深 4）**
  - 代价 = `1 − worldmodel` 的 Beta 后验；启发式写成**可采纳**形式（步数下界 × 最便宜一跳的代价），
    所以最优性仍然成立；
  - **不可达就诚实返回空计划**，绝不返回"看起来像计划"的东西；
  - `plan / greedy_plan / replay_violations / plan_is_valid / describe`；
  - 另附 `greedy_plan`（单步贪婪）作为 A/B 的**同底座**对照物。
- **把 `worldmodel` 的 Beta 后验接到算子失败率**：新增 `observe_op / predict_op`，
  **复用同一套表与同一套三层回退**（固定处境键），不新建存储、不复制统计逻辑
  —— 同一份能力只准一处实现。

### 三、实测验收（`tools/audit_planner.py`，可复核）

| 验收项 | 目标 | 实测 |
|---|---|---|
| 前提违例次数 | 0 | **0** ✅ |
| 规划器成功率（随机任务 n=40） | 高 | **40/40** ✅ |
| 贪心基线（同底座：同算子/同代价/同限深） | 参照 | 18/40 |
| 平均代价（**含失败惩罚**，全部任务） | 优于贪心 | 规划 **0.682** vs 贪心 **3.044** ✅ |
| 同交集代价（双方都成功的 18 个任务） | 不劣于贪心 | 规划 **0.540** vs 贪心 **0.652** ✅ |
| 规划耗时 | ≤ 50ms | p95 **4.2ms**（仅链路算子 **1.8ms**）✅ |
| 不可达时诚实返回空计划 | 100% | **4/4** ✅ |

**三条诚实说明**：

1. **"平均代价"第一版我算错了。** 代价只在成功的计划上有定义，只对成功项取平均时
   **失败越多反而显得越便宜**（贪心 0.652 "优于" 规划 0.682）—— 这是幸存者偏差，不是规划器更差。
   已改为对全部任务算、失败记惩罚，并额外报同交集对照。
2. **贪婪失败的原因是具体的**：它每步只看"这一步成功率最高"，会先把 4 步预算烧在
   **高成功率但对目标毫无推进**的诱饵上。差距来自"看不看目标"这一件事，不是靠调参。
3. **模块名刻意避开 `desktop/planner.py`**（v0.22.0 的「任务拆解器」，把复杂指令拆成步骤）。
   两者互补但同名会造成跨层歧义，分叉守门会（正确地）把它当成"同名却非薄壳"报警
   → 核心侧取名 `planning.py`。**同名的代价远大于改名的代价。**

### 四、已知边界（有意识的取舍，不是遗漏）

- **限深 4 是硬的** → PASM 不会做长程规划；5 步目标会被诚实放弃（审计里 A6 专门验这个）。
- 代价函数是**加性**的（Σ(1−p)），优化的是"期望失败步数"；`Plan.confidence` 仍是各步连乘，
  **那一项依旧只是上界**。P2 消灭的是"盲目相乘"，不是"相乘这个公式本身"。
- 前提/效果是**人声明的** → 能严谨规划的范围 = 声明过的算子范围。

---

## 0.29.2 前序批次 · P1：IR 中枢 —— 让三个"语言不通"的模块第一次能互相推理

### 一、为什么做这个（不是"再加一个模块"）
PASM 已有的东西不少（符号求解器 0.1ms / 8-8 全对、七层记忆、双时态事实层、Beta 后验世界模型），
但三个模块**各自用自己的形状描述同一件事**，彼此没法互相推理 —— 所以"预演"只能查历史成功率、
永远变不成真规划。三条实证：

1. **同一个概念在三处三种拼法。** `facts.py` 的规范主体是 `居住地`，而
   `memory_layers._INTENT_SYN` 的键写的是 `住址`，它又拿这个键去匹配事实文本（hay）——
   `住址` 在 hay 里**根本不存在**，于是那条"意图同义词"**自始至终是死代码，且无人察觉**。
2. **手写词表是 bug 的主要来源。** 0.29.1 修的三个真机问题里，**两个**出自手写规则
   （单字别名"住"、判定词"能否"）。
3. **符号层是唯一能保证正确的一环，却只被用来解数学题** —— 因为上游喂给它的不是结构化问题。

→ **最优解不是"更多能力"，而是一层 IR 中枢**：把神经侧 / 符号侧 / 记忆侧接到同一份结构化数据上，
**已有资产全部复用，只加一层粘合剂**。设计见 `docs/roadmap_reasoning_ir.md`。

### 二、本版落地（P1）
- **新增 `pasm/cognitive/ir.py`（IR 契约，唯一真相源）**：`Goal / Plan / Fact / Operator / Evidence / Verdict`；
  主体注册表（relation + 专指别名 + 泛指 + 口语问法框架）、能力领域声明、算子模板。
  **三条防膨胀硬约束**：relation 白名单 ≤6 种、算子模板 ≤5 种、`Plan` 限深 4 步
  （深搜组合爆炸，**宁可不规划也不乱规划**）。
- **新增 `pasm/cognitive/irextract.py`（两段式抽取）**：
  - **快路**（声明式词表 + 正则）：**0.05 ms**，零依赖、离线可用；
  - **长尾路**（本机 Ollama `format`=JSON schema 强约束）：覆盖任意口语，**默认关闭**；
  - **白名单收口**：模型返回的一切都要过 `ir.canon()`，映射不上的丢弃 —— **宁可漏，不可错**；
  - **优雅降级**：两段都失败就返回空，调用方照常自由对话，不报错不空转。
- **三处手写词表收敛为消费 IR**（不再各写一份）：
  `facts.py` 的别名表、`memory_layers.py` 的意图同义词、`desktop/pasm_companion.py` 的
  `_cap_question`（判定逻辑 91 行 → 15 行委托给 `ir.capability_kind()`）。
- **`_ALIAS_VER` 取全局版本号**：词表一改，已落盘的旧条目按键**就地重算**（不再埋雷）。

### 三、实测（`tools/audit_ir_extract.py`，可复核）
语料刻意分两组，**避免自我实现的测试**：`registered` 贴近注册问法，`heldout` 是**刻意避开的生僻说法**。

| 指标 | 修复前 | 现在 |
|---|---|---|
| 槽位抽取 F1（registered，n=30） | — | **1.000** |
| 槽位抽取 F1（heldout，n=20，快路） | — | **0.250**（诚实值：快路吃不下生僻说法，长尾路才该上） |
| 跨表述召回（口语问 → 规范名事实） | **0.500** | **1.000**（8/8） |
| 能力判定误触发 / 漏判 | 9/13 ／ — | **0 ／ 0**（14 负 + 8 正） |
| 快路单次耗时 | — | **0.051 ms** |

### 四、审计过程中揪出的 4 处真缺陷（都是实测发现的）
1. **`CAPABILITY_REQ_RE` 声明了却忘了接线** → "可以做个表格吗"被误判成问能力（要内容 ≠ 问能力）。
2. **亲属 `frames()` 含裸亲属词** → "提了一句妈妈"被判成"在问妈妈"而拿满 0.9 置信度，
   **反而堵死了长尾升级**。
3. **收口按字段位置** → 实测模型会把主体塞进 `obj`（返回 `{"subjects":[],"obj":"居住地"}`），
   信息对、字段错 → 改为**从所有字符串字段捞候选再统一过 `canon()`**。
4. **归属规则缺失** → 「我儿子的名字」会同时命中用户**自己的** `姓名` 事实（中文里这指的是儿子的名字）
   → 新增规则：亲属词出现在人绑定属性（姓名/电话）之前时，该属性归亲属所有。修后 registered F1 0.967 → **1.000**。

### 五、诚实的边界（写进代码注释与设计文档）
- **长尾路在这台机器上放不进交互路径**：实测一次抽取即便只输出 36 个 token 也要 **5.29 秒**
  （生成速度被硬件锁在 ~7 tok/s）。所以**默认关闭**，需要时设 `PASM_IR_LLM=1`。
- **真正的天花板仍是符号覆盖率**：算子与前提还得人声明（P2）。
  换更大的模型只会让"听懂 / 说人话"更顺，**永远不会替你长出"验证"这一环**。

### 六、下一期（P2）
算子库（前提/效果）+ A\* 规划器，并把 `worldmodel` 的 Beta 后验从"动作成功率"改接到
"**算子失败率**"上 —— 这一步同时消灭 `rollout` 的条件独立上界问题，
是 PASM 第一次拥有"多步可靠传递"。


## 0.29.1（2026-09-14）· 修「本地模型回复突然变慢 5 倍」——加载 40 秒不是推理慢

> 反馈原话：同一句话，0.28.5 约 10 秒回复，0.29.0 变成 **52 秒**。

### 定位过程（每一项都是实测，不是推测）
拧开日志里的那一行诊断，答案就写在脸上：

```
local-native Qwen3.5-4B | 提示1467 tok | 首字48.92s | 总79.34s
                        | 生成315 tok@10.4tok/s | 装载40.2s | 思考关
```

- **提示只有 1467 tok**（没膨胀）、**思考链关着**、**生成 10.4 tok/s**（与 0.28.5 基线一致）
  → 推理侧**没有退化**；
- 48.92 秒的"首字"里，**40.2 秒是 Ollama 把模型重新装进显存**；
- 0.29.0 新增的认知层（事实层 / 记忆路由 / 世界模型 / 记忆重放）在每轮提示词拼装里
  只花 **约 21 毫秒**（实测 fact_ingest 2.5ms + memrouter 16.9ms + WM.advise 1.3ms）→ 完全不背锅。
- 我按同样的提示长度（1467 tok）实测热态：**首字 1.35s、总 8.7s** —— 正好就是你说的"10 秒"。

### 根因
`keep_alive` 是**逐请求**带的（走原生接口带 8 小时），而 **Ollama 服务端的
`OLLAMA_KEEP_ALIVE` 默认只有 5 分钟**（本机实测 env 确认）。于是只要出现下面任一情况，
模型就被卸载，下一句白等 40 秒：

1. App 侧超过 5 分钟没有本地请求（你在打字、切窗口、想事情）；
2. 某条路径掉到 OpenAI 兼容端点兜底 —— 它会**静默丢弃** `keep_alive`；
3. Ollama 自身重启（本次日志：17:30:53 重启，17:31:59 首句即 40.2s 装载）。

### 修复（双保险）
- **驻留守卫（新增）**：每 4 分钟体检一次 `/api/ps`，**只有**发现模型真不在显存时
  才在后台把它补载回来 —— 于是"模型被人换出 / Ollama 重启"这类意外不再落到你头上。
  守卫刻意做成**按需**的：模型好好驻留时绝不打扰，因为实测任何触碰都会重置
  Ollama 的前缀缓存，把本来 0.3 秒的提示评估打回 4.2 秒（等于每句白扣几秒）。
- **请求前体检 + 显式预热**：模型不在时先把它装好再发正式请求，"装载 40 秒"
  从此不再被算进正式请求的"首字"，也不会伪装成"模型卡死 / 响应超时"。
- **预热改成「只装载、不推理」**（`/api/generate` 不带 prompt，Ollama 返回
  `done_reason: "load"`）：语义更准、更快，且只预热**本机已装**的模型，绝不会触发联网下载。
- **启动兜底预热**：旧版只在「没填 Key」那一支预热，于是**从云端切到本地、或在设置里
  换本地模型**时预热根本不会跑（日志里从头到尾没有 `warmup ok`）→ 现在不论走哪个分支，
  只要当前生效的是本地模型就先预热。
- 想关掉守卫：设环境变量 `PASM_LOCAL_KEEPALIVE=0`。

### 实测（本机 RX580 4G + Qwen3.5-4B）
| 场景 | 修复前 | 修复后 |
|---|---|---|
| 模型被换出后的第一句 | 40.2s 装载 + 9s 推理 | 守卫已补载 → 直接热态 |
| 模型在、守卫空跑 4 轮 | — | prompt_eval 0.28s → **0.32s**（前缀缓存未丢） |
| 只装载（不推理） | — | 10.0~10.6s，且 `装载0.0s` |

### 另外两项（同一轮真机反馈）

**① 修「答非所问，突然背一串能力清单」**（这个被反馈过多次）
- 现象：问「能否先吃点小吃？」，回的确是「小U能帮你：读文件、找文件、写文案、做 PPT…」。
- 根因：能力询问的判定**太宽** —— 「能否/能不能/可不可以/会不会」只要出现在句子里任意位置
  就算"在问能力"，而末尾又有一句**无条件兜底** `return "all"`。两者叠加，任何
  「能否…」「会不会…」「这样行不行」都会被判成问能力，再落进兜底直接背清单，**模型完全没参与**。
- 修复：能力询问必须**开在句首**（可带"请问/你/您"），且**能力领域词必须紧跟其后 12 字内**；
  去掉无条件兜底 —— 没有领域词就交回模型正常回答。
- 实测（14 条负样本 + 8 条正样本）：误触发 **9 → 0**，正样本 **8/8** 全中。

**② 修「播报里一串英文 / 声音突然变了」**
- 现象：回答里中英混排时，念出来是支离破碎的英文。
- 根因：微软**静默下架**了神经声线，本机实测已下架 **7 个**：
  `zh-CN-XiaoshuangNeural`（女童）、`zh-CN-XiaoyouNeural`（男童），
  以及四川/河南/山东三地方言声线。而成长档表把**幼儿 / 童年两档**全指向已下架的女童声线
  —— 真机正好在童年档 → **每次在线合成都返回空音频**（voice.log 里连刷 `NoAudioReceived`），
  只能回落到系统声线硬念。而系统声线念中英混排本就别扭，于是听着就是"一串英文"。
- 修复：① 声线表改用**当前存活声线 + 音高塑形**做童声感（不再依赖已下架声线）；
  ② 三个已下架的方言声线留空（按「北京」既有做法回落标准声线）；
  ③ 新增**声线守卫**：合成返回空音频即记入失效名单、下次自动换同性别替身，
  并定期对服务端声线表做一次校验 —— 以后微软再下架也不会**静默变哑**。
- 实测：12 个（性别 × 成长档）组合 + 全部方言声线**逐个真合成，0 处空音频**；
  拿已下架声线喂守卫，3/3 被正确替换。


## 0.29.0（2026-09-14）· 记忆层两处实现合一 + 新增「事实层 / 世界模型」+ 找回跨表述记忆

这一版把 0.28.6 / 0.28.7 / 0.28.8 三批「待装测发布」的能力**一并发布**，
并补上认知核心的两层新能力。**最要紧的一条是修掉一个正在悄悄丢记忆的缺陷。**

### 一、记忆层：把「两处各存一份」合并成「只有一处」（修丢数据缺陷）
- 桌面端原先各留了一份记忆实现，而且是**拷贝**、不是同一份 → 同一件事在两个副本里各记各的，
  表现为「聊天时有时记得、有时不记得」（半失忆），**且不报任何错**。
- 现已收敛：核心包为唯一真相源，桌面侧只留薄壳再导出。
- 加了两道**自动守门**防复发：结构侧校验「薄壳身份 + 打包模块声明」，行为侧 12 项运行时断言。
- 实测：容量 200 的记忆库被 260 条无关闲聊灌满，**三条早期重要记忆一条没丢**。

### 二、事实层：会「更新、且记得旧值」的确定事实
- 专门记住「姓名 / 年龄 / 住址 / 用药 / 过敏 / 家人电话」这类**关键事实**，
  与闲聊记忆分开存，**保证不被日常对话挤掉**。
- **改口会留痕**：从「北京」改说「上海」，旧事实不是删掉而是标记失效（历史可查），
  检索只返回当前有效的那条。
- **可信度保护**：把握不大的新说法**不会**盖掉把握很大的旧事实，而是先「挂起待确认」。
- **口语问法能命中**：问「我住哪儿」「吃什么药」「对啥过敏」都能查到对应事实。
- 修掉 5 处实际缺陷：单字别名误判、跨关系串味（问儿子却带出老伴）、同句多事实取错、
  低把握覆盖高把握、重复写入留下假历史。

### 三、世界模型：办事前先「预演一下」
- 同类处境做过几次、成过几次 → 给出**成功率估计**与**瓶颈提示**（如「成功率约 83%」）。
- **知道就说、不知道就说不知道**：只做过 1 次不会给出满把握；完全没有经验时如实返回不确定。
- 「带伞」这类动作会参考它在**别的处境**下的经验给出**弱提示**，而不是一律 50%。
- 新识别「健康 / 家人 / 生活」三类日常处境词 —— 此前「买菜 / 去医院 / 给女儿打电话」
  会被归成未分类，领域经验等于白存。

### 四、记忆检索：换种说法也找得到
- 以前问「我叫什么名字」查不到存成「姓名」的事实（两句话**一个字都不重合**）。
- 现已补上「说法 → 事实」的对应通道，实测该场景召回率 **50% → 100%**。
- 如实说明：纯口语长句（如「您老家是哪儿的呀」）仍受限于本地词袋检索，需接入语义模型才能完全解决。

### 五、打包与自检
- `PASMStudio.spec` 补全新认知层的模块声明 —— 这类模块是动态加载的，
  漏声明会在**安装版**里表现为「功能没反应」（源码运行却正常）。
- 新增 `tools/audit_memory_worldmodel.py`（30 项对抗式断言）与 `tools/check_core_fork.py`（结构守门）。
- 冻结核验：情绪不发散 / 行为不塌缩 / 存档往返一字不差；桌面运行时冒烟 13 项全过。

### 六、同时包含 0.28.6 / 0.28.7 / 0.28.8 全部能力
- **0.28.6**：由「长期验证智能体」压出的三个核心缺陷（记忆同分排序崩溃 / 容量淘汰不分重要度 / 反馈无法指定动作）。
- **0.28.7**：中文语言锁（复杂问题不再莫名答英文）+ 干活前确认门 + 浏览器自动化 + 实时天气 + 语音稳定性 + PPT 插图。
- **0.28.8**：定时 / 周期任务、邮件连接器、日历连接器、跨端远程桥、自进化沉淀技能、多智能体协作层。


## 0.28.8（能力补齐 · 待装测发布）· 定时 / 邮件 / 日历 / 跨端 / 自进化 / 协作层

对标 WorkBuddy、OpenClaw、Hermes 的能力条，把小志点名的四类缺口一次补齐；
**全部纯标准库实现，零新增依赖，安装包体积不变**（仅上一版 Playwright 带来的增量）。

### 一、定时 / 周期任务（`desktop/scheduler.py`）
- 到点自己动起来：提醒、跑技能、推手机。支持**一次性 / 每天 / 每周 / 每 N 分钟**四种计划。
- 中文时间解析：`每天早上8点半`、`每小时`、`每30分钟`、`每周一9点`、`明天下午3点`、
  `9月20日10点`、`今晚8点` 都认；「每天提醒我一下」里的「一」**不会**被误当成 1 点。
- 纯后台守护线程 + 30 秒休眠轮询（非忙等）：不跑任务时 CPU 占用为 0，关窗自动结束。
- 关机期间错过的任务只补跑最近 10 分钟内的，更早的顺延 —— 不会一开机刷屏。

### 二、邮件连接器（`desktop/connector_mail.py`）
- 真的读、真的发：IMAP 收信 + SMTP 发信（`imaplib`/`smtplib`/`email`）。
- 服务器地址按邮箱域名**自动识别**（QQ/163/Gmail/Outlook/阿里企业邮…），用户只填地址+授权码。
- 中文编码处理完备（`=?utf-8?B?…?=` 头、GBK 正文、HTML 转纯文本）。
- 未配置时给中文指引；认证失败专门提示「是不是把登录密码当授权码填了」。

### 三、日历连接器（`desktop/connector_calendar.py`）
- 标准 **iCalendar(.ics)** 读写（手写，不引依赖）→ 可直接导入手机 / Google 日历，也能导入别人的。
- 支持 RRULE 重复日程（DAILY/WEEKLY/MONTHLY + INTERVAL/COUNT/UNTIL/BYDAY）。
- `会议纪要 / 会议记录` 这类**文档**不会被误判成建日程。

### 四、跨端远程桥（`desktop/remote_bridge.py`）
- **推出去**：Telegram Bot / 企业微信 / 钉钉 / 飞书 / Server酱 / Bark —— URL 自动识别平台。
- **收回来**：轮询 Telegram 新消息 → 投进正常聊天管线处理 → 回复自动推回手机。
- **本机接收端**：`POST /msg`，**默认关闭、只绑 127.0.0.1、无 token 拒绝启动**，
  绝不把用户电脑变成公网开放中继。

### 五、自进化（`desktop/self_evolve.py`）
- 把干过的一次活**沉淀成标准 SKILL.md** 进技能库，下次同类活一句话复用。
- 扫工作台账，发现某类活**成功 ≥2 次**就主动建议沉淀。
- 有模型时润色正文，没模型时用结构化模板；**两条路径都绝不编造用户没做过的步骤**。

### 六、多智能体协作层（`pasm/cognitive/agent_team.py` P4）
- **任务板**：`board_*` —— 共享待办，谁在做、做到哪、依赖谁一目了然。
- **自动组队**：`auto_design_flow` —— 给个目标就从现有角色里自动挑人排流程（带依赖），
  不再要求用户先学会「建流程」才能用团队；模型不可用时用现成角色兜底，绝不空手而归。
- **依赖分层**：`parallel_levels` —— 语义与引擎 `validate_flow_steps` 严格对齐
  （全无 deps = 线性链，不是并行）。
- **关于并行的重要判断**：本机 Ollama 单并发，真并行只会互相排队 —— 故 `parallel` 默认 False，
  按层顺序执行；换云端多并发模型后再开才有收益。

### 七、聊天接入
- 新增 15 类意图路由（提醒新增/查询/取消、邮件看/搜/发、日程增/查/删、跨端推送、自进化、任务板、自动组队），
  并**补齐反向陷阱**：普通聊天（"提醒你一下这个方案有问题"、"会议纪要"、"我今天心情不太好"）
  不会被误劫持。
- 会改动本地状态或对外发东西的动作（发邮件 / 建删日程 / 建删提醒 / 沉淀技能 / 自动组队）
  统一走**干活前确认门**。

### 附加
- 新增 5 张技能卡（connector-scheduler / connector-mail / connector-calendar / connector-remote /
  skill-self-evolve），新能力可在技能库里被发现与检索。
- `PASMStudio.spec` 注册 5 个新模块的 hiddenimports（懒加载，不声明会在 frozen 版"功能没反应"）。
- 实测：PyInstaller 构建通过（2m08s，onedir 259MB），7 个新模块确认已进包。


## 0.28.7（桌面体验修复 · 待装测发布）· 聊天 / 干活 / 浏览器 / 天气 / 语音 / 文档

反馈："问题主要是我通过 pasm-qclaw 安装桌面测试得来的。" 四条问题逐条修复。

### 一、聊天：中文语言锁 + 干活前确认
- **复杂问题莫名答英文**：`_build_system` 把「始终简体中文回复」升为**最高优先级硬规则**
  （术语/代码/API 名可保留英文原文，解释与推理一律中文）→ 夹 `encoder/decoder/引擎/编译器`
  等词也不再甩英文。
- **干活前先问、纯讨论不动手**：重活（做 PPT / 写码 / 建项目…）在聊天模式下先反问
  「开始 / 不用 / 聊聊」；回「聊聊」则本轮只聊；**干活栏目内仍直接开工**不打扰。

### 二、能力扩展：浏览器自动化 + 实时天气
- 新增 `desktop/browser_agent.py`：Playwright 驱动浏览器——打开网页 / 搜索 /
  **点击 / 填表 / 滚动 / 截图 / 取文**；**优先复用系统自带 Edge**（免下载 150MB Chromium），
  未装依赖时给中文安装指引、不崩溃；敏感字段（密码）明确提示、不保存。
- 新增 `desktop/live_data.py`：open-meteo 免 key 查天气，**直接给答案、不打开浏览器**；
  修掉旧解析只认「X天气」、漏「X下雨 / X冷不冷 / X空气质量」的城市提取缺陷。
- 新增技能卡 `skills/connector-browser.md` / `skills/connector-weather.md`（技能库可发现）。
- 打包：`PASMStudio.spec` 补 `browser_agent` / `live_data` 隐藏导入（它们是懒加载，静态分析易漏）；
  Playwright 按需打包（构建机装了就打进、没装也能正常出包），驱动 `node.exe` 排除 UPX。
  **安装包 50.8MB → 77.3MB（+26.5MB）**，为浏览器能力的代价；想要小包可移除
  `desktop/requirements.txt` 里的 `playwright` 一行即可（功能届时优雅降级为提示安装）。

### 三、语音：朗读音色不再串味
- `accent.voice_for_text`：方言音色覆盖**全部过 `dominant()` 把关**，台湾腔分支同样加闸。
  普通话主导用户恒定普通话音 —— 回复里偶现「了啦 / 蛤 / 嘛」等口头禅不再忽普忽台。

### 四、文档产出：PPT / Word 按节配图 + 排版升级
- `agent_tools.make_pptx`：改 16:9 + 封面 + 每节内容页，可插配图，统一配色与页脚，要点上限放宽。
- `agent_tools.make_docx`：新增按节插图。
- `pasm_companion._section_images`：按节调 `creators.generate_image` 出图（前 6 节）；
  **未配置图像引擎时优雅退化为纯文字**（排版仍比旧版好）。

### 五、团队 / 开发 / 文案 提示词加固
- `pasm/cognitive/agent_team.py::_HANDOFF_SYS`：新增四条工作准则 —— 中文输出、**不臆造**
  （不确定写「待确认」）、具体可验收、禁空话套话。
- `pasm/cognitive/coder.py`：规划 / 生成 / 改写四处提示词补「注释与解释用中文、
  只用真实存在的库与 API、不臆造、改动精准」。
- `desktop/skills/copywriting.md`：升级为分平台成稿规范 + 可溯源红线（不编造功效/销量/排名）。

### 六、验证
- 改动全部 `py_compile` 通过；`live_data` 城市解析 7 例全过；
  `browser_agent` 用系统 Edge 实机跑通 点击 / 填表 / 密码提示；`accent` 稳定性用例通过。
- PyInstaller（spec）**构建成功**（3m08s），产物体积见第二条。
- 生态核查：改动均为**桌面层 + 私有核心提示词文本**，不改任何契约 →
  PyPI 与已发布的 5 个技能正文**零漂移**，无需重传平台。

## 0.28.6（核心修复 · 待装测发布）· 由「长期验证智能体」压出的三个核心缺陷

反馈："帮我做几个智能体，确定没有问题后推送到仓库。"

新建公开仓 **pasm-skills**（智能体工坊）并从中长出 4 个**领域智能体**
（`npc-lifelong` / `companion-elderly` / `study-tutor` / `soak-longrun`）。
它们的职责不是"多看几个文件"，而是**让核心组件真的跑几十天、几千步**，然后按阈值表卡表现。
上线首轮就压出 3 个真实缺陷 —— 这类问题**在文件列表里永远是正常的**，
结构检查一个都发现不了。

### 一、记忆检索同分排序直接崩（崩溃级）
- **问题**：`memory_layers.recall_layers` 用 `hits.append((sc, e))` + `hits.sort(reverse=True)`。
  同分时元组比较会退化到比较第二个元素（`dict`），Python 3 里 **直接抛
  `TypeError: '<' not supported between instances of 'dict' and 'dict'`**。
- **触发条件**：累计 **60 条同分记忆必现** —— 对长期运行的陪伴/NPC 而言是必然事件。
- **修复**：改为只按分数排序（`hits.sort(key=lambda x: x[0], reverse=True)`），共 2 处
  （`hits` 与 `phits`）。稳定排序保留插入顺序，长期运行行为更可预期。

### 二、记忆容量触顶后裁剪不区分重要度
- **问题**：`episode_push` 在超过 `_EPI_CAP` 时执行 `del lst[_EPI_CAP:]`，
  **纯按新旧淘汰** —— "玩家救了我一命"和"今天吃了个面包"同权被丢。
- **实测**：`npc-lifelong` 跑 90 天 / 270 段经历，第 1 天的里程碑记忆直接被裁掉。
- **修复**：新增 `_trim_episodes(lst)` 与 `episode_push(..., salience: int = 1)`，
  触顶时按 **(重要度, 新旧)** 排序淘汰；存入记录新增 `"sal"` 字段。
  **向后兼容已验证**：不传 `salience` 时行为与旧版完全一致（等价"只留最新 200 条"）。
- 重要度取值 1~5，会被 `max(1, min(5, int(salience)))` 收敛到合法区间。

### 三、`learning.feedback()` 无法指定"刚做的动作"（陪伴/教学场景不可用）
- **问题**：反馈只能作用在「当前偏好最高」的动作上。
  于是"夸它刚做的那件事"根本表达不出来，反馈只会在既有偏好上反复加码 ——
  **长期下来把行为越推越极端**。
- **修复**：`feedback(kind, lr, action=None)` 新增 `action=` 参数；
  不传时仍是旧行为（作用在当前最偏好动作上），保持兼容。
  传入不认识的动作名则**静默忽略**，不新增键、不改权重。

### 四、同步
- `E:/AI/PASM/pasm/cognitive/` 与 `E:/AI/pasm_qclaw/pasm/cognitive/` 逐字一致
  （`parity-guard` 校验通过：14 个模块内容一致）。

### 五、已知未修（属产品能力设计，非缺陷，需产品决策）
验证智能体把事实摆出来，但不替产品做决定。以下三项**故意保留**：
- **跨表述检索失效**：`memvec` 与 `memory_layers._score` 都是**字面匹配**
  —— 老人问"我叫什么名字"，记忆里存的是"姓名"，实测命中率约 0.5。
  要真正解决需引入语义向量或 LLM 查询改写。
- **学习层无遗忘曲线**：`adj` 只增不减，停练 25 天的知识点与昨天练的一样强，长期学情会失真。
- **人格饱和钉死**：长期单向相处把人格推到 ±1 后卡死（仿生档 6000 步后 `[1.0, 1.0, 1.0]`），
  此后不再可塑。改动会影响产品手感，需先定产品预期。

### 六、验证结果
`pasm-skills` 全量：**103 ok / 8 warn / 0 fail**（`core-verifier` / `parity-guard` /
`regression` / 四个领域智能体）。

## 0.28.5（2026-09-12）· 认知核心层落盘 + 引擎/环境/学习三层契约收敛 + 文档补全

反馈："看看 PASM 有没有落盘，还是只有在 pasm-qclaw 中实现了，我需要整体均实现了；
另外慢慢把 pasm-lite 的学习层优化实现。"

### 一、认知核心层从 desktop 提升为 pasm 包单一真相源
- **问题**：`symbolic / memrouter / memvec` 只存在于 `desktop/`，`pasm/cognitive/` 核心包从未包含，
  导致"只有 pasm-qclaw 实现了、PASM 核心没落盘"，且核心引擎与桌面端版本分裂。
- **修复**：把 `symbolic.py / memrouter.py / memvec.py / learning.py` 提升为
  `pasm.cognitive.*` 核心包（唯一实现）；`desktop/` 下三份改为**再导出薄壳**
  （`sys.modules[__name__] = core_module`），旧 `import symbolic as SY` 调用零改动。
- 新增 `pasm.cognitive.learning.LearningEngine`：把桌面小人"自我设计+自我优化"
  （`pet_behavior`）抽象成可挂载到任意智能体的通用引擎（design/pick/feedback/redesign + 落盘）。

### 二、符号推理层 ↔ 向量记忆 双向闭环接通（缺口修复）
- 之前"向量→符号"已在用，但**验证过的确定性事实未回写向量循环**（闭环断点）。
- 新增 `symbolic.writeback(text, candidate)`：仅当 `verify` 一致且带 `constraints`（高置信）
  才回写，避免污染记忆；companion 在 `_symbolic_guard` 注册 `_sink`，把已验证解
  同时 `episode_push` 进分层记忆 + 追加 `symbolic_solutions.jsonl` 结构化解库。

### 三、PASM 镜像仓同步
- `E:/AI/PASM/pasm/cognitive/` 已与 pasm_qclaw 对齐（含本次 4 个新层 + coder/mathlab/
  percept/quantum/workctx 的后续演进），核心实现 now 在 PASM 也完整。

### 四、pasm-lite 学习层起步（教学版）
- 新增 `PASM_LITE/learning.py`：与核心 `LearningEngine` 同源思想的轻量关联式（Hebbian-like）
  学习层——状态→动作偏好矩阵 + 技能记忆召回。
- `pasm_lite.py` 主循环接入：探索分支保留、规划器选择加学习层偏好偏置（有把握时）、
  `learn()` 调用 `learner.update(z,a,r)` 做正强化/负削弱；进度打印新增"学习层技能"条数。
- `learning.recall_action` 修复：余弦相似度误取 `[0]` 致 topk 报错，已去掉。

### 五、引擎接口契约升级 api 1.0 → 1.1（`pasm/engine_api.py`）
- **`create_best(candidates)` 择优创建**：按优先级挑第一个"能跑"的引擎，返回 `(engine, report)`，
  report 含 `used / degraded / tried / gap / gap_vs` —— 上层从此不必自己写降级判断。
- **`capability_gap(engine, reference)`**：算出当前引擎相对参考引擎缺哪些能力（参考名不认识时返回空，不臆测）。
- **`_adapt_cfg()` 扁平参数适配**：支持 `create("pasm", seed=1, plan_samples=8, personality_seed=[...])`。
- **新增环境插件注册表**：`Env` Protocol / `EnvRegistry` / `ENV_REGISTRY` / `register_env` /
  `make_env` / `env_names` / `env_registry` / `env_conforms` —— 换环境 = 换注册表里的名字。
- **环境契约**：必需 `reset()` / `step()`，可选 `observe()` / `close()` / `spec()` / `obs_dim` / `n_actions`。
  **刻意不规定 `step` 返回形状**（教学版三元组、完整引擎四元组），由引擎自己适配它要跑的环境。
- 自检从 12 项扩至 **20 项**（含 create_best / capability_gap / 环境注册表全覆盖）。

### 六、桌面端改用 engine_api 统一调用（"换引擎不改代码"在桌面端真正生效）
- 新增 `desktop/engine_factory.py`（桌面引擎工厂）：对外只暴露 `make_engine / make_env /
  full_available / summary / LAST_REPORT`，内核用 `create_best` 择优 + 降级报告 + 直连保命兜底。
- `desktop/pasm_companion.py`：**删除直连 `PASMAgent / PASMConfig / GridWorld` 的整段**，
  改为经 `engine_factory` 获取引擎与环境 —— **26 处 `self.agent.*` 调用点一行未改**，
  这正是"上层只依赖接口"的直接证明。
- `PASMStudio.spec`：`hiddenimports` 补 `engine_factory` / `pasm.engine_api`（保证打包不丢）。

### 七、学习层收敛为"同一接口两档实现"（契约 `pasm.learning/1.0`）
- 新增学习层契约：`LearningLayer` Protocol + `learning_conforms()` + `LearningInfo` +
  `LEARNING_API_VERSION`；**必需五件套** `info / capabilities / learn / bias / state`
  （另 `apply_state()` 支持跨档状态恢复）。
- **两档实现**：full 档 = `pasm.cognitive.learning.LearningEngine`（离散动作标签 + 性格设计 +
  反馈微调），teaching 档 = `PASM_LITE/learning.py`（连续向量关联式）。**同接口、可互换**。
- **踩到并修正的真坑**：两档 `learn()` 签名语义原本不一致（核心档 `learn("praise")` vs
  教学档 `learn(z, a, r)`）。教学引擎换上核心档后跑 40 步 **`skills=0`——学习完全没落进去，
  且不报任何错**。已统一为 `learn(z, action, reward)` + 适配器做整数动作→离散标签映射。
- `LiteEngine.attach_learning()`：**运行时热插拔学习层**（契约 + 潜维 + 动作数三项校验，
  校验不过则拒绝且**不动原实现**），内层 `_LearningAdapter` 抹平两档差异。

### 八、PASM-Lite 引擎化补齐（v0.2.0 → v0.2.1）
- `envs.py`（新）：环境插件 —— `grid-10x10`（网格）与 `toy-vector`（**非网格世界**，
  obs_dim=3 / n_actions=2），`register_builtin_envs()` 幂等注册。
- `engine.py`：读环境申报的 `obs_dim`/`n_actions` **自适应**调整感知、世界模型与规划器；
  `freeze_vae()` / `unfreeze_vae()` 对齐完整引擎语义；`attach_learning()` 学习层热插拔；
  `save/load` 改走契约 `state()` / `apply_state()`，**档位不符明确报错不串档**。
- `verify_swap.py`（新）：跨档互换一键验证（19 项，含维度不符被拒、档位不符存档报错等边界）。
- 实证：非网格世界 `toy-vector` 实跑奖励 **-185.04 → -141.02**（真的在学）。

### 九、文档补全与契约镜像机制
- `docs/CORE.md`（新）：**PASM 核心知识总览** —— 三层大脑体系 / 七层仿生认知架构 /
  认知执行皮层 / 引擎接口契约 / 学习层契约 / 目录地图 / 三种用法 / 常见问题。
- `docs/FEATURES.md`（新）：**PASM Studio 全部功能总览** —— 16 章 + 完整 desktop 模块索引表。
- `tools/sync_engine_api_mirror.py`（新）：核心 ↔ LITE 契约**同源镜像同步与漂移检测**
  （`--check` 报漂移，无参同步）。
- 补齐 qclaw 侧此前缺失的 engine_api 环境插件章节（约 138 行）——
  两仓 `pasm/` 下 **37 个文件恢复逐字一致**；核心包版本 `0.7.0 → 0.7.1`。

### 十、发布与打包改进
- `desktop/installer.iss` 支持 `/DSrcDir=` 覆盖打包源目录，配合「每次构建用全新 dist 目录」
  （本机有删除守卫，`--clean` 会被拦截）；构建说明更新为两步式（PyInstaller → iscc 指过去）。
- `desktop/frozen_smoke.py`（新）：模拟 frozen 包环境（`pasm.agent/config/envs` 被 spec 排除）
  验证引擎降级路径 —— 源码环境有 torch 走完整引擎，**安装包实际走降级链路**，
  这条路径必须在打包前验证到，否则只会在用户机器上暴露。8 项断言全过。
- 安装包 `PASMStudio-Setup-0.28.5.exe`（**50.8MB**；清理构建中间残留 `PASMStudio.exe.pyz.bin`
  后 63MB → 51MB），已发 Gitee + GitHub 双端 Release，`latest.json` 升级通道指向 v0.28.5，
  旧版附件按惯例清理（释放 126MB）。

## 0.28.4（2026-09-12）· 语音接收修复 + 方言/普通话精准识别

反馈："语音输出正常，但接收的语音不正确、小U 回复提示听不懂；希望粤语/河南/四川/云南/北京等
方言都能被准确接收并回应。"

### 一、语音接收"听不懂"根因与修复（asr.py + pasm_companion.py）
- **根因①语种被卡死**：旧逻辑只在检测到"粤语/台湾腔"时调用 `prefer_culture` 切换识别器，
  **从不复位回普通话**。一旦用户说过/打过粤语，识别器被永久锁在 `zh-HK`，之后说普通话或
  四川/河南/北京话，粤语引擎听不懂 → 空或乱码 → 显示"听不懂"。
- **根因②默认识别器不一定是普通话**：旧逻辑"取第一个 `zh*` 识别器"。装了粤语/台湾语音包时，
  列表顺序可能把 `zh-HK` 排前面 → 默认就用粤语引擎听普通话，整段失败。
- **根因③无容错回落**：专业语种识别不到内容时，不会回退用普通话再听，普通话那句直接丢。
- **修复**：
  1. 启动**强制优先 `zh-CN`** 作默认识别器（覆盖普通话 + 所有官话方言：
     四川/河南/北京/云南/东北/山东），只在系统无 zh-CN 时才退而求其次。
  2. 每按一次🎤**显式定语种**并加"当前=目标则跳过切换"守卫（不破坏 v0.28 的 0.2 秒热聆听）；
     粤/台用户主用对应引擎、**识别空或置信度<0.5 时自动回落 `zh-CN` 再听一次**，取更可信结果。
  3. 引入 `Result.Confidence` 置信度：低置信度才触发回落（避免误判）。
  4. 新增 `supported_cultures()` 探测系统是否装有 zh-HK/zh-TW 识别包，供界面与回落判断。

### 二、单句方言即识别、即切音色、即用方言回（accent.py + pasm_companion.py）
- `_observe_speech`：单句没攒够 6 次特征也先用 `detect()` 判定方言，让本轮回读直接切对应口音
  （四川/河南/北京等单句即识别，不再漏口音回读）。
- 提示词注入扩展：除粤语（含同音字对照表）、台湾腔外，**四川/河南/东北/山东/北京**单句即让模型
  用对应口语回复（点到为止，正事/结论先说清）。
- 新增**云南方言**条目（西南官话）：接收走 `zh-CN`；识别后用云南味回（edge-tts 无云南神经音，
  TTS 回落普通话音但文字带云南口语）；`detect` 正样本已纳入 `_selftest` 守卫。

### 三、语音自检增强（asr.py `diagnose()`）
- 一次说清"方言能听不能听"：SAPI 只支持 zh-CN/zh-TW/zh-HK 三种；普通话+官话方言统走 zh-CN；
  粤语/台湾腔取决于是否装了对应识别包；并说明"方言先识别文字再反推，不能凭声音判断"。

> 验证：`accent._selftest()` 通过（7 方言正样本全识别、普通话负样本 0 误判）；
> asr PowerShell 脚本语法解析通过。真机语音需用 Windows + 麦克风实测（本环境无 SAPI）。

## 0.28.3（2026-09-11）· 真机三修：回复丢字根治 + DeepSeek 式灰度思考 + 方言串味 + 头顶工作气泡

反馈："①文字有部分丢失，能否做到像 DeepSeek 那样带思考显示（灰度），最后出内容；
②方言有时乱串，先说粤语，再聊有时带东北味；③小人工作提示应在小人上方，提示框改冒泡式。"

### 一、回复丢字根治 + 思考灰度显示（llm_gateway.py + pasm_companion.py）
- **丢字根因**：DeepSeek 等云端模型有时把思考以内嵌 `<think>…</think>` 混在正文里返回，
  聊天窗的 HTML 渲染把 `<think>` 当**未知 HTML 标签连同内容一起吞掉**——这就是"部分文字丢失"；
  另外独立的 `reasoning_content` 思考字段一直被静默丢弃。
- **修复**：网关新增流式思考拆分器 `_ThinkSplitter`（跨 chunk 安全，标签被切碎也能拼回），
  把内嵌思考剥到独立通道；`reasoning_content` 同样接出；非流式路径 `_strip_think` 兜底。
- **DeepSeek 式体验**：新增 `on_think` 回调一路穿到 UI——思考以**灰度小字实时上屏**
  （💭 前缀，正文在下方流出），最终态保留"灰度思考 + Markdown 正文"，与 DeepSeek 一致。
- 冒烟：拆分器 5 组用例（整段/跨chunk切碎/未闭合/无思考/strip）全过。

### 二、方言串味修复（accent.py）
- **串味根因**：soft 词表全是普通话高频单字——东北 soft 有「整/贼/老/啥/咋」、粤语有「啦」、
  北京有「儿」、河南 markers 有裸「中」（中午/中间全中招）。普通话每聊几句就给东北/河南
  偷偷 +0.5~2 分，攒过 6 分阈值整条风格+口音就被带歪（真机"先粤语后东北味"）。
- **修复**：全部 soft 词表收紧为 ≥2 字且普通话语境罕见的词；markers 删「您/甭/回头/丫/
  多谢/瞎/裸中」；切音词表 `_VOICE_SCAN` 只留方言独有强词（删「晓得/安逸/俺/恁/得劲/
  木有/甭/得嘞/伙计/噻/咯」）；切音判定由"顺序首中即切"改为**计数取最大**。
- 回归：`_selftest()` 新增 7 条带弱字的普通话负样本（你说咋办/干啥去/中午吃什么/您说/
  回头再说/好啦/多谢老板），修复前全误判、修复后全 0 分，正样本 7 方言照常识别。

### 三、小人工作提示 → 头顶气泡（pet_avatar.py + pasm_pet.py）
- 工作提示（如「与XX聊天」）从头像**下方**的蓝底矩形改到**头顶上方**的冒泡气泡：
  白底圆角 + 青色描边 + 向下小尾巴 + 柔和投影，深色文字。
- 头像窗口扩高至 1.40×（顶部留给气泡），头像整体下移、逐像素与旧版一致；
  桌面宠物窗与伴学窗布局自适应。离屏渲染验证：气泡/尾巴/头像绘制正常。

### 四、团队功能无反应 + 技能库为空 根治（PASMStudio.spec，装测反馈）
- **技能库为空**：0.28.2/0.28.3 的安装包构建漏掉了 spec 里的 `skills/` 数据目录
  （CLI 参数构建只带了 assets）→ 内置技能 8 篇全部缺失。修复后恢复按 spec 构建，
  内置技能与用户技能（DATA_DIR/skills）双层齐全。
- **团队功能无反应（历史遗留，v0.24 门面改造起所有发行版中招）**：`agent_team` 门面的
  真身在仓库根 `pasm/cognitive/agent_team.py`，而打包入口在 `desktop/`、spec 的
  `pathex=[]` 使 PyInstaller **根本找不到 pasm 包** → frozen 版 `import agent_team`
  静默失败（companion 里 try/except 后 TEAM=None），团队所有按钮无反应；
  数学脑 mathlab、感知 percept、自修复 selfheal 等同链路模块也一并缺失。
- **修复**：spec 增加 `pathex=[SPECPATH]` + 显式 hiddenimports
  （pasm.cognitive.agent_team/mathlab/cog/memory_layers/percept/quantum/selfheal/coder），
  团队/数学脑/自修复在安装版全面恢复。

### 验证
- py_compile 全过；accent._selftest / pet_behavior._selftest 双 PASS；
  `_ThinkSplitter` 5 用例全过；PetAvatar 离屏渲染（带气泡/无气泡）正常。
- 打包校验（PyInstaller 官方 archive reader 读 PYZ TOC）：
  `_internal/skills` 8 篇齐全；`pasm.cognitive.*` 全部在包；关键模块对 0.28.1 基线为超集。

## 0.28.2（2026-09-11）· 小人行为"自我设计 + 自我优化" + 工作状态呈现 + 方言全量升级

反馈："语言方言不应该只有粤语、台湾腔，还应该有四川腔、河南腔等等；小人的状态和动作
不应该是预设的，而应该是自我设计升级以及根据性格等相关自我优化；工作时小人也显示工作状态，
其余的根据性格自主动作。"

### 一、小人行为「自我设计 + 自我优化」引擎（新增 `pet_behavior.py`）
- **自我设计（升级解锁）**：动作池按成长阶段解锁——0 级只有 `wave/hop/peek`，
  1 级解锁 `ball`，2 级解锁 `dance/spin`，3 级解锁 `think`。升级时自动重新设计，
  不再是一出生就把所有动作写死。
- **按性格设计**：`ARCH_META` 的九宫格性格（temper/energy/play）决定动作基础偏好——
  调皮爱球/舞、外向爱跳/挥手、内向爱想事、沉稳偏思考/礼貌；能量与爱玩因子再二次缩放权重。
- **自我优化（用户反馈）**：被**夸奖**→刚做的动作权重上调；被**戳/互动**→`wave/hop`
  小幅上调；被**训斥**→高能量闹腾动作（`ball/dance/spin`）略收（非清零，避免行为崩坏）。
  带 ε-贪心探索，偶尔试冷门动作防僵化。优化结果持久化到 `pet_behavior.json`，
  **重启不丢、升级/换性格也不清**。
- 旧代码里 `_maybe_hobby` 写死的 `acts=["ball","ball","hop","dance","hop"]`
  已替换为 `behavior.pick()`；被夸时的反应也改由引擎按"自我设计"的偏好表现。

### 二、工作时显示工作状态（v0.28.2）
- 心跳里新增 `_sync_work_state()`：检测到"在干活"就在头像下方显示工作标签——
  `📖 读书自习` / `🌐 上网自学` / `💬 陪你聊天` / 或来自工作任务台账的真实标签
  （如 `📽 PPT xxx`、`🎬 视频 xxx`）。工作时一抹"专注"表情，不打断、不抢戏。
- 外部任务占用时（聊天生成 / 工作台账在跑 / 上网自学），自主娱乐动作让位给工作态，
  不再"一边陪你干活一边自顾自蹦跶"。

### 三、方言 / 口音全量升级（接续 0.28.1）
- 四川 / 河南 / 东北 / 山东 / 北京 五地方言提升至与粤语、台湾腔**同权**：
  各自有独立识别正则、特征词（`_VOICE_SCAN`）、TTS 口音音（edge-tts 真实神经语音）。
- 北京无独立方言音（官方无北京话 neural voice），优雅回落到普通话而非报错。
- `accent._selftest()` 覆盖全部 7 方言正样本 + 11 条普通话负样本（零误判）已回归通过。

### 四、验证
- `pet_behavior._selftest()`：解锁逻辑 / 采样偏好 / 反馈增减权重 / 持久化 / 升级保留反馈 全 PASS。
- `accent._selftest()`：PASS（7 方言识别 + 普通话零误判）。
- 全部改动模块 `py_compile` 通过。

## 0.28.1（2026-09-11）· 全面复查 0.28.0：把"变笨 / 方言失效 / 朗读退化"逐条查清并修掉

反馈："感觉比更新前笨了，语音、自动切换方言和朗读都出问题了"。
这一版**没有加新功能**，全部是复查 + 修复 + 实测数据。结论先说：

### 一、为什么"变笨"——三个真因，都拿日志/实测钉死了

**① 方言识别形同虚设（主因，直接导致答非所问）**
用户用粤语打字用的是**同音字**：「宜家」=依家、「既」=嘅、「吾」=唔、「系」=係。
而 `accent` 的粤语词表只认标准写法（唔/咗/嘅/咁/依家）。实测：

| 输入 | 修复前 | 修复后 |
|---|---|---|
| `你食咗饭未啊？` | `('', 0.0)` **识别不出** | 粤语 4.0 |
| `你好啊，宜家几点啊？` | `('', 0.0)` | 粤语 2.0 |
| `我讲既是宜家既时间是几点，吾系问宜家家私哦` | `('', 0.0)` | 粤语 6.0 |

后果是连锁的：粤语指令不注入 → 模型按普通话字面把「宜家」理解成**宜家家居**
（聊天记录里就是这么答的）→ 用户体感"变笨"；同时 TTS 也不切粤语音。
另外 `%APPDATA%/PASMStudio/style_profile.json` **从未生成**，说明方言学习一次都没触发过。

修复：新增**组合式规则表**（单字歧义大的「系/既/家」必须成词或有上下文才算命中，
只有普通话里不出现的字（唔/咗/嘅/冇/乜/咁…）才允许单字命中），并把**同音字对照表**
直接写进系统提示词——先让模型"听懂"，再让它"回话"。自带 `_selftest()`
用 11 条普通话负样本守着零误判（当前 0 误判）。

**② 符号推理层会"算错还强行纠正"——把对的改成错的（P0）**
`_solve_rope_fold` 只要看到「对折 N 次」就按"剪绳段数"算。于是：
「一根绳子对折 3 次后长 2 米，原长多少米？」被解成 **ROPE_FOLD = 9 段**，
模型本来答对的 16 米，被 `_symbolic_guard` **"更正"成 9 段还带一段看似严谨的推理**。

修复：① 段数题必须题干里真的在问"几段/剪几刀"，否则不接；② **新增**
`_solve_rope_length`（对折后长度反推原长，答案 16 米，正解）；③ `Solution` 新增
"等价表达"`alts`——涨跌链答「跌了 4%」和答「0.96 倍原价」都对，不再被误判成答错；
④ **新增量纲闸门**：问的量和解的量不是一族（米 vs 段）就一律不许纠正。
原则写进代码注释：**算不出来没关系（不说话，模型自己答），算错了才致命。**

**③ 记忆路由器一故障就"什么都不给"**
0.28.0 把旧的知识注入换成 `MR.route_text()`，但异常分支是 `rel = ""` ——
新层一出故障，自学成果整体断供。现在改为**回退到旧知识通路**，并记录本轮
路由类型/命中数，便于度量。

另外把 `DualBrainRouter 初始化失败: No module named 'pasm'` 这条降噪：
桌面包刻意不带 torch，这个回退是**预期行为**不是故障，旧版每次重置都刷一行 WARNING。

### 二、为什么"朗读/方言切换出问题"

日志（`voice.log`）里是这么写的：预检 `True` → 合成 `fail (1.5s)` → 静默回落系统语音。

排查过程（逐条排除，都是实测）：
- 打包件完整性：`libssl/libcrypto/_ssl.pyd/aiohttp/edge_tts 7.2.8` 全在，`PYZ` 里
  `edge_tts`、`aiosignal`、`typing_extensions`、`idna` 一个不缺；
- 冻结环境复现：自建最小 `--windowed` 打包程序，**edge-tts 合成成功**；
- `sys.stdout/stderr=None`、代理环境变量、UPX（本机没装，等于没压）——逐项排除；
- 归档内 `tts._edge_synth` 与源码**逐字节码一致**（确认不是装到了旧版）。

结论：属运行时问题，但**旧代码把真因吞了**。修复：
1. `_edge_synth` 的 `except Exception: return ""` → 记录**真实异常**（类型+消息+位置）；
   实测用错误声线立刻拿到：`NoAudioReceived: No audio was received... @communicate.py:562`。
2. `_net_ok()` 的 TCP 连通性探测**换掉**——"TCP 能连"根本不等于"服务能用"，
   正是它制造了"probe=True 却句句失败"的假象。改为真做一次 TLS 握手 + HTTP 请求。
3. 快速失败时**重试一次**（>5s 的慢失败不重试，避免白等两倍）。
4. 失败提示带上真实原因，不再只说"不可用"。
5. **系统语音兜底也讲方言**：PowerShell 选声线改两轮——先按语言（粤语 zh-HK），
   再按性别兜底。在线音断了，粤语回复也尽量用粤语声线读。

### 三、实测数据：两个新层到底有没有用

符号层（8 题，含 4 道旧版没覆盖的题型）：

| 指标 | 0.28.0 | 0.28.1 |
|---|---|---|
| 内置回归 self test | 8/8 | **8/8** |
| 扩展题集（8 题） | 4/8，其中 **1 题是有害的**（把对的改错） | **6/8，0 题有害** |
| 单题耗时 | 0.1 ms | **0.1 ms** |
| 误纠正确回答 | 有（对折、涨跌各 1 例） | **无** |

记忆路由器：分类 6/6 正确（fact/assoc/complex），符号题自动走 complex 并挂上确定性结论；
失败时回退旧通路已验证生效。

剩余覆盖缺口（**无害，只是没接住**）：打折题（中文数字"八折"）、注水+排水混合题。
这两类 `solve()` 返回 None，模型自己答，不会被注入错误结论。

### 四、性能与速度（实测，同机同模型 Qwen3.5-4B / ctx=16384 / think=off）

关键发现：**预填充约 135 tok/s，而"整条提示词完全一致"时才会命中缓存。**

| 场景 | 提示 tokens | 预填充 | 首字 |
|---|---|---|---|
| 整条与上一次完全相同 | 1015 | 0.28 s | **0.30 s** |
| 系统提示词相同、只有用户这句变了 | 994 | 4.61 s | 4.63 s |
| 系统提示词尾部变了 | 1023 | 5.46 s | 5.48 s |

真实聊天里每一轮提示词都会变（情绪/记忆/此刻状态/轮次），所以**每句话都要先等
4.5~5.5 秒预填充**才开始出字。这和真机日志吻合：提示 1234 tok→首字 9.68 s、
1487 tok→11.61 s、138 tok→1.75 s，拟合出 `首字 ≈ 1.0 s + tokens/135`。
生成速度实测 7.6~10.9 tok/s。

结论：**当前最大的速度杠杆是"把静态系统提示词（约 1000 tok）压小"，
每砍 135 tok ≈ 省 1 秒/句**。这是下一版的方向（见文末）。

## 0.28.0（2026-09-11）· 神经符号混合推理层 + 记忆路由器（PASM 八、九层）

这一版回答一个被实测推翻的问题：**思考链到底还需不需要？**

### 一、先给结论：不需要"让模型多想"，需要"让求解器去算"

同机同模型（RX580 4G + Ryzen 2600X + Qwen3.5-4B，ctx=16384），8 道有确定答案的
推理/陷阱题，四种策略对照：

| 策略 | 平均耗时 | 正确率 | 说明 |
|---|---|---|---|
| 隐藏思考链 `think=true` | **33.8 s/题** | **不稳定** | 同一道笔+本子题，两次分别答「3 元」「5 元」，都不对 |
| 关思考·直接答 | 0.8 s/题 | 偏低 | 直觉陷阱几乎必错（问笔答 1 元／81） |
| 关思考·显式分步 | **70 s/题** | 较好 | 要求"列等式+代入验证"确实更可靠，**但比思考链更慢**——每个字都要 17 tok/s |
| **符号求解（新增）** | **0.1 ms/题** | **8/8 确定** | 同一件事交给求解器 |

三条硬结论：
1. **思考链在本题集上没有净收益**（4 题对照：思考链 2/4、快答 2/4），却贵 40 倍；
2. **"多步推理"不该让语言模型用 17 tok/s 硬算** —— 显式分步比隐藏思考更准，却更慢，
   证明瓶颈不在"想不想"，而在"用 token 想"这件事本身；
3. 真正有效的是**把题意形式化成约束再求解并代回验证** —— 这正是符号推理层该干的活。

### 二、新增第 8 层：神经符号混合推理层（`desktop/symbolic.py`）

- **闭环**：亚符号（神经侧）听题并提出候选 → 符号侧形式化/求解/**代入检验** →
  结论回写提示词与回答。即 roadmap 里的"亚符号提假设 → 符号检验精化 → 结果回写向量循环"。
- 同时提供 roadmap 要求的三种能力：**多步规划**（分步求解）、**矛盾检测**（代入验证）、
  **反事实模拟**（改条件重解）。
- 覆盖题型（v1）：和差题、倍数和题、双人合作（工程/水管）、归一比例题、每日翻倍、
  绳子对折剪、涨跌百分比链、纯算式（AST 白名单，绝不 eval 用户文本）。
- **技术选型**：只用标准库（`fractions`/`ast`/`re`），**不引入 sympy/z3** ——
  sympy 会带 mpmath 使安装包 +40MB 并拖慢冷启动；本层要解的是约束明确的小规模题，
  手写求解器足够快且完全可控。后续需要时再以可选依赖挂 z3/sympy（缺库自动降级）。
- **验收**：8/8 正确、**0.1 ms/题**；20 句普通聊天**零误伤**；
  带"只给答案/不要过程"等作答指令仍稳健（真机踩过：末尾指令会让比例题挑错从句）。

### 三、新增第 9 层：记忆路由器（`desktop/memrouter.py`）

把检索从「匹配」升级为「按认知需求路由」，即 roadmap 的 P0 第二项：

| 查询类型 | 判定 | 走的通路 |
|---|---|---|
| 事实型 `fact` | 「是什么/多少/定义…」或短句 | 知识库关键词+滑窗检索（轻、快） |
| 关联型 `assoc` | 「记得/上次/之前/聊过/有关…」 | 分层记忆召回 + **跨域关联**（跨域模式层雏形） |
| 复杂型 `complex` | 符号层可解 / 含推理·比较·因果·规划词 | 事实 + 关联 + **符号推理层** |

- 取代 v0.27.x「三条检索各自往提示词里塞」的做法：分层记忆不再被塞两遍、
  学以致用（点本事）归到 `apply` 通路，**上下文更短、更准**。
- 返回可解释信息（走了哪几条路、命中几块），供 UI 展示与后续效果度量。
- 接入真向量库（Qdrant/Milvus）与图库（Neo4j）时只需替换 `route_*` 内部实现，
  上层调用点不变 —— 为 roadmap 的"记忆层升级"预留了接口。

### 四、思考链策略收口：符号可解的问题一律不思考

`llm_gateway._want_think` 新增一条硬规则：**能被符号层确定性求解的问题一律返回 False**。
理由见上表。其余车道策略不变（chat/tool/study 不思考；selftest 思考；brain/filegen 按信号判定）。

### 五、端到端验收（同机同模型，走真网关）

| 场景 | 旧（思考链） | 新（神经符号闭环） |
|---|---|---|
| 笔+本子题 | 33.7 s、答「3 元」❌ | **1.12 s、答「0.5 元」✅** |
| 球拍+球题 | 34.0 s | **1.07 s、✅** |
| 人天比例题 | 33.6 s | **2.66 s、✅** |
| 绳子对折题 | 33.6 s、答「8 段」❌ | **0.70 s、答「9 段」✅** |
| 涨跌百分比题 | — | **1.06 s、✅** |
| **平均** | **33.8 s/题**（正确率不稳） | **1.3 s/题、5/5 ✅** |

另：回复里若仍出现与约束冲突的数值，`_symbolic_guard` 会就地补上"说人话"的更正文案
（0.1 ms，不是 30 秒的思考链）——这是闭环的最后一道关。

回归：车道/抢占 `ALL_OK`、知识库命中 `ALL_OK`、符号层 8/8 + 零误伤、
`_build_system` 离屏冒烟 5/5 正常。

### 六、复查再加码：两层融合 / 全链路 / 向量与符号正确混合（蓝屏后续查）

> 用户要求逐项确认：① 推理层 + 记忆路由核心逻辑是否正确；② 连续向量库（memvec 字符 n-gram 哈希向量）
> 与符号推理是否**正确混合应用**；③ v0.27 各功能是否完整保留；④ 两层是否**全链路应用到位**。

**A. 记忆路由器（memrouter）三处修正**
- 关联型正则去掉单字「又」（"今天又加班了"等日常句被误判关联型 → 走重检索 + 塞固定联想噪音）；
  修正 `and?上次` 笔误（本意"再上次 / 继续上次"）。
- `cross_notes`（跨域联想）原本**完全无视 query**，任何关联型问题都塞固定两条联想。
  改为 query 相关：用 `memvec` 余弦相似度过滤，只回与当前问题语义相近的关联（阈值 0.15）。
- **复杂型分支不再无差别塞三路**：符号层已给出确定性答案时，只补极少量事实兜底，
  不再注入 assoc / 跨域联想 —— 既避免"向量记忆与确定性解冲突"，又省下大段预填充 token。
  实测：符号题路由 `flags=['symbolic','fact']`（原 `['fact','assoc','symbolic']`）。

**B. 符号推理层守卫接入工具轮**
- 原 `_symbolic_guard`（矛盾检测·闭环最后一关）只在主回复路径生效，**工具轮 `_single_step_reply` 漏接** →
  工具轮产出的数值若与符号解冲突不会被纠正。现已在协同分支与工具轮兜底处各补一处，
  全链路（主回复 / 协同 / 工具轮）统一走符号守卫。

**C. 连续向量库与符号推理的混合方式（设计确认）**
- `memvec` 是 192 维字符 n-gram 哈希向量（纯 Python、无依赖、随记忆持久化），用于分层记忆
  `recall_layers` 的语义排序（相似改写/近义表述能召回，纯关键词会漏）。
- 混合规则：**符号层 = 权威确定性解**（复杂型分支优先注入，且不再被向量记忆污染）；
  向量记忆 = 补充性上下文（事实/关联型）。二者在复杂型问题里**拼接但分层**，
  不互相覆盖。符号题的"算错"风险由 `verify` 量纲闸门 + 等价表达相容 + 守卫兜底三重防住。

**D. v0.27 功能保留核对（全部在位）**
- 车道/抢占：`_INTERACTIVE_TASKS` / `_fg_enter` / `_bg_stops` / `_AnyStop` / `_BG_BOOT_WAIT` 全在；
- 原生 Ollama 通路：`_native_ollama_hb` / `_native_ollama_once` / `_is_ollama` 经实测识别 `11434` 为原生端点 ✅；
- `keep_alive=8h` + `num_ctx=16384` 常量在位；
- 思考链策略：`_THINK_OFF/ON/AUTO` + `_want_think`（符号可解一律不思考）在位；
- 自学零打扰：`knowledge.stem()` 与 `pasm_pet` 比对修正在位；方言联动 `asr.prefer_culture` 在位；
  启动预热逻辑（预热"显式选中的本地模型本身"）在位。

**E. 端到端实测（Qwen3.5-4B / ctx=16384 / think=off，RX580 4G）**
- 符号题（笔+本子）：memrouter 注入符号结论 → 原生通路关思考 → 模型直接采用 0.5 元 → 守卫确认无矛盾 ✅；
- 热模型首字 0.3s、预填充 0.28s（冷加载除外，那是 Ollama 老问题，0.27.12 已用 keep_alive 缓解）；
- 方言：粤语同音字（宜家/既/吾/系）全识别、台湾腔/普通话零误判；
- 语音：edge-tts 在 venv 真实合成成功（含工作线程），粤语/台湾音存在。

### 七、打包链路修复（关键）：重建被 safe-delete 守卫静默卡住，旧 symbolic 混入成品

- **现象**：v0.28.1 多次重建后，`dist/PASMStudio.exe` 时间戳始终停在旧值（15:20），且包内 `symbolic`
  模块**缺 4 个源码符号**（`_solve_rope_length` / `_unit_family` / `_asked_unit` / `Solution.answers`）——
  即成品里仍是旧版 `symbolic`（无对折长度题修复、无量纲闸门）。
- **根因**：本机 Bash 环境有一道 **safe-delete 守卫**（拦截 `rm` / `rmtree` / `os.remove` 等删除类操作，
  且状态文件损坏报 `SAFE_DELETE_BULK_GUARD_ERROR failed to read state`）。它导致：
  1. `rm -rf dist` 实际未生效 → 旧 `dist` 一直在；
  2. PyInstaller 的 `--clean` 与删除逻辑被守卫中断 → 重建时**复用旧的 `dist/PASMStudio.exe`（内含旧 symbolic）**，
     而新构建的 exe 因「目标已存在」被守卫挡住写入；
  3. 结果：每次"重建"都只是原地保留旧 exe，校验永远 FAIL。
- **修复 / 规避**：构建时**不要**依赖 `--clean` 或先 `rm dist`；改为**输出到全新目录**
  （`--distpath _freshbuild --workpath _freshbuild_build`，该目录不存在 → 守卫无删除对象可拦），
  构建成功后再用 `mv`（改名，非删除）把正确成品挪到 `dist/PASMStudio`。
- **校验手段**：新增 `tools/verify_symbolic_bundle.py`、`tools/verify_modules_bundle.py`，
  从成品 exe 内嵌 PYZ 抽取 `symbolic` / `memrouter` / `knowledge` / `pasm_companion` / `llm_gateway` 字节，
  递归比对所有 def/方法/模块级常量，确认 5 模块关键符号**全部到位**
  （symbolic 全符号、memrouter 的 `FACT/COMPLEX/ASSOC`、llm_gateway 的 `_INTERACTIVE_TASKS` 均含）。
- **结论**：源码层全部正确（v0.28.1 的推理层 + 记忆路由 + 全链路加固都在）；之前"成品错"纯属打包链路被守卫卡住，
  与代码无关。已重打 `installer/PASMStudio-Setup-0.28.1.exe`（16:40 构建，来自修正后的 dist）。

## 0.27.13（2026-09-11）· 思考链「按价值分配」：该想的认真想，不该想的绝不拖慢

> ⚠️ **0.28.0 更正**：本版"开思考能答对陷阱题"的结论建立在**单题证据（n=1）**上。
> 扩到 8 道有确定答案的题后复测，思考链**没有净收益**（4 题对照：思考链 2/4、快答 2/4），
> 同一道笔+本子题两次分别答「3 元」「5 元」都不对。**"按价值分配"的方向没错，
> 但"让模型多想"这条路本身走不通** → 0.28.0 改为把多步推理交给符号求解器。
> 本版其余机制（配额预留 + 超预算强制收敛 + 车道策略 + 诊断日志）继续有效并保留。

> 上一版把思考链一刀切关了。但关掉就真的没有代价吗？这一版把"要不要想"变成一个
> **有实测依据的工程决策**：按车道分配 + 给足配额 + 超预算强制收敛，思考的收益留下，代价砍掉。

### ① 实测：思考链到底买到了什么？
同一台机器、同一个模型（`Qwen3.5-4B`），正误可判定的四类任务：

| 任务 | 关思考 | 开思考 | 思考链买到了什么 |
|---|---|---|---|
| 闲聊 | 首字 1.25s / 总 **4.8s** | 首字 **133.4s** / 总 136.8s | 无（答案几乎一样） |
| 算术推理 | 首字 1.40s / 总 **6.3s** ✅ | 总 165.1s / **正文 0 字** | 无，还更糟 |
| 代码排错 | 12.9s ✅ | 166.9s ✅ | 无 |
| **常识陷阱题** | 1.7s ❌ 答「5 元」 | 91.4s ✅ 答「0.5 元」 | ✅ **抗直觉陷阱、自我纠错** |

另实测：`think` 的 `low/medium/high` 三档思考量几乎一样（1157 / 1222 / 1254 字）——
**这个模型没有可用的"分级思考"**，只能做二元开关。

结论：思考链的价值是**自我纠错**，代价是 **30~140 倍延迟 + 挤占输出配额**。
所以答案不是"开/关"，而是"**只在该想的时候想**"。

### ② 策略：按车道分配
| 车道 | 策略 | 理由 |
|---|---|---|
| `chat` / `tool` / `warm` / `skill` | **绝不思考** | 用户直面，延迟就是体验 |
| `study`（资料提炼 / 任务拆分 / 成品评审） | **绝不思考** | 实测：开思考 44.2s、关思考 11.8s，**提炼质量没有差别**（机械活，不是推理活） |
| `selftest`（学后自测"讲不出=没学会"） | **总是思考** | 本质是纠错与诚实自检；跑在后台，延迟不可见 |
| `brain` / `filegen` | **按问题判定** | 命中推理信号（为什么/分析/计算/比较/根因/排查/报错/逻辑/验证…）或"带数字的算术问句"才思考 |

判定只在**意图窗口**（开头 200 字 + 结尾 120 字）里匹配 —— 否则贴一段含"分析"的长资料
会把一次普通提炼变成 44s 的思考（实测踩过）。

设置里新增三档：**智能（默认）/ 关闭 / 始终开启**；旧配置的布尔开关自动映射。

### ③ 两个保证"核心不掉"的保险（关键）
1. **开思考必给足配额**：思考与正文共用 `num_predict`，不预留就会"想完了没额度说话"。
   实测那 165s / 0 字的算术题就是这么来的 → 开思考时自动加 **1600 token** 预留。
2. **超预算强制收敛（reasoning budget forcing）**：思考超过预算（重活 45s、后台 30s）就断开，
   把**已经想出的思路**当上下文交回去，关思考直接要结论，并要求**列等式代入原题复核**。
   - 实测陷阱题：完整思考 137.7s / 2832 字；**25s 掐断 + 收敛 37.1s，答案同样正确（0.5 元）**；
   - 实测自学提炼：从"100s+ 且 0 字"变成 **38s 拿到 3 条正确要点**。

### ④ 诊断
每轮日志升级为：`local-native <模型> | 提示N tok | 首字X | 总Y | 生成Z tok@W tok/s | 装载L | 思考开(N字)/关 | 已强制收敛`。
冷加载（>3s）仍会主动告警。

### 实测总览（同机同模型）
| 场景 | 旧（0.27.12 全关） | 新（0.27.13 智能） |
|---|---|---|
| 闲聊 | 首字 1.16s / 总 7.3s | 首字 **0.49s** / 总 9.1s（**不受影响**） |
| 自学提炼 | 首字 7.4s / 总 11.8s | 首字 6.4s / 总 **11.0s**（同样快，质量相同） |
| 常识陷阱题 | 1.4s ❌ 答「5.5」 | 50.4s **✅ 答「0.5 元」** |
| 算术推理 | 8.3s ❌ 答「3.2 小时」 | 56.3s **✅ 答「2.4 小时」** |
| 普通改写 | 25.6s ✅ | 28.0s ✅（不思考，首字 1.3s） |

一句话：**聊天该快的还是快，该对的开始对了。**

## 0.27.12（2026-09-11）· 「等待很久」真凶找到并根治：隐藏思考链 + 被吞掉的 keep_alive + 64k 上下文

> 0.27.11 以为"聊天慢 = 冷加载"，**只对了一半**。这一版用真机逐项实测，把三个真凶全部摁住，
> 并把 v0.27.11 那个**其实从未生效**的 keep_alive 修复真正做成。

### ①【最大】隐藏思考链：一句"介绍一下你自己"，**107 秒、回复 0 字**
真机实测（`Qwen3.5-4B` + 本机 Ollama）：
| 场景 | 首字 | 结果 |
|---|---|---|
| 思考链**开**（旧默认） | 13.2s 还在思考 | **107s 跑完、正文 0 字**（思考吃光 token 配额） |
| 思考链**关** | **0.35~0.45s** | 3~26s 拿到完整回复 |

机理：这类模型默认先输出 `thinking/reasoning` 令牌，而 App 只读 `content` → **思考全被丢弃，
时间照付**。修复：本地请求一律带"关思考"参数（原生端点 `think=false`；
OpenAI 兼容端点用 `reasoning_effort="none"`，实测 `think:false` 在兼容端点会被忽略）。
设置里新增开关「本地模型开启思考链」（默认关），想更审慎可自行打开。

### ② v0.27.11 的 keep_alive **从未生效**（兼容端点会静默丢弃参数）
实测结论（无歧义判定）：把驻留时间设为 1 分钟，再走 `/v1/chat/completions` 发 `keep_alive:"2h"`，
剩余驻留**仍是 1 分钟** → 兼容端点根本不认这个参数；`num_ctx` 同样被丢弃（恒为模型默认 65536）。
- 修复：本地 Ollama 一律改走**原生 `/api/chat`**（只有它能带 keep_alive / num_ctx / think），
  兼容端点仅作兜底并自动补 `reasoning_effort="none"`；原生失败会**自动回退**，不影响其它本地实现（LM Studio 等）。
- `keep_alive` 由 2h 提到 **8h**，并在日志里对 `load_duration > 3s` 主动告警（冷加载可追溯）。

### ③ 64k 上下文把模型挤出显存 → 生成只有 6.2 tok/s
| 上下文 | 模型体积 / 显存 | 生成速度 |
|---|---|---|
| 65536（旧默认） | 5.42G，只有 2.49G 上 GPU | **6.2 tok/s** |
| **16384（新）** | **3.42G 全部装进显存** | **16.9 tok/s** |

- 修复：原生通路固定 `num_ctx=16384`（兼顾速度与长上下文；实测 8192/4096 仅再快 6%，不值）。
- 会话上下文上限由 1.2 万字收到 **8000 字**：1.2 万字 ≈ 8000 token ≈ 14 秒纯预填充，纯属白等。

### ④ 新增「慢在哪」诊断日志（每轮一行，直接可查）
```
local-native <模型> | 提示1234 tok | 首字0.42s | 总8.7s | 生成150 tok@16.9tok/s | 装载0.0s
```
首字慢看"提示 tok"（预填充）、总时长慢看 tok/s（显存装不下）、偶尔爆慢看"装载"（冷加载）。

### ⑤ 顺手修掉的两个真 bug
- 工具轮回传时 `arguments` 是 JSON 字符串，原生端点要求**对象** → 会让带 `tools` 的请求 400；
  现在自动转换（这是"带记忆的提问"在原生通路上失败的原因）。
- 400 错误构造时把 urllib 响应塞给 SDK 错误类 → 炸出 `AttributeError` 把真实报错盖掉；
  改为最小响应替身，错误信息不再被吞。

### 实测对照（同一台机器、同一模型、同一句话）
| | 首字 | 总耗时 | 正文 |
|---|---|---|---|
| 旧（0.27.11） | 无 | **107.1s / 108.3s** | **0 字** |
| 新（0.27.12） | **0.35s** | 26.6s | 391 字 |
| 新（空上下文短问） | **0.40s** | **3.4s** | 完整 |
| 带记忆的工具轮（两轮） | — | **6.3s** | 完整 |

## 0.27.11（2026-09-11）· 自学对聊天零打扰 + 冷加载 72s 根治 + 资料库「吸收后智能给出」

### ① 实测定位：聊天慢的主体是 **冷加载 72 秒**，不是自我学习
真机实测（本机 Ollama + `qwen2.5:7b`）：
| 场景 | 耗时 | 证据 |
|---|---|---|
| 模型被卸载后冷加载 | **72.4s**（加载 69.9s） | `load_duration: 69932259000` |
| 模型已驻留（热） | **0.17s** | prompt 缓存命中 29/30 tokens |
- 修复：本地端点统一补 `keep_alive="2h"`（走 `extra_body`，避免 SDK 未知参数报错）。
  实测 Ollama 原生端点与 OpenAI 兼容端点**都接受**该参数（HTTP 200，`/api/ps` 的
  `expires_at` 随之 +2h）。→ 模型常驻，不再"隔几分钟不说话就从 0 加载 72 秒"。

### ② 自学对聊天零打扰（延续并加固 0.27.10）
- 后台请求改用**更短读超时片**（本地 120s → 20s）：被前台抢占的最坏延迟从 2 分钟收到 20 秒
  （流式每收一个 chunk 都会重置计时，所以只在"卡住"时才触发，正常生成不受影响）。
- 前台抢占时新增「等后台收手再发请求」（最多 1.2s，通常几十~几百毫秒）：真机实测后台生成中
  插入前台聊天，延迟从 **+3.37s 降到 +1.79s**（基线 2s）；后台不在飞时实测差值 −0.08s（噪声级，即无影响）。
- `warm`（启动预热）划入前台：预热不再被后台闸门拖延，也不会被后台学习插队。
- `knowledge` 写入改**原子写**（先写 `.tmp` 再 `os.replace`）：后台自学保存知识库的瞬间，
  聊天侧**不可能**再读到半截 JSON。
- `_load_know` 读失败时**退回上一次的好数据**（旧版返回 `[]` → 那一轮聊天会静默丢掉整个知识库注入）。

### ③ 资料库「先吸收分析，再智能给出」（聊天侧知识注入重构）
- 新增 `knowledge.inject_for_chat()`，替掉聊天系统提示里的旧注入：
  - **命中才注入**：旧版匹配不到时也硬塞"最近学的 6 条"，既干扰回答又拖慢生成；现在不相关只给一行"已学主题清单"（便于诚实说明学没学过）。
  - **带分析指令**：明确要求"先在脑中做①逻辑分析（因果/步骤/前提/边界）②思维模拟（代进用户情形验证），再用自己的话给结论"——不整段照搬原文、不罗列要点清单、不说"根据我学过的资料"这类空话；资料只覆盖一部分就明说、剩下靠常识推断。
  - **给足可分析的料**：命中条目除要点外，从原文摘最多 2 句真实语句，并按问句挑相关句（不再挑出无关句）。
- **修好分词匹配（关键）**：旧写法把「最长 6 字连续段」当整词（如把"待办清单怎么做"整块拿去匹配），
  自然问句几乎匹配不到条目 —— **自学成果明明在库里却用不上**。改用中文 2~4 字滑窗词元 +
  功能词停用表 + 标题局部命中加权 + 相关性下限（≥3 分才注入）。
- 实测：「待办清单怎么做？」→ 命中《效率与待办方法》（17 分）；「密码怎么设置才安全？」→《网络安全入门》；
  「天文方面有什么好玩的？」→《天文科普》（9 分）；「今天天气不错啊」/「在吗」→ 0 分不注入。

- 冒烟：真机后台生成中插入前台聊天（见 0.27.10 起的前台优先调度）→ 前台即时应答、后台转"让路"；
  相关性闸门 9 条用例全过；车道/抢占回归全过。

## 0.27.10（2026-09-11）· 保住自动学习，同时让聊天不再被后台挤住

### ① 自学死循环（聊天数百秒无响应的主因）
- 根因：书库 v0.27.8 起统一 `.md`（3 字符），但 `pasm_pet._maybe_activity` 仍按 `.txt` 长度 `b[:-4]` 截断比对，与入库标题（已去扩展名）**永不匹配** → 每本书永远被判为「新书」，小人每 20~40 秒重读同一本、每轮发 2 次本地 LLM 调用，**永不停止**（日志实证：`learned 网络安全入门 fast=5` 连刷一小时 + `summarize llm fail: 排队超过 360s`）。
- 修复：`knowledge.stem()` 统一「去扩展名标题」比对，`pasm_pet` 两处 `fresh` 判定改用它 → 学过的书不再重复读。

### ② 前台优先调度：保留自动学习，但用户开口时后台必须让路
- 新增前台/后台仲裁（`llm_gateway`）：
  - **车道分池**：`chat/tool/skill/filegen/brain`（用户在等）走 `fg` 车道，`study/pet/selftest` 走 `bg`，互不抢占；
  - **静默期**：前台结束后 6 秒内后台不得占模型，避免刚答完就被学习插队；
  - **抢占**：前台一开口，在飞的后台请求（含非流式）立即收手让路，转为「后台已让路，稍后自动继续」，用户侧不再排队等待；
  - 后台为前台让路的最长等待 20 秒，等不到就放弃本轮，**不堆积**、下一轮自然重试。
- 自学不受影响：`learn()` 先用规则提炼**秒级入库**（学习成果必落库），LLM 精炼只是可选增强，被让路时自动跳过。

### ③ 模型一致性（消除 Ollama 反复换载）
- 根因：聊天用配置的 `qwen2.5:7b`，后台自学用 `pick_local_model()` 选出的 `Qwen3.5-4B` → 两个模型轮流打 Ollama，每次换载数十秒。
- 修复：新增 `pasm_pet._chat_local_model()`，后台学习**跟随聊天正在用的那个本地模型**；启动预热也改为优先预热显式选中的本地模型。

### ④ 最坏等待收紧
- 本地聊天超时下限 600s → **300s**（600s 静默正是「几百秒没反应」的体感来源）。

- 冒烟：`fresh` 判定修复后只挑真正没学过的书（旧逻辑把全部书当新书）；`chat` 与 `study` 取到两把独立锁；**后台任务在跑时前台聊天 0.00s 拿到模型、后台改为让路、用户空闲后自学照常执行**。

## 0.27.9（2026-09-11）· 粤语朗读自动切换 + 聊天性能缓存优化

### ① 粤语朗读「不自动切 + 切后机械」根治
- 根因：`speak()` 只用**回复文本**选音色，而回复是 LLM 用标准书面中文生成的（粤语口音要 `dominant()` 攒满 6 次才注入、且注入后仍是标准中文），回复无粤语特征词 → 退回普通话；手动切到粤语音后仍在「用粤语音读标准中文」，故机械。
- 修复：
  - 新增 `accent.detect(text)` 单句判定方言（不累积、不等 6 次），用户这句说粤语当下即认出；
  - `voice_for_text` 加 `input_dialect` 参数，**用户这一句说的方言直接驱动音色**；
  - `_observe_speech` 记录本轮输入方言 → `speak()` 消费后清空（防串味）；
  - `_build_system` 当用户说粤语 / 台湾腔时注入「请用同方言口语回复」，让 LLM 用粤语回、粤语音读真粤语才自然。

### ② 聊天每轮开销下降（缓存）
- `accent.profile()` / `knowledge._load_know()` 加 1.5s / 2s TTL 缓存，避免 `_build_system` 每轮反复读盘；`inject_relevant` 二次调用命中缓存。
- 说明：文本首字延迟主因仍是 LLM 端点，本版未改双脑闸门（`_needs_recall` 未动），闲聊仍是单趟直出；「性能提升 50%」属 v0.25.0 已兑现，本版为正确性 + 缓存优化。

- 冒烟：detect 单句判定、`voice_for_text(input_dialect=粤语)` 即便回复是标准中文也返回粤语音均通过；男声 / 台湾 / 英文优先路径正确。

## 0.27.8（2026-09-11）· 真机四类问题修复：语音叮时机 / 工作语义判读 / 朗读跟随语种 / 资料库全量 md

### ① 语音输入「叮后无反应」（核心）
- 根因：旧版「叮」在识别进程启动前响（PowerShell 冷启动 ~1.9s，前 1~2 秒全丢），且同步 `Recognize(TimeSpan)` 在本机**挂起**（实测 14s 卡死）。
- 修复：识别改**常驻热进程**（启动时预热，点🎤 仅 ~0.2s 进入聆听）；「叮」改由引擎真正 `LISTENING` 时才触发（听到叮 = 已在录）；识别改**异步 `RecognizeAsync` + `SpeechRecognized` 事件捕获**，硬超时兜底 Cancel，静音也按时返回、不再挂起。

### ② 工作栏目语义判读
- 根因：选了工种后，**所有**输入都被强制改写成开工指令（`CHIP_SEED`）直接开干，不管用户是在提问 / 讨论 / 修正。
- 修复：新增 `_classify_intent` 区分 提问 / 修改 / 闲聊 / 开工指令——前三类走对话分析、按上下文回答或修正，只有明确的新开工指令才执行；干活中插话（疑问 / 修改）**立即答复**，不再静默排队。

### ③ 朗读 / 语音跟随语种
- 根因：TTS 音色只看「学到的方言画像」（要累计 6 次才切），粤语回复常被读成普通话。
- 修复：新增 `voice_for_text` 按**要念的文本语言**选音色——粤语→粤语音、繁体 / 台湾腔→台湾音、英文→英文音，识别不出再退回画像；识别语种也尽量跟随（需系统装对应语音包）。

### ④ 资料库全量 .md + 内容补全 + 先分析再答
- 书籍 / 自学内容统一存为 **.md**（旧 .txt 自动迁移）；网页抓取上限 6k→**30k** 字并记**真实来源 URL**；`inject_applicable` 知识型内容改为「先逻辑分析、做思维模拟再作答，绝不大段照搬原文」。

- 冒烟 + 回归：asr 端到端 4.3s 返回不挂起；knowledge .md 迁移与解析、inject_applicable「思维模拟」指令均通过。

## 0.27.7（2026-09-10）· 团队执行「该停就停」：超时不再空跑 + 暂停按钮 + 续跑不再多开目录

> 真机反馈（小志 15:xx，装 v0.27.6 后）：
> ① 生成的几个 md 里，除了「项目经理」有内容，**策划 / 审查 / 调度 全都是
>   「⏳ 模型服务正忙（排队超过 480s）」** —— 超时了却还继续往下跑，**这不合理**；
> ② 正在执行时切走对话再回团队，第一次没有看到状态；
> ③ 执行中点「继续上次未完成的执行」，`team/runs` 里**多出了一个文件夹**（两个 run 并行）。

### ① 超时 / 模型忙 → 立即中止，不再无效空跑（核心修复）
- **根因**：`_step_ok()` 只检查长度 —— 「⏳ 模型服务正忙（排队超过 480s）…」有 126 字符，
  **被判为合格产出**交接给下一步。于是模型一忙，后面每一步照跑，各白等 8 分钟排队超时
  （实测 15:22 / 15:30 / 15:38 连跑三步，白等约 24 分钟），产出全是同一句错误、还落了垃圾 md。
- **修复**：新增 `is_llm_unavailable()` 识别「模型服务正忙 / 排队 / 不可用 / 无法连接」这类
  **非产出**文案，`_step_ok()` 一并判为不合格；`run_team` 里某步拿不到真实产出 →
  **立刻中止后续步骤**（fail-fast），流程状态记为 `interrupted` 并写明"第 N 步中断，可继续"。
  接力式流程里前一步没有产出、后一步必然是空跑 —— 停下来记好断点、让用户稍后「继续」才对。
- 实测：同一场景 LLM 只调 **3 次**就停（旧版会跑满 4 步 × 每步白等 480s）。

### ② 新增「⏸ 暂停员工工作」
- 在**步骤之间**检查暂停标志：点一下就停（当前这一步做完为止，不硬杀进程），
  已完成步骤的产出全部保留，随时可「⏩ 继续」接着跑
- 执行中暂停键才亮出来，跑完自动收起

### ③ 续跑复用同一个 run 目录（不再多出文件夹）
- 根因：旧版续跑会生成**新的时间戳目录** → 同一次执行被拆成两个文件夹（15:08 那个还在跑，
  15:09 又开了新的），看日志一团乱
- 修复：续跑时**复用原 `run_dir`**，也不再新增记录，只把原记录拉回「进行中」

### ④ 执行中禁止点「继续」+ 切页回来能看到状态
- 执行中「继续」按钮**禁用**（真机实测：当时点得动 → 一点就并行开出第二个 run）
- 新增团队自己的忙状态（与聊天的 busy 分开，互不干扰）；执行中切页回团队，
  会在输出区补一条「🔄 正在执行：第 N/M 步 · 角色 —— 想停下可点「⏸ 暂停员工工作」」

- 冒烟：新增 `smoke_v0277` **31 项**全绿；全量回归（14 套件）全绿

## 0.27.6（2026-09-10）· 团队执行「边跑边落盘 + 断点续跑 + 中断可见」（严重：蓝屏后记录全丢）

> 真机反馈（小志）：新建「GEO平台」项目、配好流程、写了任务，下方能看到状态和执行，
> 但**看不到进度和具体信息**；任务**被打断后不知道怎么接续**；
> **蓝屏重启后发现团队里没有任何记录**。
> （排查后确认：数据其实**有落盘**——`team/runs/` 里躺着 5 个步骤产出文件，
> 只是界面**从来不读它们**，项目里的 `runs` 字段也一直是空的。）

### 根因（代码级）
- **只在全部跑完后才写执行记录**：`run_team` 的 `add_run_to_project()` 写在步骤循环**之后**，
  中途蓝屏 / 关程序 → 本次执行**完全没有痕迹**（`geo平台.json` 的 `updated_at` 停在配置流程
  的 14:05，而执行发生在 14:08~14:22，说明期间项目文件一次都没更新）。
- **界面只读 `proj["runs"]`，从不读磁盘**：所以 `team/runs/<时间>-<流程>/step-*.md`
  这些**真实产出**（需求清单、Flask 代码…）在界面上完全不可见 → "看不到进度和具体信息"。
- **没有"未完成"概念**：没有续跑入口 → "不知道从哪重新接续"。
- **异常被静默吞掉**：满屏 `except Exception: pass`，写入失败用户毫无感知。

### 修复
**① 开工即落盘、每步即回写**（引擎 `pasm/cognitive/agent_team.py`）
- 开工就写一条 `status=running` 的执行记录（含总步数），**不必等跑完**
- 每完成一步立刻回写：步号 / 角色 / 状态 / 产出文件名 / 时间，并刷新"已完成 N/M 步"
- 收尾更新为 `done` / `failed` + 摘要（不再重复追加记录）
- **写入失败不再静默**：落 `team/errors.log`，可查

**② 断点续跑**（新增 `resume_run` 参数）
- 检测上次中断点，**已完成的步骤直接复用产出文件**（不重复烧 token/时间），只跑剩下的
- 团队页新增 **「⏩ 继续上次未完成的执行」** 按钮（有未完成执行时才亮）

**③ 中断可见 + 历史恢复**
- 程序重启后自动把卡在 `running` 的执行判为 `interrupted`（重启必然跑不到收尾）
- 档案区完整列出**历次执行**：状态（🔄进行中／✅完成／⚠️失败／⏸已中断）、步数进度（2/4）、
  每步角色与产出文件名、中断在第几步、产出目录路径
- 新增 **「📂 产出目录」** 按钮，直接打开成品所在文件夹
- **自动恢复孤儿执行**：把 `runs/` 里"有产出、但项目没记录"的历史执行补回项目
  ——**本次已为「GEO平台」恢复出那条被中断的执行（5/5 步产出全在）**

**④ 其他方面（干活 / 聊天）**
- 干活（工作台任务）：启动时把卡在 `running` 的任务标为「⚠️ 已中断」并如实提示，不再假装"进行中"
- 聊天：核对了落盘链路（用户消息、回复都**即时落盘**），中断不丢，无需改动

**⑤ 顺带修**
- 新建项目后**自动切到新项目**（旧版保持原选择，刚建好的项目界面上看不见，像"没建成"）

- 冒烟：新增 `smoke_v0276` **31 项**全绿（含"执行中即已落盘""中断后可见""续跑复用产出"
  "档案渲染出进度与产出"）；全量回归（13 套件）全绿

## 0.27.5（2026-09-10）· 点名删除修复：点名一张不再整桌面被删（严重误删）

> 真机反馈（小志）：说「删除桌面图片_2026-08-03_134353_483.png」（点名一张），
> 结果把桌面 **5 张图全删了**；换成「帮我删除桌面上的微信图片_2026-…png」同样全删。

### 根因：点名识别失效 → 被静默降级为"全删"
- 旧点名正则依赖 **"XXX图片"（"图片"必须作后缀）** 的写法认名字。而真实文件名里
  "图片"往往在**最前面**（`图片_2026-08-03_….png`）或**中间**（`微信图片_2026-….png`）
  → 正则认不出具体是哪个文件 → `named` 为空 → 直接落到"桌面上有图就全删"的分支。
- **安全底线失守**：点名失败本应"如实说找不到"，却被静默降级成批量删除。

### 修复：点名识别重做 + 堵死静默全删
- **① 扩展名优先（最强点名信号）**：文本里出现带图片扩展名的文件名
  （`.png/.jpg/.jpeg/.gif/.webp/.bmp/.ico`）→ 直接按该文件名精确匹配；
  自动剥掉「删除/帮我/桌面上的」等指令前缀后再匹配（防止把指令词吞进文件名）
- **② 引号 / 书名号**里的名字 → 匹配
- **③ 才轮到 "XXX图片" 模式**（名字里不含"图片"二字的常见说法）
- **④ 安全底线**：以上都没命中 → **如实说"没认出是哪一张"，绝不全删**；
  只有明确泛指（所有 / 全部 / 不用的 / 多余的 / 没用的 / 这些 / 那些 / 垃圾图…）
  才列出全部图片请确认
- **⑤ 找不到目标时**，直接把**桌面图片清单列给用户挑**（只读展示、绝不动手）
- 顺带修意图冲突：「清理桌面垃圾图片」以前会跑去清 TEMP（系统垃圾），
  现在文本明确指向图片时走图片删除；同时图片删除的动词补上「清理」
  （此前只有"删除/清掉/清除"，"清理…图片"会掉进普通聊天）

- 冒烟：新增 `smoke_v0275` **15 项**全绿；全量回归（12 套件）通过

## 0.27.4（2026-09-10）· 真机 4 问：确认窗不弹 / 朗读停不下 / 语音输入报错 / 聊天↔工作关联

> 真机反馈（装 v0.27.3 后）：①让它删文件，说"弹了确认窗但没等到你确认"——实际**根本没弹窗**；
> ②朗读开时右侧按钮状态不清楚，点暂停再点不能续播，切对话报"系统语音不可用"；
> ③语音聊天报错「找不到类型 [System.Speech.AudioFormat.AudioState]」；
> ④希望**聊天和工作关联**：聊天里让它做的工作要能在"那项工作"里看到，工作里也能讨论进度。

### ① 确认窗不弹 —— 根因是代码 bug（NameError）
- **实锤根因**：`_on_ask_confirm` 用了 `QtWidgets.QMessageBox`，而本文件里
  `QtWidgets` **从未导入**（项目惯例是每个用到它的函数内局部 `from qt_compat import QtWidgets`，
  这一处漏了）→ 主线程一执行就 `NameError` → 窗口从未创建，worker 干等 90 秒超时
  → 回复"我弹出了确认窗，但没等到你的确认"（**冤枉用户**）
- 修复：补上局部导入（`_ui_confirm` 主线程分支 + `_on_ask_confirm` 两处）；
  并把同样未导入的 `qt_compat.QtCore.Qt.WindowStaysOnTopHint` 改为已导入的 `Qt.*`
- 加固：确认窗显示前 `adjustSize()` 并**移到屏幕可见处**（父窗口最小化/移出屏幕也能看见）
- 冒烟实证：窗口真创建、真可见、点"确认执行"→ 回传 True

### ② 朗读：暂停/继续对"系统语音"也生效 + 失败原因具体化 + 按钮状态清楚
- **通道统一**：`_sapi_wav_speak` 改为优先用 **MCI 播放 WAV**（`waveaudio`）——
  旧版走 winsound **无法暂停**，只有 edge 通道能暂停；现在两条通道都能暂停/继续/停止
- `_sapi_speak` 入口改为 **WAV+MCI 优先**（可暂停），PowerShell 直读退为兜底
- **失败原因具体化**：新增 `_LAST_ERR`，不再笼统说"PowerShell/语音包缺失"，
  而是"系统语音合成失败 rc=…（多为缺少中文语音包）"这类可执行信息
- **TTS 自检**：新增 `tts.tts_diag()`；"语音自检"现在同时报 **输入 + 朗读** 两条链路的真实状态
  （在线语音能不能用 / 系统有没有中文声线 / 最近一次失败原因 / 该装什么）
- **按钮状态**：朗读键 `🔊 朗读:开`(绿)/`🔇 朗读:关`(灰)；暂停键 `⏸ 暂停`↔`▶ 继续`；
  停止键 `⏹ 停止`；朗读期间每 0.6s 实时刷新（`_refresh_voice_btns` / `_voice_tick`）

### ③ 语音输入报错 —— 类型名写错
- 根因：PowerShell 脚本里用了**不存在的类型** `[System.Speech.AudioFormat.AudioState]`
  （`AudioState` 枚举属 `System.Speech.Recognition`，不在 `AudioFormat` 下）→
  整个识别脚本语法错误，报"找不到类型"
- 修复为 `[System.Speech.Recognition.AudioState]::Speech`

### ④ 聊天 ↔ 工作 关联（新功能）
- 新建 `desktop/worklog.py`：把"它正在干的活"做成**一等实体**（工作任务台账）
  —— 标题 / 类型 / 状态(running|done|failed) / 进度 / 产物路径 / 所属会话 / 讨论记录，
  存 `%APPDATA%/PASMStudio/work_tasks.json`
- 聊天里触发任何重活（开发/PPT/Word/表格/视频/图像/漫剧/脚本/自学）→ **自动开一条任务**，
  结束时写状态、并从回复里提取产物路径
- **工作台**顶部显示任务行（🔄进行中 / ✅已完成 / ❌失败 + 类型 + 标题 + 进度），
  **双击进"工作详情"**
- **工作详情窗**（新）：看状态/进度/产物（双击打开），并可就该工作**就地讨论**修改与进度
  —— 讨论独立记录在该任务的 chat 里，**主聊天内容完全不受影响**
- **桌面小人**：工作时在头像下方显示当前工作标签（`PetAvatar.set_work`）

- 冒烟：新增 `smoke_v0274` **26 项**全绿；全量回归（11 套件）通过

## 0.27.3（2026-09-10）· 真机 7 问连根修：删不掉 / 检测假报 / 语音 / 性格 / 分析无落地 / 搜索词

> 真机反馈（全新安装 0.27.2 后）：删除、查病毒仍然不工作；麦克风无反应；
> 朗读停不下来、换对话还在念；性格只有标签没有味道；分析表格/桌面没有任何动作；
> 浏览器搜索词变成整句话。本版逐条**先复现定位根因再修**，全部有冒烟实证。

### ① 删除：从"找不到"到"真扫出来给你删"
- **泛指空文件夹**：`find_empty_dirs()` 真扫桌面（递归判定"没有任何文件"），
  列清单 → 确认 → 逐个移回收站 → 逐个记账。旧版解析不出名字就回"找不到"
- **文件类型泛化**：旧正则只认图片扩展名，`.txt/.zip/.docx` 全部掉进普通聊天被模型编造；
  现在任意 `\.\w{1,6}` 都算文件
- **图片口语化**：「把桌面上**不用的**图片删掉」旧版把"不用的"当**点名**处理
  （不在泛词白名单里）→ 找不到 → 说"删不掉"；现在泛词判断改"包含"匹配
- 新增执行器 `_sysops_del_paths`（多目标删除，确认后逐项真删逐项记账）

### ② 检测电脑/查病毒：不再假报"取不到"
- **根因实锤**：`_ps_run` 用裸命令名 `powershell` 在子进程里 0.0s 直接失败
  （异常被 `except` 吞掉）→ 改用 `System32\WindowsPowerShell\v1.0\powershell.exe` 完整路径
- **根因实锤 2**：PS 脚本用了 `'RT='+(try{...}catch{...})` —— PowerShell 里
  try/catch 是**语句不能当表达式**，整条命令语法错误 → 输出全空。改为语句式逐字段取值
- 每个字段独立容错（`AntivirusSignatureLastUpdated` 为 null 不再拖垮整条命令）
- 输出解码 UTF-8→GBK 双路尝试；加 `-ExecutionPolicy Bypass`；过滤远程模块加载噪声
- Defender 不可用时退到 WMI `SecurityCenter2`（覆盖第三方杀软）

### ③ 语音：输入给真因，朗读能暂停能停
- **输入**：`listen_once` 返回 `(文本, 诊断)`。旧版把"没装中文识别器/麦克风被占/
  权限没开"全吞成一句"没听清"；现在原样告诉你。识别器改为**列实际已安装的**并优先挑中文，
  挑不到就用第一个可用的并 WARN
- 新增 `asr.diagnose()` + `micdiag` 路由：说「语音自检」一次查清识别器/中文包/麦克风
- **朗读**：新增 ⏸（暂停/继续，MCI pause/resume，保留进度）与 ⏹（停止当前这句）；
  **开新对话 / 切换会话自动掐断**（旧版换对话还在念上一个对话的语音）

### ④ 性格：从"标签"到"话术"
- 新建 `persona_style.py`：每个性格给出 语气 / 口头禅 / 句式 / emoji / 禁忌 / 2~3 句示范
  —— 旧版只给一句"你的性格是 X"，不同性格说出来几乎一样
- **新增两个性格**：`豪爽直率`（大大咧咧，偶尔冒一句"我靠"）、`毒舌损友`（嘴硬心软）
- 市井口语按 `spice` 分级并明确约束：**每 5~8 句最多一句，正式场合与安慰人时一句不许用**
- 语音随性格变（语速/音高 bias 叠加到 tts）

### ⑤ 语言天赋：跟用户学说话
- 新建 `accent.py`：方言特征词表观察 → 计数落盘（带 45 天半衰期）→ 攒够阈值后
  把"这位用户的说法"注入提示词，并把 TTS 换成对应口音
  （**东北→zh-CN-liaoning-XiaobeiNeural**、粤语→zh-HK、台湾→zh-TW、河南/山东各有音）
- 分寸：一句话最多带一两个方言词；说「别学我说话」一键清零；「你听出我什么口音了吗」可查

### ⑥ 分析：不再干瞪眼，给真报告 + 下一步
- 新增 `sysops.scan_dir_report()`：文件数/文件夹数/体积/类型分布/最大与最新文件/
  疑似重复/空文件夹数，全部真扫出来
- 「分析一下桌面上的文件」→ 真扫描 + 4 条可执行下一步（删空文件夹/按类型整理/找大文件/分析表格）
- 「帮我分析这个表格」→ 主动在桌面/下载/文档找最近 `.xlsx/.xls/.csv` 列出来；
  回「1」「第一个」能接上（候选接选路由）

### ⑦ 浏览器搜索词
- 旧正则 `[^，。！？\n]{0,6}?` **禁止跨逗号**，于是「帮我打开浏览器**，**并搜索'诛仙'」
  匹配失败 → 整句话被当搜索词。新写 `_extract_search_query()`：
  ① 引号/书名号内优先；② 取最后一个搜索动词之后；③ 反复剥残留连接词与渠道名
- 实测 8 种写法全对（中文引号/英文引号/书名号/裸动词/百度/网上）

- 冒烟：新增 `smoke_v0273` **57 项**全绿（含串词守卫回归：闲聊提到清理/病毒/删除仍不劫持）
- 全量回归 10 套件通过（mathlab / pet_hobby / v0261 / v0264 / v0270×3 / v0271 / v0272 / v0273）

## 0.27.2（2026-09-09）· 真机误报修复（确认框/删图片）+ 不定性任务泛化

> 真机反馈：v0.27.1 删除桌面文件夹/图片仍失败——两次被说"你点了取消"（实际没看到
> 弹窗，实为 180 秒超时被冤枉）；删图片说"已删除"但图片还在（掉进普通聊天被模型编造）。
> 本版连根修，并把"不定性任务"（浏览器搜索/查病毒/其余杂活）纳入真实执行与自学闭环。

- **🔔 授权确认框重构（跨线程不再吞弹窗）**：
  旧版用模态 `QMessageBox.question`（queued signal 驱动），主线程忙时 worker 干等
  180 秒超时 → 被当成"用户点了取消"。新版：**非模态 + 置顶 + 自动激活**确认窗，
  主线程绝不阻塞；超时（90s）如实区分——台账记"确认窗超时未应答"、
  回复说"没等到你的确认，原样保留"，**绝不再冤枉用户点了取消**；
  超时后自动关闭失效弹窗，避免"幽灵确认"
- **🖼 删图片进真实路由**：旧版只认"文件夹/文件"，"删除图片/照片/截图"掉进普通聊天
  被模型编"已删除"。新版：
  - 泛指（"删掉桌面上不用的图片"）→ **先列出桌面全部图片请确认**，确认后逐张
    移入回收站，逐张记账，结果如实汇报
  - 点名（"删掉 XX 图片"）→ 只删那张；桌上没有 → 如实说找不到，**绝不转成全删**
  - 图片名识别前先剥离"删除/桌面上"等指令词，防误吞；"截图文件夹"不会被当成图片；
    "搜索怎么删除图片"不会误入删除路由
  - 桌面定位改用 `SHGetKnownFolderPath` + OneDrive 重定向候选
    （桌面被 OneDrive 接管的机器不再删错地方）
- **🛡 防幻觉守卫（兜底）**：聊天回复宣称"已删除/已清理"，但执行台账里查不到
  600 秒内的真实成功记录 → **强制改口**："我并没有真的删除或清理任何东西"，
  并引导用户下确切指令；拦截事件本身也入台账
- **🛡 串词守卫（正常聊天绝不劫持）**：回复路由只认"**对我下的命令**"。
  聊天里就算出现"清理/删除/图片/病毒/搜索"等词，只要句子是叙述
  （"我今天清理垃圾累死了"）、转述他人（"我让电脑自动清理"）、疑问探讨
  （"这软件会不会有病毒""删除和格式化有什么区别"）或求推荐
  （"帮我推荐清理垃圾的软件"）→ **一律当普通聊天**，绝不弹窗、绝不动手。
  判据：≤70 字 + 句首祈使词（帮我/请/把/继续…）或句首即干活动词（删除/清理/
  打开浏览器…）或呼名下令（"小U帮我删…"）；台账核验路由也收紧为只认
  "查我干活的账"（"你刚才真的删了吗？怎么验证"仍可查）
- **🌐 不定性任务泛化（回应"让它干别的活"）**：
  - **浏览器搜索成真实动作**：'用浏览器搜索 X'/'在网上查一下 X' → 真的用系统默认
    浏览器打开必应搜索页（关键词自动抽取），开了就是开了、失败说失败原因
  - **电脑安全体检（只读）**：'帮我看看电脑有没有病毒'/'做个安全体检' →
    真实读取 Windows Defender 实时保护/病毒库日期/防火墙状态/最近威胁检出记录
    （PowerShell 只读查询），取不到就如实说取不到，**绝不编"没有病毒"**
  - **cando broker 触发面放宽**：浏览器/搜索/病毒/杀毒/防火墙/系统信息/壁纸/剪贴板等
    不定性任务都能进"能做/学过做/学着做/说不能"判定——其余没接住的活继续走
    "上网自学 → 学到知识写脚本 → 真跑真验证 → 失败自动修"闭环
- 冒烟：`smoke_v0272` 扩到 72 项（诚实话术 5 + 图片路由 7 + 真删图片 6 + 搜索 8
  + 体检 6 + 防幻觉 4 + cando 7 + 桌面路径 3 + **串词守卫 27**）全绿；
  全量回归（10 套件）通过

## 0.27.1（2026-09-09）· 真实系统操作层 + 能力判定 broker + 难度自适应分裂

> 根因修复：之前让小U"清理垃圾/删除文件夹"时它会说"清理完了""已删除"，
> 但什么都没做（工具闭环里只有记忆类工具，模型编造成功话术）。本版让它**真做**，
> 做不到就**说真话**，需要授权就**弹窗问你**。

- **🛠 真实系统操作 `desktop/sysops.py`（新增）**：
  - 删除 = **移入回收站**（Win32 SHFileOperationW，可恢复，绝不永久删除）
  - 安全护栏：Windows/Program Files/盘符根/用户主目录**硬拒**（怎么问都不删）；
    System32 等系统目录内部拒绝代做；非空文件夹删除必须**确认授权**
  - 清理垃圾 = **先扫描只读报告**（各临时目录实际占用）→ 弹窗列明细请确认 →
    只清白名单（用户/系统 TEMP、pip/npm 缓存）里超过 2 天的旧文件，
    正在用的跳过并如实计数；大目录设时间预算，没清完如实说"还有剩余可再清一次"
  - **执行台账**：每个动作记 时间/动作/对象/原因/结果（JSONL），
    聊天里问"你刚才真的删了吗/怎么验证"→ 直接给你看台账
- **🧭 能力判定 broker `desktop/cando.py`（新增，非预设机制）**：
  需求先判定 → **DO**（有真实工具直接做）/ **LEARNED**（自学过相关知识 → 用学到的写脚本真跑）/
  **LEARN**（不会但可学 → 先上网自学再真做，学不会如实交代）/ **NO**（物理世界/资金/违法类
  → 说明真实原因，如"关机需要物理动作""涉及你的钱我不代做"）
  → 自学不再只是"存进知识库"，而是真的拿来用
- **🧬 难度自适应分裂**：简单任务主体直接干；复杂任务（然后/分别/批量/全部…）
  自动拆子步骤逐个执行，每步结果入台账、主体逐个复核；破坏性操作**主体亲自执行**
  且必须用户确认（子智能体无权做危险动作——主体兜底）
- **🛡 防幻觉兜底**：系统提示注入诚实守则——没有真实执行成功，绝不允许说"已完成"
- 桌面端：`_detect_agent` 新增 sysops_del/sysops_clean/sysops_report/cando 路由；
  跨线程安全的授权确认弹窗（worker 线程请求、UI 线程弹出、不点=不动手）
- 冒烟：新增 `smoke_v0271_sysops` 42 项（护栏 7 + 真删 7 + 扫清 5 + 台账 3 + 难度 3
  + 路由 9 + 判定 8）全绿

## 0.27.0（2026-09-09）· 数学脑 mathlab + 三方向全落地（B 多模态 / C 量子策略 / A3 拓扑校验）+ 四大趋势

- **🧮 Phase A 数学脑 `pasm/cognitive/mathlab.py`**（纯 numpy，无重依赖）：
  线性回归（R²/预测）· 描述统计（IQR/3σ 离群）· 相关矩阵 · 线性方程组 · Kahn 拓扑排序（环检测）
  · Dijkstra 最短路 · 连通分量 · 马尔可夫稳态 · `analyze_table` 一句话中文结论；
  聊天里"帮我分析这个表格/数据"自动走 `tableana` 路线（openpyxl 读表 → mathlab 真算），创作类请求不误触
- **🔀 趋势① 多模型路由器**：`_brain(prefer=...)` + `_route_endpoint()` —— 闲聊走本地 Ollama（快）、
  干活走 DeepSeek（强）、评审走本地交叉校验；本地不可用自动回落云端
- **🔁 趋势② 团队步骤自检重试闭环**：单步异常/空壳/过短产出 → 自动第 2 次重试 + 质量自检，状态如实回写
- **🏗 趋势③ 经验沉淀**：项目跑完自动沉淀"流程模板"（可保存/列出/一键套用），下次同类项目直接复用
- **🧠 趋势④ 项目级跨会话记忆**：团队执行自动注入本项目档案（成员/流程/历次产出），跨会话不丢上下文
- **👁 Phase B 多模态感知 `pasm/cognitive/percept.py`**：
  · **看**：图片拖进聊天框 / 剪贴板直贴截图 → 本机 Ollama 视觉模型（llava/qwen-vl/minicpm-v/gemma3…）
    本地看图，不传云端零成本；描述注入下一条消息上下文，"你"气泡保持干净；
    没装视觉模型给一行 `ollama pull` 指引不弹窗
  · **听**：语音转写降级链 faster-whisper → Vosk → 可读安装指引（供语音输入链路调用）
- **⚛ Phase C 量子策略决策层 `pasm/cognitive/quantum.py`**：
  候选策略 = 叠加态（振幅 |α|²=被选概率）；玻尔兹曼退火（早期高温多探索、随经验收敛到最优路线）；
  同族干涉（direct 干成过，quick 这类同族新面孔也沾光）；情绪 PAD 作外场偏置（急躁→直给、从容→先想）；
  成败如实回灌振幅，决策→执行→反馈完整闭环；数据存 DATA_DIR/quantum/ 跨会话演化。
  桌面接入：复杂任务模糊区（规则拆不开时）由叠加态测量决定「问模型拆一步」还是「直接单步干」
- **🕸 A3 团队流程拓扑校验**：步骤支持 `deps` 依赖（0-based 索引）；mathlab 拓扑排序校验
  环依赖/越界/自环/缺角色，校验不过不准跑不准存；执行按拓扑序（无 deps 即线性，兼容旧流程）
- **🗺 三方向路线图**：`docs/roadmap_multimodal_quantum_math.md`（A/B/C 全部落地）
- 冒烟：mathlab 29 + phaseA 22 + trends 29 + **BC 拓扑 29** + v0264 18 + v0262 22 + v0261 26 + v0260 39 全绿

## 0.26.4（2026-09-08）· @引用升级 WorkBuddy 式：候选弹层 + 上方引用条

- **📎 @引用交互重做**（你反馈"@引用还是没有实现/想看到像 WorkBuddy 那样的引用条目"）：
  - 输入框打 `@` → 弹出可引用文件快选（与 `/` 同款弹层，↑↓+Enter/Esc 一致），
    分组：最近产物 / 资料库词条 / 工作区·桌面·文档·下载 里的文本文件，继续打字即时过滤
  - 选中后 → 消息里落成 `@路径` 引用写法，**输入框正上方出现 📎引用条 chip**（显示文件名，可点 ✕ 移除，移除会同步清掉消息里的引用写法）
  - 支持 `@“含空格路径”` 引号写法（此前含空格路径会被截断找不到）
  - 引用正文只进模型上下文，"你"气泡保持干净，不再整段刷文件内容
- **🐛 "@小U 被瞎聊"根因修复**：旧版把消息里 `@小U` 这类找不到的引用**原样发给模型**，
  模型就当是打招呼回"哎呀想起你了"；现在纯引用消息找不到即明确提示并剥除标记，
  不再产生答非所问；长文本/代码里的 @（如 `@app.route`）不会误伤
- 冒烟 v0264 新增 18 项全过；v0262(22)/v0261(26)/v0260(39)/v0250(33+33) 回归绿

## 0.26.3（2026-09-08）· 能力提示去串符 / @引用补齐 / 团队升级真公司项目组

- **✂️ 能力提示不再一坨串符**：你反馈"我不是说了不要再出这些提示吗"→ 根因=「能给出教程吗/能教我…吗/能发个例子吗」这类**要内容**的句子被误判成"问能力"，回一长串"小U能帮你…✍️📊📽…"。修复：内容请求一律正常聊天/干活；真正问"你会做什么"也只回两三行 + 一句开工引导，快捷入口串整段移除
- **📎 @引用补齐**：支持 @完整路径 / @文件名（自动在 工作区/桌面/文档/下载 按名搜索兜底）/ @资料词条；找不到不再静默——明确提示怎么写；/help 更新用法
- **🏢 团队升级真公司项目组**：
  · 新建项目 = **空白项目**：成员与流程都从零亲手组建（不再自动塞默认流程，也不需要的话就不加）
  · 每个项目**独立编制**：左栏显示本项目成员（从人才库 🧑‍💼 编入 / 🌐 招聘 / 🎓 技能库 / ＋建岗），成员可随时编入或移出本项目，人才库档案与评分保留
  · 切换不同项目 → 各自呈现 成员 / 流程 / **任务状态**（✅完成 ⚠失败 ○待跑）/ **历次执行效果档案**（目标、最近产出、历次记录），做好随时回看
  · 跑完自动把每步状态与产出回写进项目、记入历次执行
  · 聊天"让团队做 X"仍可用：自动组建内置班底开工
- 冒烟：v0250/v0230 断言更新至新模型；v0261(26)/v0262(22)/v0260(39)/v0250(33+33)/v0240(57+15)/v0231(26)/v022x 全回归绿

## 0.26.2（2026-09-08）· 干活报错修复 / PASM 记忆情感真正调制 LLM / 表格文档同一文件精准续改

- **🐛 修复干活报错**：`str.join() takes exactly one argument (8 given)`——能力问答的 8 个快捷入口锚点漏包了列表，一句话中括号修复（全仓库扫描确认仅此一处真多参 join）
- **❤️ PASM 记忆与情感真正可感知**：
  · 情绪词表扩到 40+（含"太强了/真行/想你了/烦死/破防/没反应/卡死/闪退"等口语），抱怨、报错、等太久都会拉动心情，不再只认"谢谢/讨厌"几个词；
  · 情绪反馈修正：每条消息的情绪冲击一次性给足（此前被 6 步随机游走稀释、反复累加把情绪撞到顶格，感知是"心情永远一样"）；中性消息不再一路向好（0.02 缓坡）；
  · 心情翻译成**「此刻口吻」**写进每条回复的提示词：开心→轻快俏皮（~哈耶）、低落→放轻放短先接情绪、气氛急→直接给重点；文字能感受到它的状态；
  · **干活也带记忆**：做表格/方案/PPT/开发时，会把与你相关的记忆与偏好自动带给模型（沿用你的风格与历史约定，不再每次像第一次见面）
- **📄 表格/文档/PPT 同一文件精准续改（修"反复改却不停新建"）**：根因=旧逻辑每轮都带时间戳生成新文件、且模型拿不到上一版内容只能重写。现在同会话说"表格再加两行/文档补一节/PPT 换配色"→ 命中产物续改路由 → 把**上一版完整内容**喂回模型（保留主题与结构，只按新要求增删改）→ **覆盖写回同一个文件**；明确说"另存/新做一份/换名"才另开。产物记忆随会话存盘（切会话/重启后回来仍记得上次那份）
- 冒烟：v0262 新增 18 项全过；v0261(25)/v0260(39)/v0250(33+29)/v0240(57+15)/v0231(26)/v0230(11) 全回归绿

## 0.26.1（2026-09-08）· 五连真机修复：健康卡压缩 / 团队页卡片化 / 技能三层落地 / 打开软件真启动 / 任务不丢回复

- **🩺 健康状态不再占屏**：自检自愈收成底部一条小卡（固定小高度、内嵌细列表），自动动态区回归主位
- **👥 团队页卡片化重排**：项目条 → 左「成员」右「流程」两张卡 → 底部开工区；成员行直接显示 ★评分与职责，单击即员工卡（含 ⭐ 手动打分），右键菜单辞退/详情；流程卡拖拽排序不变
- **🎓 技能三层真实落地**：新增内置 🎯专家2位（内容创作总导演、数据分析师）+ 🔌连接器3个（联网检索/本地文件与程序/办公文档生成），添加向导第4步可选手层类型；技能页按类型筛选即刻可见三型差异
- **📂 打开软件真实启动（修假成功）**：根因=「小U，帮我打开××」带称谓被识别成普通聊天，模型文本幻觉"已打开"且从未真执行。修复：send 统一剥称谓前缀 + 应用名更长容忍与尾巴词剥除（听歌/放歌…）+ 品牌应用真实安装路径探测（网易云/QQ音乐/钉钉/腾讯会议/微信…装哪盘都能找到）+ 启动后 tasklist 真验证，未见进程如实告知——绝不口嗨
- **⏳ 任务并发加固（修"只有提问没有回复"）**：真机实证=开发任务做完了项目文件，但回复未写进会话。修复三件套：重活开工前聊天区给「⏳ 正在干活…结果不会丢」提示；用户切走又回到原会话时，完成的回复当场显示（旧逻辑一律收文件、界面不刷新，看着像没干）；turn 收尾全程兜底，任何一步异常都保底把回复写回原会话文件并记日志
- 冒烟：v0261 新增 25 项（UI 离屏 + turn 生命周期 + 品牌探测）全过；v0260(39)/v0250(33+29)/v0240(57+15)/v0231(26)/v0230(11) 全回归绿

## 0.26.0（2026-09-08）· 项目级台账重调 / 真实文件开发多语言 / @引用与/指令

- **📒 项目级台账（workctx，引擎层）**：文案/表格/PPT/网站/软件开发，每个项目一本台账——
  需求史 + 每轮改动清单（改了哪些文件、核心动作）+ 冒烟结果 + 你强调过要遵守的规则；
  续写/修改时把台账摘要自动拼进提示词（"在已有成果上精准修改，只动涉及部分"），
  根治"改着改着牛头不对马嘴、整段推倒重写"的问题
- **🛠 真实文件开发（修复"开发不落目标目录"）**：
  · `/开发 需求` 或"用php/vue/java/go/c#/c++ 开发…"→ 先让你**选目标目录**，
    代码真实写入 <目录>/<项目名>/，不再掉进应用数据目录
  · 内置 8 种语言脚手架（python/vue/js/php/java/go/c#/cpp），LLM 逐文件生成完整内容
  · 按语言跑**静态冒烟**（python -m py_compile / node --check / php -l，缺工具明确跳过）
  · 每轮产出自动记台账并打开目录；说"改这个项目/继续优化…"→ 精准续改（只动涉及文件）
- **📎 @引用**：聊天框写 `@文件路径` 或 `@资料词条`，自动把文件/资料内容读进上下文再干活
- **/️⃣ 指令扩展**：`/开发 需求`、`/技能 技能名 需求`（点名调用技能）、`/项目`（看台账）
- 引擎新增 pasm/cognitive/{workctx,coder}.py（桌面端兼容门面，同频共用）
- 冒烟：v0260 台账+多语言生成 39 项全过；v0250 引擎33+UI29、v0240 57+UI15 等回归绿

## 0.25.0（2026-09-08）· 公司式项目组 / 聊天提速 / 自愈纯后台 / 技能三层化 / 安装器根除错误5

- **👥 团队升级为公司式项目组**：
  · **项目空间**：新建/切换/删除项目，目标、流程、历次执行产物全部留存（data/team/projects/）
  · **成员=智能体员工卡**：点开看职责/接收/产出/红线/擅长，附评分体系（★分数+次数+最近评语）
  · **流程拖拽编排**：项目流程支持拖动排序、双击改任务、一键把选中成员加入流程
  · **🌐 网上招聘**：输入岗位关键词，自动抓取网上资料并提炼成员工卡入职
  · **🎓 技能库选才**：把技能一键转化为对应岗位的员工
  · **评分换人闭环**：每步执行自动记分（成功+ / 打回−），用户可手动打星——分低就换更好的成员
- **⚡ 聊天响应提速**：双脑协同（起草→评审→修正=2~3 次模型调用）此前每条消息都跑，
  真机反馈变慢；现改为**只在写代码/写作等重活时启用**，闲聊问答单次直出，首字延迟大幅回落
- **🩺 自检自愈改纯后台**：全程静默自动运行（启动巡检崩溃栈+落状态档），不打扰不占资源；
  「⚡ 自动化」页去掉模式选择，只留一个「🩺 查看健康状态」按钮
- **🎓 技能三层专业化**（参考专家-技能-连接器体系）：技能库支持 🎯专家/🎓技能/🔌连接器
  类型标注与筛选；团队选才与技能类型打通
- **🔧 安装器釜底抽薪（根除 MoveFile 错误 5）**：升级安装不再"替换旧文件"（该路径才会
  触发错误 5），改为杀进程→清空程序目录（保留便携 data）→全新复制，最多 5 轮；
  仍失败弹窗给出指引（退出程序/关杀软实时防护/重启/更换安装路径）
- 冒烟：v0250 引擎 33 项 + UI 离屏 29 项全过；v0.19.0→v0.24.0 全系回归绿

## 0.24.3（2026-09-08）· 安装器釜底抽薪：先清空再全新复制，根除错误 5

- **🔧 升级安装彻底绕开「替换旧文件」路径**：0.24.1/0.24.2 的探锁方案在真机仍有残余
  占用场景（杀软实时防护、句柄释放时序等）；本版改为安装前 **清空程序目录
  （保留便携 data 目录）→ 全新复制**——不存在被替换对象，MoveFile 错误 5 的
  触发路径被整体消除
- 杀进程 → 等 2s → 清空，最多 5 轮；仍失败则弹窗给出三步指引
  （退出程序 / 关闭杀软实时防护 / 重启后再装）
- 应用功能随 v0.24.2（无变化）

## 0.24.2（2026-09-08）· MoveFile 错误 5 彻底修复（探锁假阴性）

- **🔧 v0.24.1 探锁方法有假阴性，真机升级仍报错 5——本次彻底修复**：
  · 根因：v0.24.1 用「重命名探测」判断文件是否被占用，但 Windows 允许重命名
    **运行中的 exe**，只禁止重命名**已加载的 DLL**——旧进程还活着时探锁误报「无锁」，
    放行后 Inno 替换 `_internal` 下被占用的运行库 → 依然是 MoveFile 错误 5
  · 修复：改用 kernel32 `CreateFileW` **独占打开**探测 `PASMStudio.exe` +
    `_internal\python313.dll` + `_internal\Qt6Core.dll`，任一被占用即拦截；
    杀进程 → 等句柄真正释放（最长 15s）→ 仍占用则弹窗给出三步指引
    （托盘退出 / 任务管理器结束进程 / 以管理员身份运行安装器）
- 其余随 v0.24.1（安装器先杀进程再装 / PASM 核心引擎同频 0.5.0 / v0.24.0 全部能力）

## 0.24.1（2026-09-08）· 安装器 MoveFile 错误 5 修复 / PASM 核心引擎同频 0.5.0

- **🔧 升级安装 "MoveFile 失败，错误代码 5" 根因修复**：
  · 根因：旧版 PASM Studio 是托盘常驻程序，安装器原先只「发出 taskkill」就继续装——
    进程尚未真正退出、exe/DLL 句柄未释放，Inno 重命名替换目标文件时被系统拒绝（错误 5）
  · 修复：安装前改为 **「杀进程 → 重命名探锁 → 未释放自动重试（最长 15s）→ 仍占用则弹窗
    明确提示先退出再装」**；`CloseApplications` 升级为 force
- **🧬 PASM 核心引擎与 Studio 同频（引擎 0.4.0 → 0.5.0）**：
  · v0.24 两大能力的**真身移入引擎执行皮层**：`pasm/cognitive/agent_team.py`（多智能体
    技能团队）与 `pasm/cognitive/selfheal.py`（自检·自定位·自修复）
  · 桌面端 `desktop/agent_team.py`、`desktop/selfheal.py` 变为兼容门面（re-export）——
    Studio 与引擎共用同一实现，此后增改只在引擎层做，天然不再分叉
  · 引擎数据目录与记忆层 memory_layers 同一约定（PASM_STUDIO_DIR → 桌面 DATA_DIR →
    %APPDATA%/PASMStudio），团队流程产物与故障案例库落位与桌面完全一致
- 冒烟：v0.24.0 引擎 57 项 + UI 离屏 16 项经门面回归全过（验证同频无感）

## 0.24.0（2026-09-08）· 多智能体技能团队 / 系统自检自愈 / 方舟404修复 / 主题加固

- **👥 多智能体技能团队（方向一 P1-P3 落地）**：
  · 新增「👥 团队」页：内置 6 张种子角色卡（🔒项目经理/策划/调度/开发/审查/调试），
    每张卡写清 职责·接收·产出·质量红线·擅长——用户可添加自己的"员工卡"（同名覆盖、内置不可删）
  · 流程编排：内置「交付小队」（策划→开发→审查→调试返工→调度汇总）等种子流程，
    可自建流程（引用不存在角色会被拒）
  · 团队执行器：按流程逐步调用 LLM 角色；**审查步不过 → 自动带意见返工开发（最多2轮）；
    单步失败自动重试**；全程步骤可视化，汇总结论 + 分角色产出
  · 对话触发：聊天里说「让我的团队做 X」→ 自动选流程开工，跳团队页看过程
- **🩺 系统自检 · 自定位 · 自修复（方向二 C1-C3 落地，安全阀全程把守）**：
  · C1 哨兵：启动后静默巡检 crash.log/pasm.log/voice.log（Traceback 连栈提取，
    同症状按 文件+首行 去重计数）→ 记入 data/selfheal/cases.json 故障案例库
  · C2 自动定位：症状 + 代码树索引 → LLM 给文件+行号级根因 → 落盘 bug_report_*.md
  · C3 建议修复三档（自动化页可选）：**off** 只记录 / **report** 出诊断+修改建议（绝不改码）/
    **apply** 本地源码环境自动修——补丁前自动备份 .bak → 应用 → 跑冒烟回归 → 不过自动回滚；
    frozen 安装版强制降级 report，绝不静默改代码、绝不自动发布
- **🔥 火山方舟视频 404 根因修复**：默认模型 doubao-seedance-1-0-lite-i2v-250428 已在方舟下线
  （调用即 404 ModelNotFound）→ 默认迁移到在售 doubao-seedance-1-5-pro-251215；
  设置页模型改下拉（可手填 ep- 推理接入点），存了已下线旧值的自动迁移；HTTP 错误信息可读化
- **🎨 聊天主题再加固**：亮色 Palette 之上补全局 QSS 兜底，聊天框/会话列表/内心栏显式写死深色文字，
  深色系统下任何继承链都不会再把文字变隐形
- 冒烟：v0.24.0 引擎层 57 项 + UI 离屏实例化 16 项全过；
  v0.23.1(26)/0.23.0(11)/0.22.4(10)/0.22.3(6)/0.22.2(10)/0.22.1(15)/0.22.0(44)/0.21.0(18)/0.20.0(29)/0.19.0(42) 回归全过

## 0.23.0（2026-09-08）· DeepSeek式闲聊门 / 排队并发 / 亮色主题 / PASM×LLM真协同

- **🗣 闲聊绝不再蹦“干活入口”（彻底去串符）**：
  · 观点/比喻/求知句（觉不觉得…像系统/像代码、比喻、为什么、怎么、什么、介绍一下…）一律走正常聊天；
    只有「你会写代码吗/能不能做个网站」这类**明确问能力**的短句才答能力总览
  · 能力入口锚点从裸 HTML 改为 Markdown 链接——渲染成可点按钮，聊天里不再出现一坨源码“串符”
- **⚡ 并发与排队**：同栏正在想时再发消息 → 自动排队并提示，上一条完成自动接续执行；
  切到别的会话不杀旧任务（结果收进原会话）；本地模型并发由 1 放开到 2（Ollama 只载一份模型，内部排队）
- **🎨 主题修复**：应用统一强制“亮色 Fusion + 浅色 Palette”——朋友系统是深色模式时，
  聊天框/会话列表不再黑底深字看不见
- **🧠 PASM×LLM 真互补交叉作业（非伪证）**：重活每步 = LLM 起草 → PASM 规则校验 + 回放同类历史教训
  → LLM 当挑剔评审给具体意见 → LLM 复审修正 → PASM 采纳并把新教训写回程序记忆（下次开局可见）——
  互相在改对方的下一次；回复尾部带「PASM×LLM 真协同：起草→评审→修正→采纳✓」可见标记
- 📄 新增 `docs/roadmap_agents_and_selfheal.md`：多智能体技能团队（角色卡/流程编排/团队执行器/沉淀）
  与 自检·自修·自升级（哨兵→自动定位→自动修复→演练发布）两方向的分级方案
- 冒烟：v0.23.0 新增 11 项全过；v0.22.4(10)/0.22.3(6)/0.22.2(10)/0.22.1(15)/0.22.0(44)/0.21.0(18) 回归全过

## 0.22.4（2026-09-07）· 语音延时修复 + ComfyUI 随 PASM 托管

- **🔊 朗读/点小人语音延时修复**：edge-tts 原为“整段合成完才开播”——短句首音 1~2s，
  长回复先静默数秒（voice.log 实证 106s 长句先等约 6s+ 才出声）。改为**分句块边合成边播**：
  · 文本按句切成 ≤90 字句块，第一块立即合成出声（首音 ~0.5s）
  · “生产-消费”双线程：播第 N 块的同时后台预合成第 N+1 块 → 块间无缝
  · 停止逻辑不变：每块播放走轮询，点「朗读:关」≤0.2s 掐断并清空队列
- **🤖 ComfyUI 随 PASM 托管**：安装包不含 ComfyUI（本体+模型数十 GB，需独立安装），但可托管本机已装的它：
  · 设置 → 视频引擎 → 勾选「随 PASM 启动/退出 ComfyUI」
  · PASM 启动时若选了 ComfyUI 引擎且服务没跑 → 自动在后台拉起（无黑窗）；PASM 退出时自动关闭
  · 向导新增「▶ 启动（自动找）/ 🔎 手动定位 main.py / ⏹ 停止」——自动扫桌面/下载/盘根目录常见位置
- 冒烟：v0.22.4 新增 10 项（切块/中途停/托管探活与启动报错/源码接线）全过；回归全过

## 0.22.3（2026-09-07）· 修复 ComfyUI 检测误报

- **🔧 ComfyUI 检测修复**：向导「① 检测」误把 `timeout` 传给了 `urllib.request.Request`（该参数不受支持）——
  导致无论 ComfyUI 是否在运行都报「连不上」并甩 TypeError。改为由 `urlopen` 统一传超时：
  · 服务在跑 → 正常显示 ComfyUI 版本、已装图生视频节点与模型文件
  · 服务没启动 → 给清晰的「请先启动 ComfyUI」提示
- 验证：桩服务（本地起真 HTTP 服务）连通/拒连两条路径 + 源码无 Request(timeout) 模式，冒烟 6/6

## 0.22.2（2026-09-07）· ComfyUI 一键配置向导

- **🧰 ComfyUI 一键配置向导（本版重点）**：设置 → 视频引擎新增向导按钮——
  普通用户不再需要手动搭 Wan2.2/LTX 工作流：
  · **① 检测**：一键确认 ComfyUI 在运行（版本）并列出已装图生视频相关节点与模型文件；
    缺模型/节点时给出明确下载与升级指引
  · **② 官方示例**：内置官方 Wan2.2 5B I2V 与 LTX-Video I2V 两套工作流（随安装包携带），
    点一下另存到电脑，拖进 ComfyUI 网页即可运行试片
  · **③ 导入**：把 ComfyUI「Workflow → Save (API Format)」导出的 json 选进来，
    自动静态自检（LoadImage/图生视频节点/正向提示词/输出节点）后落到引擎路径
  · **④ 自检**：随时校验当前 workflow，出问题直接指到具体缺什么
- **🎬 引擎识别补齐**：识别遗漏的 Wan22ImageToVideoLatent / LTXVImgToVideo /
  HunyuanVideoImg2V 图生视频节点与 SaveWEBM 输出节点——官方 Wan2.2 示例也能被正确替换首帧与动作提示词
- 成片前“workflow 缺失”提示改为指向一键配置向导（不再贴手工步骤）
- 冒烟：v0.22.2 新增 10 项全过；v0.22.1（15）/ v0.22.0（44）/ v0.21.0（18）回归全过

## 0.22.1（2026-09-07）· 真机五修（回看 0.22.0 实测反馈）

- **🗣 "讲心理学"不再答非所问**：① `_cap_question` 加"讲内容"拦截——"能和我讲讲
  心理学/给我介绍下xx"这类求**内容讲解**的问句，不再被误判成"问上网自学能力"而甩
  能力清单；同时去掉裸"学/查/知识/了解"等过宽触发词 ② 学以致用改**双模式**：
  学科知识类（心理学/编程…）只注入要点+“用自己的话给朋友讲、≤300 字、别照抄”指令，
  表演类（笑话/段子/谜语/古诗…）才注入原文整段照讲——修"把资料库原文当答案甩"
- **🖥 ffmpeg/脚本全程无弹窗**：creators 的 ffmpeg/ffprobe 探测与合成、agent_tools
  的运行脚本统一加 `CREATE_NO_WINDOW`（合成视频时不再弹出黑窗）
- **🔊 朗读即停补刀**：edge mp3 播放改“异步起播 + 100ms 轮询 _STOP/播放状态”；
  真机证据显示旧的 `play … wait` + 跨线程 stop 在长句上不生效（关了还在读 16s），
  现在点「朗读:关」≤200ms 必断
- **🎬 图生视频引擎预检**：成片前检查引擎是否真就绪——ComfyUI 没放
  comfy_i2v_workflow.json / 方舟没填 Key / 服务没启动，都会在开始时明确提示
  原因与解决步骤（并给出 workflow 导出三步），不再一路静默降级成"静态图+配音"
  让人以为引擎不行；设置页选引擎后立即显示就绪状态
- **📚 资料库排版**：原文超长行按标点/宽度折行（pretty_text）、阅读器行距 1.8 +
  断字换行、学习结果弹窗改富文本分条——阅读器/书架/聊天引用不再挤成一团
- 冒烟：v0.22.1 新增 15 项全过 + v0.22.0（44）/ v0.21.0（18）回归全过

## 0.22.0（2026-09-07）· 认知提速 + 规划/验证/模板/few-shot + 学以致用 + 真知识库

- **⚡ 回复提速（修复"调优后变慢"）**：
  · **真流式**：普通对话首 token 即上屏（真打字机），不再是"全文生成完才假打字"，
    感知响应速度 = 首 token 时间；本地与云端统一走流式
  · **智能总线**：工具轮（recall/verify/mood）只在"记得/上次/偏好"这类需要回忆的
    话上触发，普通对话单趟直出——修复"每条消息都先工具后回答=串行两趟生成"的
    最大变慢根因；工具轮上限也从 4 轮收到 2 轮
- **📋 任务规划器（planner.py）**：复杂指令自动分解为步骤——规则优先（然后/接着/；
  多句多动作零成本切分），切不开才用一次轻量 LLM 分解（JSON 步骤表，解析失败兜底）；
  简单任务/创作管线绝不规划，直通不折腾
- **✅ 输出验证器（validator.py）**：Python 走 ast 真语法树、JS/Java/Go 括号配平、
  JSON 解析、占位符/拒绝话术/代码块未闭合检测；不通过带具体问题重生成一次，
  仍不过如实降级；写码管线已接入
- **📝 结构化提示词模板（prompts.py）**：按任务类型（总结/推理/生成/代码/抽取）
  下发"目标+步骤+输出格式"规格，闲聊绝不套壳；让不同底层 LLM 在 PASM 框架下
  表现趋于一致
- **🧠 记忆 few-shot**：从程序/情景记忆检索相似成功案例（"以前要做什么→做法→成功"）
  注入提示，引导模型模仿已验证做法；无关不硬塞
- **🔄 多轮交互**：复杂任务先出计划待确认（回「开始」执行/「改」作废/其他输入自然
  失效不卡人），确认后逐步干、每步完成即刷新界面（渐进可见）；每步都吃
  模板+验证器+few-shot 的框架增益
- **🎭 学以致用**：检测"讲个笑话/教我xx/举例"这类点本事的请求，命中自学条目时把
  学到的**原文整段**带进提示词让模型直接用出来——修复"自学了笑话大全却讲不出
  笑话"；知识分类新增"娱乐"（笑话/段子/谜语/古诗…）
- **📚 资料库升级为真知识库（Obsidian 式）**：新增 `/查 关键词` 聊天检索命令
  （条目+要点+原文摘录+关联一步到位）；阅读器关联条目可点击跳转（双链导航）；
  全文加权检索（标题>标签>要点>原文）
- 冒烟：新增 44 项全过；v0.21.0（18）/ v0.20.0（29）/ v0.19.0（42）回归全过

## 0.21.0（2026-09-07）· 图生视频双引擎：即梦 Seedance（云）+ 本地 ComfyUI + 朗读即停

- **🎬 图生视频双引擎（本版重点）**：漫剧/图文短片从「静态图运镜」升级为
  **真视频**——分镜图作为首帧 + 每镜动作提示词，交给图生视频模型生成
  会动的片段（人物会动、镜头会推、环境会流动），再由 ffmpeg 组装成片。
  设置 → 视频引擎 三选一：
  · **即梦 Seedance**（火山方舟 API，即梦同源模型）：填 API Key 即用，
    一镜 5s 约几毛到几元，720p，失败单镜自动降级
  · **本地 ComfyUI**（开源模型 Wan2.2 / LTX，零 API 费用）：本机运行
    ComfyUI 后导入 API workflow 即用
  · **不启用**：退回 Ken Burns 静态运镜（零成本兜底）
- **🎞 成片管线升级**：render_mp4 支持真视频片段输入——片段原声与旁白
  混音、统一规格转场拼接、字幕烧录；音频转场（acrossfade）同步生效
- **🔊 朗读即停**：点「朗读:关」/  立刻静音——三通道中断
  （MCI stop / SND_PURGE / 终止朗读进程）+ 待读队列作废，不回落不误报
- 冒烟：新增 18 项全过（含真视频片段组装实测）；v0.20.0 / v0.19.0 回归通过

## 0.20.0（2026-09-07）· 视频真成片：ffmpeg 自动集成 / 图生视频专业管线 / 同图多轮微调 / 分步指令直通

- **🎞 视频不再是"图文展示"，是真视频**：内置 ffmpeg 自动集成——首次合成时自动
  下载静态构建（约 90MB，一次性，落点 %LOCALAPPDATA%\PASMStudioin，无需管理员
  权限，失败自动换源重试），下载进度实时显示。
- **🎬 图生视频专业管线**：漫剧/图文短片成片全面升级——每镜 Ken Burns 运镜
  （相邻镜交替推近/横移）、镜间 xfade 叠化转场（fade/circleopen/smoothleft 交替）、
  旁白字幕烧录（微软雅黑描边样式）、成片首尾淡入淡出、音画逐镜对齐，
  输出真正可发布的 MP4。
- **🖼 同图多轮微调强化**：图片会话里直接说"背景换成黄昏""再亮一点""加只猫"
  等微调话术即在上图基础上修改（轻改保构图 0.35 / 大改 0.62）；强编辑动词
  （改成/换成/去掉）压过"画一张"量词误判；全新主题仍自动分新图互不覆盖。
- **📖 漫剧分步指令直通**：企划进行中单说「出图」「配音」「合成」「重画第2镜」
  「第2镜改成夜街」即直达对应步骤执行，不再掉进普通聊天——修复真机
  "录入出图并没有执行"。
- **📊 评估报告入库**：evalkit 标准化评估报告（JSON+MD）随仓库发布，
  便于社区复现与对比。
- 冒烟：新增 29 项（编辑判定/粘性路由/ffmpeg/真视频成片实测），
  并跑 v0.19.0 / v0.18.0 回归。

## 0.19.0（2026-09-07）· 认知升级四连：遗忘曲线记忆 / 大五人格 / 强化学习习惯 / 标准化评估

> 本轮不修 bug，纯能力升级——把 PASM 往"会忘、有性格、越用越聪明、可量化"推进。

- **🧠 记忆系统强化（模拟人类遗忘）**：每条记忆带"巩固强度"与"最近复述"元数据，
  检索得分 = 语义相似 × 艾宾浩斯保持率 R=exp(-Δt/(τ·√强度))——越久没想起越难想起，
  常被想起/睡眠巩固过的记忆衰减越慢；容量满时"最弱先忘"（保持率×强度×显著性最低），
  不再是机械 LRU。桌面侧同步升级：新增 `memvec` 字符 n-gram 哈希向量（192 维纯
  Python 零依赖），**换个说法也能想起**；召回即巩固（hits↑ 强度↑）；退出前/每 8 轮
  自动巩固——相近情景沉淀为「长期印象」（semantic.json），老记忆永不彻底消失（保持率下限 18%）
- **🎭 情绪与性格参数化**：情绪升级为完整 PAD 三维（愉悦/唤醒/控制感），动态增益
  全部参数化，且基线随人格走；性格从 3 特质升级为**大五人格 OCEAN**，支持**用户
  反馈在线调参**——你夸它 E↑N↓、骂它 N↑C↑，带限幅与向先天基线回归，不会一句话
  把性格扭歪；旧接口（openness/caution/sociability）与旧存档完全兼容
- **📈 学习机制升级（程序记忆 × RL）**：习惯不再"一固化就死"——每个习惯带 Q 值，
  检索时 UCB1 平衡"用最优习惯"与"探索次优习惯"；使用结果实时回填 Q 值，连续失败
  拉低 Q、破地板自动拆除（忘掉坏习惯）。新增 `pasm/sft.py` SFT 样本采集器：
  成功任务与被点赞的对话自动沉淀为微调语料（sft_samples.jsonl），支持 alpaca/
  chatml 导出——为微调小模型积累数据资产
- **📊 标准化评估框架 `pasm/evalkit.py`**：四类指标——记忆保持率（注入事实→延迟→
  recall@k）、检索精度（MRR/NDCG@k，量化"少注入无关信息"）、个性化程度（大五漂移+
  PAD 响应差）、任务成功率（GridWorld+习惯 Q）；CLI 一键运行
  `python -m pasm.evalkit --quick`，输出 JSON+Markdown 双报告，便于学术对比
- 验证：新增冒烟 42 项全过（含旧存档迁移/限幅/坏习惯拆除/向量召回）+ v0.18.0
  48 项回归全过；evalkit 快速档全链路跑通
- 备注：本轮未打包安装器——建议真机验证记忆与性格表现后随下个版本一起发布

## 0.18.1（2026-09-07）· 本地模型不再假死：冷加载可等待、出字不被掐、停止仍即时

> 真机（v0.18.0）：与小U聊天，本地模型"思考"数分钟毫无回应。实测定位：
> Ollama 冷加载 qwen2.5:7b 需 76~81 秒，而 v0.17.5 的 25 秒心跳读超时在"加载中"
> 就掐断请求；OpenAI SDK 又对超时整单重发（每 25s 一次，Ollama 中断重来），
> 永远凑不齐一个完整窗口 → 12:10:32 `reply failed`，且失败前界面一直"思考中"。

- **⏱ 本地预算放宽**：本地端点（Ollama/127.0.0.1/11434）按任务给总预算下限
  （聊天 600s、工具 480s、预热 300s…）；云端维持 v0.17.5 收紧不变
- **🌊 本地流式接收**：出字后每个 chunk 到达都重置读计时——长回答/慢生成不再被
  掐断重发；点「⏹ 停止」仍关流真中断（tools 轮本地自动退回非流式）
- **🔥 预热真正可用**：启动预热超时 60s→本地 300s，冷启动预热能实际把模型装进
  内存，首条消息不用再扛冷加载
- **🚫 关闭 SDK 静默重试**：`max_retries=0`，消灭 25 秒一次的"Retrying request"
  重试风暴与翻倍等待（重试统一由网关管理）
- 验证：网关单测 12 项 + 真实冷加载端到端（76.1s 出回复）+ v0.17.3/0.17.4/0.17.5/
  小人回归全过

## 0.18.0（2026-09-07）· 边界更懂事、创作更专业：音量不越权 / 聊天不串岗 / 工作台见产出 / 知识进创作 / 漫剧分步

> 真机五条反馈：音量被自动改、聊天老弹"要不要开工"、产出不在工作空间、自学知识没用于干活、
> 图片改不动/漫剧太粗糙。本轮逐条落地：

- **🔊 音量永不越权**：此前每次朗读/点小人都做"音频自愈"，音量低于 35% 就被自动拉到
  60%——你电脑的音量不该被应用偷偷改。现在自愈改**只读诊断**：只报告设备/音量/静音状态，
  绝不动手；设置页新增「允许自动调整系统音量」开关（默认关），明确同意后才会代调
- **💬 聊天不串岗**：删掉"你这句话听起来是想让我开工？"的提示卡——聊天里提到"方案/PPT"
  这类词不再被推销工作；**明确指令**（帮我写/做个/生成/开发…）在聊天里也直接干活不打扰，
  **弱提及**（只是聊到）就安心聊天。切栏目不再弹提醒、点开历史会话不再塞"已载入会话…"系统行
- **🛠 工作台见产出**：工作台此前只列脚本/项目，生成的图片视频漫剧"生成完就不见"。现在
  新增**创作产出**板块（🎨图片 / 🎬短片 / 📖漫剧），按最近修改排序、双击打开目录、与磁盘对账
  不留僵尸记录；分类条新增「创作」筛选
- **📚 自学知识用进创作**：资料库上网自学的知识此前只在聊天里引用；现在注入**出图提示词精修**
  与**漫剧企划**——自学不是为了聊天，是为了干活更专业
- **🎨 图片微调更专业**：基于上一张修改时按语义分级——轻改（亮度/柔光/稍微…）自动用
  低重绘幅度（denoising 0.35）**保留构图只调画面**，大改（换主体/换风格）才 0.62 放开手脚；
  提示词精修失败时以"上一张提示词+本次要求"兜底，不再丢掉原图语境
- **📖 漫剧分步专业流程**：告别"一条龙出一坨"——新流程 **企划 → 改镜 → 出图 → 配音 → 合成**：
  给题材先出分镜企划（落盘 story.json）；不满意说「第2镜改成 夜晚的霓虹街道」；满意说「出图」，
  某镜翻车说「重画第N镜」；然后「配音」「合成」出成片（播放器+MP4）。每步可停、可回改，
  切会话/重启接着做；不想分步说「一条龙」仍一键到底
- 回归：v0.17.3/0.17.4/0.17.5 冒烟全过（10+13+22 项），新增 v0.18.0 冒烟 48 项全过

## 0.17.5（2026-09-05）· 思考不再锁死界面：可中断 / 可新建对话 / 等待设上限

> 真机反馈：就问一句"晚上建议我吃啥好呢"竟等了 238 秒——期间不能打断、不能新建对话。
> 根源是"普通对话回合把整个界面锁死 + 模型等待无上限"。本轮拆锁：

- **⏹ 思考中可随时「停止」**：等回复时右下出现红色「⏹ 停止」按钮，点一下界面立即恢复、
  可发新消息或开新对话。底层模型等待改为"25 秒心跳分片"：点停止即时断开请求
  （本地 Ollama 会随连接断开真正中断生成），不再干等到天荒地老
- **💬 思考中也能新建对话 / 点开别的会话 / 切换栏目**：此前的"并发"只覆盖了学习与创作
  后台任务，普通对话仍会把整个界面锁成 busy，"新建/切换"一律提示"等回完再说"。现在对话
  改造成**回合制**：你的提问进入独立回合线程（上下文在发出那一刻已冻结），界面立即可用；
  它答完时若你已开新对话或切走，会自动把回复收进它原本所在的那条会话文件并浮窗提示，
  绝不写进你现在看到的界面
- **⏱ 等待设上限**：单轮模型响应预算整体收紧（聊天 240s→90s、工具轮 180s→120s、
  技能 300s→180s 等），到点给出可读的"超时"提示并引导点停止或换更小的模型——
  "问一句等 4 分钟"成为历史
- **🛡 顺带加固**：PASM 成长小训练在 torch 版本差异下偶发 inplace 梯度报错，会把整条回复
  拖成"出错"文案；已加防护——成长失败仅记日志跳过，正常回答不受影响
- 其它：切换栏目/开新对话不再弹出"它还在忙"；「📚 学新主题」在思考中也能发起（后台执行）

## 0.17.4（2026-09-05）· 不误解你的话：道谢不再误记待办 / 质问会认错 / 标题不切半句

> 真机回看与小U的聊天记录发现三处"读不懂人话"的硬伤，本轮根治：

- **🙏 道谢不再被当成"让我记待办"**：说"谢谢你提醒我下班，不过我还要再一会"——这明明
  是在道谢，却因为话里带"提醒我"三个字被子串误判成待办指令，还把这仨字从内容里剥掉，
  记成了变形句「谢谢你下班…」。现在先识别"谢谢…提醒"的道谢句式：回"不客气～"，绝不再
  建待办（仍含"帮我/请/麻烦+记/提醒"的强指令除外，照记不误）
- **🗑 误记可一键撤销**：对它有异议时回「撤销刚才那条」，立刻删掉最近新增的待办
  （新增"撤销刚才/删掉刚才…"口令，无需命中待办触发词）
- **💬 质问会先认错，不再甩宣传单**：质问"你怎么写记住啦？"之前因"问号结尾+含'记'字"
  被误判成"在问它能干什么"，输出整页能力介绍答非所问。现在识别"你怎么/你干嘛/凭什么…+
  记住/写成/当成"的质疑句式 → 先道歉解释（"是我理解错了"）+ 给撤销指引；`_cap_question`
  同步排除质问句式，能力清单只留给真正问"能不能做X"的句子
- **📄 会话标题不再切半句**：新会话标题从"机械截首句 18 字"改为：剥句首客套（好的/谢谢）→
  按标点断完整分句 → 超长加"…"。"已载入会话「好的，谢谢你…我还要再」"这类半句标题消失

## 0.17.3（2026-09-05）· 语音"还是无声"真根因修复 / 学习彻底不占会话 / 模型弹框不污染 / 小人性格动作回归

> 真机四连报的逐个收官：这次终于把"系统音能听到、但试听/点小人/朗读全部无声"的
> 真正根因挖出来了——不是音量、不是设备，而是代码级 bug。

- **🔊 朗读/试听/点小人"还是没声音"的真正根因（代码级）**：`tts.speak_text` 把
  `global _EDGE_COOLDOWN_UNTIL` 写在了外层函数、赋值却在内层工作线程里 → 线程一进来
  读它就抛 `UnboundLocalError`，**所有 auto 语音（试听声线/小人说话/朗读开关）在后台
  线程里静默崩溃**——不响、不落日志、连失败提示都没有；而系统音（winsound）不走这条
  链路所以"系统音能听到、别的全哑"。修复：`global` 归位到工作线程内；同时兼容
  edge-tts 新版 chunk 字段（audio/data）；SAPI 直接发声失败时新增第二兜底——
  **System.Speech 先合成 WAV，再用与"系统音"完全相同的 winsound 通道播放**，
  把语音输出绑定到已经验证可听的通道上，杜绝再次出现"说完了却没声音"
- **左下角「模型」检测/切换不再写进聊天框**：点「📡 模型」或切换「大脑」下拉，结果
  一律用**独立弹框**展示（检测到哪些本地模型/当前大脑是什么），不往聊天记录里写任何
  一行、不影响正在进行的对话
- **资料库「学一个新主题」彻底移出聊天会话**：v0.17.2 已不写聊天内容，但还会把界面
  置为"思考中"（busy）→ 出现"没输入却莫名正在思考、左侧会话点不动（提示还在处理
  上一条）"。v0.17.3 起学习只走**自主后台线程**：不占用 busy、输入框不禁用、左侧会话
  随时可切换，开始时右下角浮窗提示、学完只弹结果框；学到的内容照常进知识库
- **桌面小人恢复"按性格的随机自发小动作"并让它被看见**：调皮/活泼/外向 → 会自己踢球、
  蹦跶、跳舞；温柔/内向 → 会安静地坐下看书；沉稳 → 看书或散步。动作按性格能量加权
  随机出现（更频繁且可持续数秒），并偶尔冒一句小气泡（"自己踢球玩会儿""安静看书"），
  不再只有散步/睡觉/别打扰三种状态；小动作与真实读书学习互不干扰、不打断自动化

## 0.17.2（2026-09-05）· 栏目线程强隔离 / 声音自愈诊断 / 图生图原地优化 / 开发原地改

- **🔊 声音：从"能播"到"听得见"（音频自愈+诊断）**：此前系统音 winsound 播放成功
  却听不到——真机探测发现默认输出端"音量仅 20%"等原因。新增音频自愈模块：每次系统音/
  朗读前自动读取默认播放设备，**发现被静音自动取消静音、音量过低自动拉回 60%**；设置页
  「🔔 系统音」会展示真实诊断（输出到哪个设备 / 音量多少 / 是否静音 / 是否指向无声的
  显示器输出），不再只回一句"已播放"
- **栏目线程强隔离（切栏目=换对话）**：每个栏目（聊天/文案/表格/PPT/Word/开发/视频/
  图像/漫剧）现在拥有**自己独立的对话线程**——切换栏目即切换到该栏目自己的对话记录框，
  别的栏目内容不再残留、也不像"新对话丢了上下文"；点开左侧会话自动跳回所属栏目并完整
  载入；空会话（没聊过内容）不出现在左侧列表
- **对话列表右键删除 + 不再顶置**：右键任意会话可「🗑 删除」，二次确认（只删记录、
  不影响已生成文件）；点开/载入会话不再把它顶到列表第一位（列表始终按最后活跃排序）
- **🎨 图像"在上一张基础上改"（不再重画一张不相干的）**：出图后接着一句"让光柔和些、
  加两只小动物"，会自动**基于上一张图修改**——本地 Stable Diffusion WebUI 走真实
  img2img；云端引擎（无图生图接口）则带上上一张的画面描述、以延续风格同目录续画并
  如实说明；同一文件夹内编号递增、永不覆盖旧图，方便对照迭代
- **🖥 开发"在原项目上改"**：再次提开发需求默认**读入原项目代码、原地更新**（改动前
  的旧文件自动归档到「旧版_时间」），不再每次另起一个没关联的新项目；明确说
  "另建一个/新开一个/换个项目"才新建
- **提示不打扰对话**：资料库「学新主题」改为后台学习+结果弹框，不再跳到对话页把学习
  过程写进聊天；左下角切换模型改为右下角浮窗提示，同样不污染当前对话
- 会话文件新增 `_slots.json` 记录栏目↔会话归属（列表自动跳过该元文件）

### 上一版（v0.17.1）重点

- **🔊 系统音测试修好（声音第一道门）**：原实现经 PowerShell `Add-Type System.Media`
  播放系统提示音，真机 voice.log 证实该程序集在打包环境 try 内加载必失败 → 误报
  「系统音测试错误」且朗读自查失去基准。已改为 **Windows 原生 winsound 通道**
  （MessageBeep → 系统 Notify.wav → 系统通知别名 三级兜底），不再依赖 PowerShell
- **会话 ↔ 工种栏目联动**：每条会话记住它所属栏目（图像/视频/漫剧/…），左侧会话列表
  显示栏目图标；点开任意历史会话 **自动切回对应栏目**——不再出现"点开图像会话还停在
  视频栏目"；切换栏目不清空当前会话，旧记录始终在左侧可点回
- **同会话创作不另起新项目**：同一会话里"优化上一幅图"自动复用该会话图片文件夹、
  图号递增续写（图01→图02/03，不覆盖旧图）；成片（漫剧/短片）"在这个基础上重做"
  也复用同一文件夹，上一版 图/配音/播放器 自动归档进「旧版_时间」子目录便于对照；
  目录归属随会话文件保存，切换会话/重启各归各位
- **漫剧/短片成片更有"动画感"**：播放器升级——奇偶镜交替推拉/平移运镜、镜头间
  淡入转场、字幕上浮显现、点画面翻镜、←/→ 方向键翻镜、连放按配音时长驱动

## 0.17.0（2026-09-05）· 创作引擎接入：图像/视频/漫剧真出成品 + 全工种体检

- **图像工种真出图**：点「🎨 图像」说画面需求 → 调用接入的文生图引擎直接生成 PNG
  到 `桌面/PASM创作`，聊天里实时预览；支持按需求一次出 1~4 张
- **视频/漫剧一条龙成片（链路全跑通）**：选「🎬 视频 / 📖 漫剧」给题材 →
  ① LLM 写结构化企划（片名/画风/3~6 镜：旁白+字幕+英文出图提示词）→
  ② 每镜调用图像引擎出图 → ③ edge-tts 逐镜配音存 mp3 → ④ 自动生成可放映的
  「播放器.html」（画面+字幕+配音+上一镜/下一镜/全部连放，浏览器即放，零依赖）；
  检测到本机 ffmpeg 时再合成真实 **MP4 成片**，全程在对话里汇报每镜进度
- **创作引擎懒接入（用到才配，一次配置永久生效）**：不用提前填任何东西；
  首次使用图像/视频/漫剧时自动弹出接入框 → 支持预设「硅基流动 SiliconFlow
  （OpenAI 兼容，推荐）」/ OpenAI DALL·E / 自定义 OpenAI 兼容服务 / 本地
  Stable Diffusion WebUI（免 Key），含地址/Key/模型/画幅/配音音色与「测试连接」；
  填好自动保存到设置 cfg["engines"]，之后再创作零配置；设置页也加了「创作引擎」
  入口可随时改
- **取消接入不空手**：不想接时降级——图像给完整中英文提示词包、视频/漫剧走
  「分镜脚本」技能产出可拍脚本，并说明接入后同样的话能直接成片
- **聊天里也能直接下令**：干活模式下说「画一张…」「生成一个…短视频」「做一部漫剧…」
  会自动识别并开工；聊天模式则给可点的开工入口（不擅自动手）
- **文案/表格/PPT/Word/开发 全工种体检修复**：生成文件前剥掉模型外套的代码块与
  客套话、校验正文与文件有效性（拒绝"空壳假文件"）；技能文档（video-script /
  manga-pipeline）更新为引擎接入后的真实口径；意图分流回归（写脚本/文案不会被误判成出图）

## 0.16.7（2026-09-05）· 语音终极根因修复 / 工作台右键与删除可选 / 资料库 Markdown

- **语音终于真正出声（终极根因）**：排查发现 System.Speech 脚本里的**声线选择代码因
  花括号转义错误从未执行**——ps1 模板里 `{{`/`}}` 本应为 `{`/`}`（模板用 replace 非 format，
  双括号被原样写入），PowerShell 把循环/选择整段当字面量跳过，朗读一直用**系统默认声线**
  （本机默认非中文 → 读中文无声）。修复后本机验证：选中 `Microsoft Huihui (zh-CN)` 朗读
  SPEAK_OK；声线选择失败时自动回落任一中文声线
- **语音诊断日志 voice.log**：每次朗读记录引擎选择/网络探测/合成结果/选中的声线/
  PowerShell 输出与错误 → 真机无声时一眼定位，不再反复猜
- **设置页「🔔 系统音」测试按钮**：播放 Windows 系统提示音，一键区分"应用问题还是
  系统扬声器/音量问题"；试听成功也显示 ✓ 反馈（不再无声无息）
- **工作台右键彻底修复（真根因）**：右键菜单代码漏 import QMenu → 一点右键即
  `NameError: QMenu` 弹不出菜单——这就是"列表内容删不掉"的直接原因；会话列表右键
  一并修复（同样漏 QMenu）
- **条目删除可选（二次确认）**：右键按条目类型给动作——已读文件=删除阅读记录(保留源文件)；
  脚本=删除脚本文件(永久删)；项目=「仅删除记录(保留目录)」或「删除项目与目录(不可恢复)」；
  空白/分类删除弹窗二选一：仅删记录(保留文件) / 连文件一起删除；删除前都有明确确认
- **资料库阅读器 Markdown 排版**：双击条目/书架内容按 Markdown 渲染——标题/列表/代码块/
  引用分块、段落行距清晰，长文不再"一堆文字看不清楚"
- **聊天 Markdown 渲染增强**：标题/有序无序列表/引用块/段落行距，代码块样式优化

## 0.16.6（2026-09-05）· 语音彻底修复 / 工种栏去标题 / 工作台视觉与删除 / 斜杠快选

- **语音彻底修复（点小人与试听真出声）**：根因是 `auto` 引擎先试在线 edge-tts，而部分网络
  访问 edge 服务卡死在 DNS 解析阶段（实测挂死 240s+），把后面的系统语音兜底与失败提示
  全部堵死 → "点了没声音、也没任何提示"。现新增**秒级网络预检**：后台线程约 0.2s 探测
  edge 服务，连不上立即回落 Windows 系统语音（实测 6s 内出声），60s 内不重复傻试；
  去掉"失败一次永久静音"的锁，改为失败限频提示且每次都重新尝试
- **工种栏去掉组标题**：「📄 文档 / 🚀 开发与创作」组标签删除，聊天 + 文案/表格/PPT/Word +
  开发/视频/图像/漫剧 九个按钮直接流式排列，更清爽
- **工作台视觉与删除重构**：删除键改为嵌在分类胶囊**右上角 ✕ 热区**（可删分类才有；
  「全部」不可删）；点胶囊主体=选中、点 ✕=确认后删整类含内容；顶部按钮统一高度对齐；
  列表**磁盘对账**——外部删掉的文件/项目在刷新后自动消失，不再残留僵尸记录
- **输入框圆角 + 斜杠快选**：输入框圆角 14px、聚焦蓝描边；输入 `/` 弹出快选窗
  （⚡指令 / 🎯切工种 / 🛠技能 三组），可打字过滤，↑↓/Tab 选择、Enter 或点选立即执行、Esc 收起

## 0.16.5（2026-09-05）· 资料库重构为真知识库 + 学习链路修复 + 上轮六修
- **学习链路修正（核心）：「学到 = 原文入库」，提炼要点只是学完后的速记索引**：
  - 资料库点「让它学一个新主题」不再因"内容质量太差、提炼不出要点"被回绝——那是把
    "提炼"错当成了"学习"的闸门。现在多源搜索抓到内容即**全文入库**（同时写入书架），
    模型不可用/资料杂自动落句子级兜底要点，学习永远不被要点卡死
  - 聊天/小人两处学习链路统一 store-first，抓取上限提升至 1.6 万字
- **资料库变成真的知识库**：
  - 双击条目打开**全文阅读器**：元信息（来源/分类/掌握度/关联）+ 要点索引 + 📄 原文全文
  - 底部新增**书架**：双击直接读整本书/整份原文（Python 基础这类原始资料可完整翻阅）；
    条目没存原文的旧数据自动回填同名书架书
  - 顶部关键词过滤（标题/分类/正文）；条目显示掌握度/要点数/**原文字数**
  - 复习巩固路径把全文写回知识库，早期"只存要点"的老条目逐步补齐原文
- **自动化动态只记真实结果**：删除"学习开始前就写『自动上网学习』"的假日志；
  失败如实记"没连上信息源"（不入库），成功才记"原文已入库"（资料库同步可见）
- **工种分栏归位与分组（上轮六修①）**：FlowLayout 按父布局真实区域摆放——工具条回到
  聊天内容下方/输入框上方，不再叠到顶部挡小人形象；去掉「干活」切换栏；
  📄文档（文案/表格/PPT/Word）与 🚀开发与创作（开发/视频/图像/漫剧）分组展示；
  新增视频/漫剧走技能、图像出文生图提示词包（如实说明绘图引擎未接入）
- **语音兜底改系统自带引擎（上轮六修②，静音根因修复）**：edge-tts 网络不可用时原
  pyttsx3 打包缺驱动导致全静音；现兜底改为 Windows 自带语音（PowerShell + System.Speech，
  零依赖），离线也能出声；试听/点小人失败给明确气泡与弹窗提示，不再无声无息
- **改名即时生效（上轮六修③）**：小人每 5 秒同步配置——改名后头顶气泡/称呼/左侧品牌名刷新
- **工作台改造（上轮六修④）**：列表右键删该条、空白处右键删当前分类全部、每个分类右上角
  「−」删除键（「全部」不可删）；有内容先确认再删，连内容一并删除（仅限工作区/脚本目录内
  文件物理删除，已读文件只移除记录）
- **导航可回当前对话（上轮六修⑤）**：左侧导航首位新增「💬 对话」入口；默认落在对话页；
  点会话列表任意条目或新建会话时自动跳回对话页

## 0.16.3（2026-09-05）· 界面四修：会话列表 / 工种分栏 / 快捷命令 / 语音人格化
- **左侧会话列表去滚动条 + 单行省略**：垂直滚动条隐藏（滚轮仍可翻页），每条会话
  超宽按像素自动加 `…` 截断（ElideListDelegate），鼠标悬浮可看完整标题
- **工种分栏改真·流式布局（FlowLayout）**：一行排得下就排一行、排不下才自动换行，
  窗口拉宽自动并回一行——根治"每行写死 3 个只显示三个就换行"；按钮两档样式美化
  （💬聊天/🔧干活 主蓝色高亮，其余工种浅灰圆角，hover/选中态齐全）
- **快捷命令**（`/` 开头，不占聊天上下文）：
  `/clear` 清空当前聊天重新开始（内容先归档进长期记忆，不彻底丢）、`/new` 新对话、
  `/mode 文案|表格|PPT|Word|开发|聊天|干活` 切换工种、`/voice 开|关`、`/quiet`(别打扰)、
  `/call`、`/skills`、`/help`；未知命令给提示
- **语音人格化 × 情感（重点）**：
  - 根治机械音根因：edge-tts 每句失败要傻等 25s 才静默回退 SAPI（且系统常只有一个
    中文音 → 性别/年龄听不出差别）。现在**连续失败 90s 冷却**快速回落，不再卡顿
  - **真童声**：幼儿/童年不再用成年音硬顶高音——edge 真童声（女 Xiaoshuang / 男
    Xiaoyou）奶声奶气；少年男 Yunxi / 女 Xiaoyi，青年男 Yunjian / 女 Xiaoxiao
  - **情绪语气通道**：按当前情绪（开心/兴奋 → 语速快音调上扬，委屈/低落 → 低沉，
    生气 → 急促大声，困倦 → 拖慢放缓）自动微调语速/音高/音量
  - 设置页新增 **「🔊 声线试听」**：选幼儿/童年/少年/青年 + 性别 + 性格，立刻念一句验证
- 0.16.2 与 0.16.1 内容一并合入（见下）

## 0.16.2（2026-09-05）· 工种分栏纯净版 + 真技能(SKILL.md) + LLM 网关
- **工种分栏回归「干活=帮我工作」**：分栏只保留 💬聊天 / 🔧干活 / ✍️文案 / 📊表格 /
  📽PPT / 📄Word / 🖥开发。**自学与技能不再放进干活分栏**（自学在聊天里说
  「上网学一下 xx」即触发；技能在左侧「🎓 技能」页管理、聊天与干活中按需自动检索）
- **技能升级为标准 SKILL.md（对齐 WorkBuddy/QClaw 技能与专家模式）**：
  frontmatter 齐全（name/description/keywords/category/version/author），支持
  技能包目录 `<技能名>/SKILL.md`；可**导入外部 .md 技能文件**、查看原文件并复制、
  按模板分步添加（触发词单独录入→检索更准）；内置三技能补 version/author 元信息。
  单机技能体系与 WorkBuddy SKILL.md 同构，为后期网络版「技能广场」铺路
- **新增统一 LLM 网关（llm_gateway）**：所有 LLM 调用收敛一处——客户端连接复用、
  指数退避+抖动重试、错误分类（Key 无效 401 / 模型未装 404 / 超时 / 限流 429 均
  转为可读中文）、finish=length 自动续写统一化
- **任务路由**：按任务给参数画像（聊天/工具轮/技能创作/自学提炼/自测/小人搭话/
  预热），重任务给足 token 与续写、轻任务小 token 低温度短超时
- **并发闸**：本地 Ollama/llama-server 单模型**串行排队**（并发 1，防同时打爆/抢显存），
  云端宽松（并发 4）；排队超过预算给出人话提示而非无限卡死
- 干活/具体工种模式下"选中即干"，全程不再弹能力清单串符
- 0.16.1 修复一并合入（见下）

## 0.16.1（2026-09-05，随 0.16.2 合入发布）· 六点真机修复
- **左侧改三栏**：上方 = 任务 / 工作台 / 自动化 / 资料库 / 记忆 / 技能（技能与
  任务同组）；中间 = 会话列表（新对话在列表内新建，不另占列）；下方 = 设置
- **「别打扰我」真安静**：散步与蹦跳定时器加安静/动画守卫——它会躲到屏幕最右
  边不再来回走动；安静期间整点/待办提醒只写聊天窗不发声
- **模型选择下移**：与底部模型状态同区（当前大脑 + 切换下拉同一行）
- **干活改工种分栏（仿 WorkBuddy）**：聊天/干活等 8 个工种按钮化，选中工种后
  输入即按该工种定向执行，不再靠猜、不再弹"小U能帮你…"串符
- **语音按性格×性别×年龄自动切换**：SAPI/edge-tts 按女生/男生挑声音，婴儿期奶声
  奶气、少年期少年音；性格定基调（活泼语速快、温柔语速慢文静）；音色设置页面可选
- 大输入框/会话持久化/意图修正等在 0.16.0 已含

## 0.16.0（2026-09-05）· 界面与自主能力大版本
- **【聊天/干活】模式分栏**：输入框上方一键切换。聊天模式凡命中开发项目/写代码/
  PPT/Word/Excel/上网学/技能 → 绝不执行，只提示+可点开工入口；根治"聊着聊着
  突然去开发项目"（原误触发把文档需求当成全栈项目开工）
- **多行大输入框**：类 WorkBuddy 尺寸，Enter 发送 / Shift+Enter 换行
- **大模型切换**：对话页头部下拉 = 🔄自动 / ☁️云端(有 Key) / 🤖各 Ollama 模型，
  选择持久化，选本地模型自动后台预热
- **左侧会话列表 + 聊天永不丢**：每说一句/每回一句实时整段落盘到会话文件；
  新对话/切会话/重启都保留全文；启动自动载入最近会话；送模型仅截最近 40 条
  （完整记录永远在）；右侧仍可管理全部历史
- **技能系统（Skill）**：新增技能库（数据目录可随时增删，重启保留），PASM
  干活时自动检索匹配技能并按其规程执行；内置示例：视频脚本创作/营销文案/
  漫剧一条龙（演示技能互相调用）。技能里"出图/渲染成片"等未接入能力如实标注
- **意图修正**："帮我写个短视频/口播/漫剧…脚本" → 走创作技能，不再误入
  python 写码；python/代码语境明确跳过技能
- **知识库 v3（全文+要点双层）**：每条知识存全文(可回取原文)+要点索引+来源，
  注入带〔原文摘录〕；上网自学/自动学/读书全部原文入库；提炼接真模型
  （不可用才句子级兜底，绝不假装学会）
- **自主进化（默认开启）**：自动上网学默认开；固定 10 主题换成好奇心选题器
  （避开已学+延伸进阶）；学后自测 3 问，答不出记入"待弄懂清单"不假装学会；
  动作全量进日记可审计
- **桌面小人情绪与人格**：情绪状态机（平静/开心/委屈/生气/害羞/犯困/好奇/得意，
  各配专属眼型/眉/泪珠/腮红/氛围光）；事件驱动反应（被夸开心蹦跳/跳舞，被凶
  委屈，骂错归因→先鼓脸生气再消气）；"别打扰我"→躲屏幕边缘、隔一会儿探头
  偷看再缩回，喊名字/点击即回；7 套性格档案×行为偏好（内向爱看书、活泼爱
  散步踢球）；性别=外观(蝴蝶结/护目饰带/中性)+自称语气，桌面小人/聊天头像实时同步
- **移除向下兼容（v0.13 遗留）**：不再兼容 Win7/8，去掉 PyQt5 回落与 Qt5 适配层
  → qt_compat 固定 PySide6 单后端；安装器加 MinVersion=10.0（旧系统安装时即明确
  提示，不再装完双击报启动错误）；启动失败改为可读中文提示并落 crash.log
- 底层：修复知识库关联联想对 set 切片的潜在崩溃；TTS 长文本分块朗读不中断
  （v0.15.1 的修复随本版一并合入）

## 0.15.0（2026-09-04）· 认知大脑版
- **会话级认知状态机**：每次回复前先判断你的意图（任务/提问/倾诉/收尾/接着上次）与心情，
  用情绪调制语气与回复温度（低落更稳、开心更活），跨轮维护话题与"正在推进的目标"，
  重启后还能接上——不再千人一面
- **分层记忆**：情景记忆自动归档（每 8 轮/新会话/退出时）、办事经验自动沉淀进程序记忆；
  无需模型主动调工具，检索结果自动拼进上下文——跨重启也能想起上次聊到哪、上次怎么干成的
- **计划-执行-验证环**：脚本任务升级为"写→跑→校验→报错自动带错修复重跑一次→仍败才如实反馈"；
  成功/失败都沉淀为程序记忆
- **引擎可选化**：有 torch 走完整七层引擎，无 torch 自动降级轻量认知体（接口一致）；
  安装包 143MB → **45MB**（单文件直传，无需分卷）
- 历史窗口 12→16 轮；修复 LLM 调用把本轮消息重复追加一次的上下文污染
- 新增认知执行皮层核心包 `pasm.cognitive`（cog + memory_layers）与 `pasm.light`，
  与桌面文件镜像同源（详见私有核心仓库文档）

## 0.14.0（2026-09-04）· 上下文与工具打通
- **上下文关联**：记住本会话最近路径/文件，支持"刚才那个文件 / 那个文件夹"别名引用
- **打开电脑应用**：内置别名（微信/计算器/记事本/PowerShell…）→ System32 → 路径 →
  开始菜单快捷方式 → PATH 白名单（不执行任意命令拼接）
- **打开聊天里的路径**：文件用默认程序打开、文件夹开资源管理器
- **读路径下的文件**：`resolve_path()` 容忍口语尾巴（"…路径下的文件有没有问题"也能定位）；
  读文件夹自动抽取最多 6 个可读文本文件内容
- **意图分流修正**：纯能力询问（"你能读文件吗"）只答不干活；"打开计算器吧"不再把语气词
  吞进应用名；"什么"不再误判成疑问句拦截带路径指令
- 退出不再误弹"很抱歉，程序启动时发生错误 0"（根因：sys.exit(0) 被 except BaseException 兜住）

## 0.13.0（2026-09-04）
- 界面升级为 qclaw 式多面板：对话 / 任务 / 工作台 / 自动化 / 资料库 / 记忆
- 文件生成：直接产出 PPT / Word / Excel（说出主题就生成），可保存到任意路径
- 全栈开发：生成并运行完整项目——前端页面 + 后端服务 + 数据库脚本
  （支持 Python / JavaScript / HTML / Go / Java / Node，不再局限 Python）
- 修复回复不完整：max_tokens 提升至 1500 + 空回复自动重试
- 持续自学闭环：自动上网 30 分钟一课、主题自动进阶、学完自动成书入库；
  聊天按话题自动引用所学知识（学了金融，聊金融就更专业）
- 语音输入不再弹 PowerShell（进程内识别+提示音+重试兜底）；朗读开关修复
- 成长阶段聊天窗与桌面小人统一；宠物体型/配色/天线随阶段明显变化；双击小人开窗
- 启动入口修复：以真正产品（桌面小人+对话窗）为打包入口，异常自动写 crash.log
- 升级检查与下载页改到公开仓库，外网用户也能收到更新提示

## 0.12.0（2026-09-04）
- 能读电脑文件：txt/md/json/py/js/html/docx/xlsx/log 读入分析、总结、关联上下文
- 找文件/读目录；写即运行的脚本沙箱目录（自定义脚本库）
- 语音输入按钮（需系统中文语音识别引擎）；内心面板显示情绪/性格/成长阶段
- 系统提示增强：明确知道自己能读文件、能编程、能干活，不再说"做不到"
- 修复本地模型回复截断（max_tokens 256→700）

## 0.11.3（2026-09-04）
- 修复自主学习不生效：缺 threading 导入致读书/自动上网静默崩溃
- 读完书立即规则提炼入库(秒级)，后台再本地 LLM 精炼增强
- 知识库为空时自动化会自动先读书（学习永不空转）
- 打盹不影响复习/日记/自动上网等认知学习；右键可见知识库条数与今日读书数
- 自动上网开启后 60 秒先试学一个主题

## 0.11.2（2026-09-04）
- 安装器根治 code5：改为「仅当前用户」安装到 %LOCALAPPDATA%\Programs\PASMStudio
  （无需管理员、不写 Program Files，绕开拒绝访问）；数据仍在 %APPDATA% 不受影响

## 0.11.1（2026-09-04）
- 安装器：自动结束旧进程（修复 code5 拒绝访问）、请求管理员权限、简体中文向导

## 0.11.0（2026-09-04）
- 双脑总线 B3：LLM 按需调用 recall/verify/learn/mood（工具化，记忆按需回填）
- 真机验证：本地模型主动 recall「周三不加班」并正确回答

## 0.10.0（2026-09-04）
- 工具智能：帮我找文件 / 分析文档方案 / 写并运行 Python 脚本 / 上网学话题
- 自动化工作：23点成长日记、10分钟复习巩固、自适应阅读频率
- 自动上网自学开关（约90分钟学一个主题）
- 知识库 v2（0.9）：自动分类/重读巩固/交叉联想

## 0.7.0（2026-09-04）
- 会成长：幼儿→童年→少年→青年（体型/天线随阶段变化，经验驱动）
- 三款皮肤：科技蓝/萌糖粉/机甲金（右键切换）
- 自主生活：散步之外会自己翻开书阅读（记读书笔记）；12:00/18:00 语音提醒吃饭/下班
- 气泡改科技玻璃风；修复语音只能响一次（每次重建语音引擎）

## 0.6.0（2026-09-04）
- 形象重做：全息科技风机器人（金属渐变、LED 辉光眼、发光猫耳、全息天线环、悬浮粒子、情绪光晕）

## 0.5.x（2026-09-03）
- 桌面独立小人：常驻桌面/无边框置顶/可拖拽/偶尔散步；单击说话冒泡，双击打开完整对话窗
- 手势与交互修复：单击/双击/拖动改由头像控件直接处理；单实例锁防重复窗
- 小人行为动画：走路摆腿、被冷落45s打盹(Zzz)、互动唤醒；右键菜单

## 0.4.x（2026-09-03）
- 角色上线：Q 版小人（会眨眼、情绪表情、说话张嘴、点击回应并朗读）
- 修复朗读无声：改进程内 SAPI(pyttsx3)，不再依赖外部 shell；失败自动提示原因
- 数据面板：录入你的偏好/日程/事实（参与对话，可删）；拟人流式打字；历史会话
- 清理旧版本发行（Gitee 附件配额限制，仅保留最新）

## 0.3.x（2026-09-03）
- 对话版上线（双脑：LLM 语言脑 + PASM 认知脑）
- 本地即服务器：自动检测并接入本机 Ollama（免 Key 直接对话，自动选模型）；无本地模型退回「离线微脑」
- 启动即后台预热本地模型；空回复自动重试；修复后台线程回调丢失（Qt 信号回主线程）
- 会话恢复/新会话、Markdown 渲染、记忆管理器、状态栏显示当前模型与思考秒表

## 0.2.0（2026-09-02）
- 首发实验版（网格养成），已被 0.3.x 取代
